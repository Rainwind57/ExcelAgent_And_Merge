# Step1/Step2/Step3 准确率与速度优化 TODO

> 约束：全部方案走「代码框架 / Agent 组成」层面（LLM 参与决策、结构化数据注入、
> 已有 SubAgent 能力复用），**不写业务硬编码规则**（不写表名/列名专用 if 分支，
> 不写新正则模板去猜语义）。每条 fix 都必须能说清楚"数据/判断来自哪个已有
> 结构化来源或哪次 LLM 调用"，说不清楚就是硬编码，打回重设计。

来源：
1. 「兽潮来袭」真实失败案例（9 段任务链，8/9 成功，6 项失败未解决）—— 本文档主证据。
2. 此前对话已确认但未落地的优化点（schema 动态裁剪、双路对抗采样等）。

---

## 案例复盘（原始证据，所有 TODO 项据此展开）

输入：新建任务组 830 + 3 个任务环（Quest 链）+ 3 场战斗（combat_data）+ 3 个
怪物刷新点（interaction，含坐标/model_id/技能/AI），共约 12 个实体，3 张表
（quest / combat / interaction）。

结果：Step1 报"已解析 9 个子任务"，Step3 执行 8 成功 1 失败，Step4 汇总另有
**6 项失败未解决**：

```
❌ interaction/Interaction: 列[3005: 目标space ID] 类型 float，值 '320,0,220'
   无法转浮点，已阻止写入（partial_write，跳过字段）
❌ 第6段「刷兽潮前锋model_id 1120、技」未能解析出意图
❌ 第7段「25083002 坐标 (310,0,2」未能解析出意图
❌ 第8段「刷兽潮首领model_id 1121、技」未能解析出意图
❌ 该 add 无字段值且未被本批消费，已丢弃（raw=刷兽潮前锋...aggressive_ai；）
❌ 该 add 无字段值且未被本批消费，已丢弃（raw=刷兽潮首领...boss_ai；）
```

净效果：**3 个怪物刷新点（兽潮前锋/兽潮首领/兽王）里，2 个完全丢失，1 个只
写入编号缺坐标**。用户三场战斗的怪物刷新数据链路基本报废，但顶层汇总说
"成功 8 / 失败 1"，严重低估真实损失（6 条子问题被压缩成 1 条 partial_write）。

### 根因链（按证据强度排序）

1. **`multi_intent_splitter.py` 纯规则切段，无语义完整性校验。**
   三个怪物刷新点描述（`25083001 坐标(...) 刷「兽潮前锋」model_id.../技能.../AI...；`
   ×3，仅用「；」分隔）在切分后，有的段丢了前半（坐标部分去了别的段），有的段
   丢了后半，导致每段单独喂给 DecomposeAgent 时都不是一个语义完整的实体，
   产不出可执行 intent。**分段器是全正则实现（标点/连接词/编号列表匹配），
   完全不理解"这三句结构相同、每句是一个独立实体"，也不检查切完的每段是否
   还是一个完整从句。** 这是本案例最大丢数据根因。

2. **段级"0 条重跑"机制没覆盖"产出空壳 intent"这种情形。**
   现有覆盖对账只在某段**完全产 0 条**时重跑；但案例中段6/8 各产出了一个
   "无字段值"的空壳 add intent（日志：`该 add 无字段值且未被本批消费，已丢弃`），
   这种"产了但产不对"的情况被直接丢弃，不触发任何补救（合并邻段重跑/定向重问）。

3. **Step3 类型转换失败时只会跳过字段，不会回 Step1 重新判定字段形状。**
   坐标数组 `[320,0,220]` 被当成一个字符串塞进声明为 `float` 的单值列
   `3005`，类型转换失败→整字段跳过。系统从未检查"这张表的 3005 列附近是否
   本来就该拆成 x/y/z 三列"或"这列历史数据是否本就是逗号分隔字符串"——
   这类"形状再判定"完全没有兜底路径，纯粹丢数据。

4. **同一列 `3005` 在两处日志里显示了两个不同的中文标签**
   （类型转换报错处显示"目标space ID"，跳过字段汇总处显示"目标位置"）。
   这说明该 effect.key 数值码到列显示名的解析在两条代码路径上不一致，
   需要复核 `produces_inference`/coerce 校验两处是否读了不同版本的表头缓存。

5. **Step2 的引用一致性校验只查 `<placeholder>` 形式引用，不查字面量引用。**
   `combat.主怪=25083001` 这类字面量 ID，理论上应该在本批次或库里存在一条
   `interaction.编号=25083001` 的对应记录，但案例里 25083001/25083002 对应的
   interaction 记录根本没生成（被根因1吞掉），Step2 没有报出"这个字面量引用
   查无实据"，只在 Step4 事后从执行失败侧面反映，用户看不到"引用断裂"这个
   直接结论。

6. **Step4 顶层汇总把"6 条子问题"折叠成了"1 个失败"的错觉。**
   "已完成 9/9 · 成功 8 · 失败 1" 的头部数字和下方"6 项失败未解决"信息量
   不对等，用户一眼看到"失败1"会低估问题规模。这是汇总口径问题，不是决策

### T3. 字段类型/形状不匹配时的一次性重判定（治根因3）

现状：Step3 `coerce_value` 转换失败 → 跳过字段 → 结束，没有回头路。

修法（不硬编码"坐标类列名要拆3列"这种规则）：
- 转换失败时，触发一次**定向 LLM 重判定**：把该字段所在 sheet 的**真实表头
  片段**（该列前后各 2-3 列，含列名+类型行）+ 原始取值 + 失败原因回给 LLM，
  问"这个值应该怎么落到这张表的哪一列/哪些列"。LLM 看到的是真表头，不是
  代码猜的规则，符合"数据驱动而非硬编码"要求。
- 只对**转换失败**这一条件触发（零成本增加，不影响成功路径），且只重判定
  1 次，避免死循环。
- 若重判定仍失败，按现状跳过字段但保留原始值到 warning，不静默丢弃语义。

### T4. Step2 引用一致性校验扩展到字面量引用（治根因5）

现状：`validate_two_layer` 只查 `<placeholder>` 形式的 produces/consumes 引用，
不查字面量 ID 之间的交叉一致性。

修法：Step2 收集**本批次所有子任务将要产出的字面量主键值**（如
`interaction.编号=25083001/25083002/25083003`），与**本批次所有子任务引用的
字面量外键值**（如 `combat.主怪=25083001`）做一次集合比对——引用值不在
"本批次产出集合"里、也不确认在目标表已存在（可选：调用现有行查找能力核实），
则报 soft warning"引用的 ID 在本批次和现有表中均未找到对应记录，确认是否
预期"。这是集合比对，不是硬编码业务规则。

### T5. Step4 汇总口径修正（治根因6，纯展示层，低风险）

汇总头部"成功 N / 失败 M"的 M 应该等于下方"失败未解决"列表条数，而不是
按"子任务级 1/0"计数。修法：`step4_conclude_subagent.py` 汇总时统计
`len(failures)` 而不是 `子任务失败数`，让头部数字如实反映问题规模。

---

## P1：此前已确认方向，尚未落地

### T6. Schema 裁剪从"候选数固定阈值"改成"内容重要性驱动的动态预算"

现状：`_build_schema_block` 的 `max_sheets`/`max_cols` 按候选数分桶
（≤3/3-5/>5 → 固定 4/3/2 sheets、16/10/6 cols），是固定阈值，不看 prompt
实际长度，也不看列的关系重要性。

修法：去掉候选数分桶，改为**贪心按字符预算填充**：
```
优先级序：column_signal 命中列 > PK 列 > FK 列 > 其它列
从高优先级开始逐列累加渲染后的字符数，达到 CODEMAKER_DECOMPOSE_SCHEMA_CHAR_BUDGET
（如 3000）即停，不再看候选表数量
```
关系列（已在上一轮实现 `[PK]`/`[FK→...]` 标注）不再因"候选数多"被机械砍掉，
边界完全由内容重要性决定。改动点：`decompose_agent.py::_build_schema_block`。

### T7. 双路并行采样 + Step2 打分择优（对抗式自校验）

现状：单路 LLM 单次调用，产空才重采一次，没有"两路独立结果比对"。

修法：`_decompose_single_prompt` 内用 `ThreadPoolExecutor(2)` 同 prompt 双采样
（同模型两次独立调用，利用 LLM 自身非确定性）：
- 两路结果结构一致（表/字段集合相同）→ 直接采用，零额外延迟感（并发耗时
  ≈ 单路耗时）
- 不一致 → 都送入 Step2 现有校验（`validate_two_layer` + 结构守卫），取校验
  通过项更多/错误更少的一路；两路都有问题则合并取并集再校验一次
- 全程复用 Step2 已有校验逻辑打分，不新写"谁对谁错"的业务规则

### T8. 失败重试携带定向线索，而非盲重采

现状：LLM 产空/产可疑结果时，重试是"同 prompt 再采一次"，不带上"哪里可能
错了"的线索。

修法：重试 prompt 追加一句由**已有结构化信息**生成的定向提示，例如：
"上一轮候选表 X 的 FK 列 Y 未被任何输出引用，检查是否遗漏该表的写入"——
这句提示的内容来自 `fk_edges`/`candidates` 结构比对（代码算好的事实），
不是新写的业务判断规则。

### T12. 分段与粗定位合并为一次 LLM 输出（减少一轮调用）

现状：T1 让 LLM 分段后，各段仍需再走一次 `LocatorAgent.locate` 才能拿到
候选表。可以让 T1 那次分段调用**顺带**给每个 segment 输出一个候选表 hint
（不要求精确，仅提示），`LocatorAgent` 拿这个 hint 做初筛起点而非从零跑
5 级规则递进，省一部分规则匹配开销，纯粹是让已经在读全文的 LLM 多产出
一个廉价字段，不是新增判断逻辑。

### T13. 大指令用"outline-then-expand"代替一次性全切

现状：无论指令多长，T1 都要求 LLM 一次性把所有段的完整内容切好。指令特别
长（20+ 实体）时，一次性精确切分本身就是重负载。

修法：LLM 先只产出"总共识别到 N 个独立操作，每个一句话摘要 + 大致原文
范围"的粗框架（轻量输出），Step1 按框架逐个把该范围的原文精确取出再各自
decompose，避免"一次输出既要分段又要保证段内容一字不差"的双重负担。这是
分阶段问同一个 LLM，不是新增规则。

---

## P2：增量收益项，可延后

### T9. 列值域约束前置注入 Step1 prompt

现状：`value_constraints.yaml` 的枚举/范围约束只在 Step2 校验时使用，Step1
决策阶段 LLM 看不到，猜错了才在 Step2 被打回重写，多一轮往返。

修法：`_build_schema_block` 标注列时，若该列在 `value_constraints.yaml`
有声明，追加 `[枚举:值1/值2/...]` 或 `[范围:min~max]`，与 T-上一轮的
`[PK]`/`[FK]` 标注同一套机制，数据源已是现成 yaml，不新增硬编码判断。

### T10. produces/consumes 跨表变量声明表随链路累积注入

现状：链长（10+ 表）时，`<new_X>` 占位符命名一致性全靠 LLM 自己在单次
prompt 里维护，容易在长链断裂。

修法：把当前累积产生的 `produces_label` 列表（代码已有的结构化数据）
作为"本次链已声明变量表"随每次追加/分组 prompt 一起注入，供 LLM 复用已有
命名而非重新发明，纯粹是把已有数据多传一份，不新增规则。

### T11. Step4 反模式归纳结果回灌 Step1

现状：`induce_anti_patterns` 只做事后归纳留痕，不反哺同 session 内后续同类
输入的 Step1 决策。

修法：把本次 session 内已归纳的反模式（结构化 dict，已有）作为一段简短
"本次会话已知易错点"注入后续 Step1 prompt（同 session 内才注入，不跨
session 固化为规则库，避免变成隐性硬编码）。

---

## 已完成（前几轮已落地，供参照，不在本 TODO 范围内）

- ✅ Step1/DecomposeAgent 主链路 `_splitter_baseline`（11 模板硬编码）调用点
  已注释，LLM 产空即报空由段级对账处理，不再回退硬编码模板。
- ✅ `decompose_agent.py::_build_schema_block` 逐列标注 `[PK]`/`[FK→目标]`，
  数据源 `LocatorAgent.fk_edges`（声明式 json + 运行时列名推导）+
  `rules_loader.get_primary_key_overlay()`（用户声明数据），非硬编码业务规则。
- ✅ 前端 thinking 文案清理 baseline/兜底措辞，如实反映 LLM 实际执行状态。

---

## 实施顺序建议

```
T1 → T1b → T2 → T4     (根治本案例三个最大丢数据点，T1 系列是本次修正重点)
T3 → T5                (兜底修复 + 汇总口径，风险低)
T6                     (裁剪动态化，独立改动，随时可插)
T12 → T13              (分段效率优化，依赖 T1 先落地)
T7 → T8                (对抗采样，改动面较大，建议单独验证)
T9 → T10 → T11         (增量项，时间允许再做)
```

每完成一个 T 项，用「兽潮来袭」案例原句重跑一次验证，作为本 TODO 的统一
回归基准。
