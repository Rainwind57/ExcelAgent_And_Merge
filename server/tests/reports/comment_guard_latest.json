{
  "round": "R9-D",
  "date": "2026-08-17",
  "method": "D 批注/样式确定性快照回放（核心回写层）",
  "wave": "第一波 P0 + 独立可测",
  "scope": "cli_interface.py 下沉批注守门到 _save_with_cache_check（同 wb，save 前 snapshot + save 后 reload 做差 + 丢失回写 Comment + 二次 save + 二次做差记数）",
  "changes": [
    {
      "file": "server/agent/excel/cli/cli_interface.py",
      "items": [
        "CLICallResult 加 comment_replay: dict = field(default_factory=dict) 字段（replayed/still_lost，默认空 dict 兼容老调用方）",
        "_save_with_cache_check 集成批注守门：save(wb) → wb.save → _detect_comment_loss reload 做差 → 丢失则 _replay_comments 原 wb 回写 Comment → 二次 save → 二次做差记 still_lost",
        "新增 _comment_snapshot(wb) 遍历 wb 所有 sheet 所有 cell 收集 {(sheet,coord):(text,author)}，无批注 fast-path 返回 {}",
        "新增 _detect_comment_loss(path, before) 独立 openpyxl.load_workbook 读批注做差（不走 _load 避免污染 wb 缓存）",
        "新增 _replay_comments(wb, lost) 原 wb 上重新构造 Comment 对象赋值触发二次序列化",
        "write_cell/append_row 透传 comment_replay=cache_info.get('comment_replay',{})",
        "环境开关 CODEMAKER_COMMENT_GUARD=on|off（默认 on，off 跳过守门仅 save）"
      ]
    },
    {
      "file": "server/tests/test_comment_guard.py",
      "items": ["新建 5 用例：无批注 fast-path / 改非批注列 / 改批注列 / append_row / 环境开关 off"]
    }
  ],
  "metrics": {
    "comment_retention_rate": 1.0,
    "retention_cases": 5,
    "retention_cases_pass": 5,
    "guard_trigger_rate_in_tests": 0.0,
    "guard_trigger_note": "openpyxl 简单场景原生不丢批注，replayed=False；机制就绪，偶发丢失时自动 reload 做差 + 回写 + 二次 save 补救",
    "new_tests": 5,
    "new_tests_pass": 5,
    "regression_tests": 21,
    "regression_tests_pass": 21,
    "regression_status": "zero_regression",
    "regression_scope": "test_save_cache_scope(3) + test_formula_cache(6) + test_fast_apply_eligible(7) + test_write_verification(5)"
  },
  "external_consumer_compatibility": {
    "agent_service.py:1346/1383": "用 cache_info.get() 按 key 访问，加 comment_replay 字段不破坏",
    "formula_semantics.py:450": "CLICallResult 构造未传 comment_replay，用默认值 {} 兼容",
    "formula_ref_shifter.py:54 merge_into": "返回新 dict 时丢 comment_replay 字段，但 comment_replay 为信息性字段不影响公式逻辑"
  },
  "wiring_correction": "原方案 D 接线点 _verify_write:4722 错误（只在 enable_verify_repair_loop 路径跑，非通用；且 restore 须在 wb.save 同一 wb 前注入否则二次 save 仍丢）。修正为下沉到 cli_interface._save_with_cache_check 内 save 前/后包裹，同 wb 无需跨层传 snapshot。",
  "verification": "python -m pytest tests/test_comment_guard.py tests/test_save_cache_scope.py tests/test_formula_cache.py tests/test_fast_apply_eligible.py tests/test_write_verification.py -v → 26 passed",
  "next_wave_upgrades": [
    "第二波 hold 通道就绪后，comment_replay.still_lost>0 时接 pre_commit_hold（kind=comment_loss）",
    "agent 层 _run_set/_run_add 消费 comment_replay 记 audit_log operation=comment_replay_partial"
  ]
}
