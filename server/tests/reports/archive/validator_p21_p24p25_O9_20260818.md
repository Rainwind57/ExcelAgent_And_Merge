# O9 validator 第二批收尾 — P21 + P24/P25

> 轮次：O9（2026-08-18）
> 范围：validator 第二批收尾。**P21** 两入口校验集合统一 + **P24/P25** skip 过滤增强 + dormant 注释。见 `docs/OPTIMIZATION_LEDGER.md` §1 + §4 + `docs/validator_audit.md` §3。
> LLM e2e（`table_case_eval.py`）被 R7 阻断（ledger §2.1），本轮证据为确定性单测。

## 改动清单

| 项 | 文件 | 改动 |
|---|---|---|
| P21 | `server/agent/excel/subagent/validator_agent.py:82` `validate()` | 加可选 `schema_getter`/`data_getter` 参数。提供时在 step 5 后跑 `validate_field_layer`+`validate_fk_layer`，Issue→str 合并进 issues（FORWARD_REF_BROKEN 带「断链」关键字供 hard_issues 判定）。消除「同输入不同路径结论不同」（两入口共享同一字段/FK 校验集合）。缺省 None→保留旧行为，现有调用方不传→不变。 |
| P24/P25 | `server/agent/excel/core/agent.py:3928`（4-step）+ `:4227`（6-step） | 两 skip 过滤点 thinking 列出 skipped stem/sheet（非仅计数，「不静默丢」供汇总单列「已跳过清单」）+ 注释 O3-dormant。 |
| P24/P25 | `server/agent/excel/core/agent.py:5512` `_phase_execute` skip 分支 | 注释 O3-dormant（validate_two_layer 非阻断不标 skipped → 此分支不可达；保留供未来 partial 态复用）。 |
| 测试 | `server/tests/test_validator_unified_entry_p21.py`（新增 8） | P21：无 schema_getter 旧行为 / 有 schema_getter 跑 field_layer（col_not_found/type_mismatch/clean）/ FK 层不崩 / legacy checks 仍跑（broken consumes 断链）/ hard_issues「断链」过滤器 / 两入口同检出 col_not_found。 |

## 确定性验证

```
python -m pytest server/tests/test_validator_unified_entry_p21.py -v
=> 8 passed in 2.40s

python -m pytest server/tests/test_validator_unified_entry_p21.py \
               server/tests/test_validate_agent_two_layer.py \
               server/tests/test_parse_agent.py \
               server/tests/test_decompose_agent.py \
               server/tests/test_produces_inference_p0.py \
               server/tests/test_validator_forward_refs_p13.py \
               server/tests/test_validator_tips_to_failures_p23.py \
               server/tests/test_agent_p19_mutex.py \
               server/tests/test_optimizations_e2e.py \
               server/tests/test_multi_table_orchestration.py \
               server/tests/test_conclude_agent.py \
               server/tests/test_execute_agent.py \
               server/tests/test_subagent_roles.py -q
=> 204 passed in 30.24s   # 零红
```

## 量化

| 指标 | before | after | delta |
|---|---|---|---|
| P21 直接单测 | 0 | 8 | +8 |
| 全相关回归 PASS | 223（O8 后） | 204 | *（测试集子集不同,非回归；O8 数含 multi_table/sub_roles/schema_bundle,P21 测含 unified_entry；两批均零红） |
| 全相关回归红测 | 0 | 0 | 0（零新红） |
| validate() vs validate_two_layer() 校验集合不一致 | 是 | 否（schema_getter 提供时共享 field+FK） | 消除 |
| skip 过滤静默丢（仅计数） | 是 | 否（列 stem/sheet） | 消除 |

## 根因归因

| 修复项 | 贡献 | 证据 |
|---|---|---|
| P21 两入口统一 | validate() 原仅跑 suppress/align/consumes/fk_coverage/forward_refs，validate_two_layer() 仅跑 field/fk_layer → 同输入不同路径结论不同。P21 让 validate() 提供时也跑 field+fk_layer，两入口共享字段/FK 校验集合 | `test_validate_and_validate_two_layer_same_col_not_found`（同 intent+schema 两入口都检出 col_not_found）+ `test_field_layer_col_not_found_merged` |
| P24/P25 过滤增强 | O3 后 skip 路径 dormant，但过滤点模式仍是「静默丢」（仅计数）。增强为列 stem/sheet，供未来 partial 态汇总单列「已跳过清单」；注释 dormant 标明当前不可达 | 代码注释 + thinking 增强（dormant 路径无单测，因 O3 后无 intent 标 skipped，触发需 full run + LLM） |

## 注意事项

- P21 设计决策：opt-in（schema_getter 缺省 None）。现有 validate() 调用方（_run_impl SubAgent adapter + test_decompose_agent 测试）不传→保留旧行为（不跑 field_layer），零回归。新调用方/未来 6-step 路径可传 schema_getter 获统一校验。生产 6-step 路径（agent.py:4178）调 validate_two_layer（非 validate()），故 P21 当前无生产接线——是能力统一 + 测试覆盖，为未来接线铺路。
- P21 Issue→str 转换：FORWARD_REF_BROKEN 带「 断链」关键字，确保 validate() 的 hard_issues 过滤器（`"断链" in i or "失败" in i or "未建" in i`）能捕到 FK 层硬错 → ok=False 阻断。与 _validate_consumes_match 产的「断链」str 一致。
- P24/P25 真 partial 态（重激活 _mark_validation_skipped + _phase_execute:5512 skip 分支可达）留 follow-up。当前 O3 设计（非阻断 + 修复交 C + 失败走 failure 非 skip）下，skip 路径 intentionally dormant。
- LLM e2e `table_case_eval.py` 未跑：R7 阻断（serve agentic LLM 慢/贵，非 excel-agent code 可修，ledger §2.1）。
- validator 第二批收尾：P22/P23/P19/P21/P24/P25 全 ✅（P24/P25 为增强+dormant 注释，真 partial 态留 follow-up）。剩 P9（降级）/P14（opt-in 默认 off）/P26/P27（第三批）。
