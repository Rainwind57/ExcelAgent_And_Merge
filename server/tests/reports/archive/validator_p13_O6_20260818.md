# O6 validator P13 — 确定性单测报告

> 轮次：O6（2026-08-18）
> 范围：validator P13（`_validate_forward_refs_llm` produced 收集改 relation `to_column`，非 `"id" in kl` 启发式）。P0 第一批唯一剩余可动项，收尾第一批。见 `docs/OPTIMIZATION_LEDGER.md` §1 + §4 + `docs/validator_audit.md` §3。
> LLM e2e（`table_case_eval.py`）被 R7 阻断（ledger §2.1），本轮证据为确定性单测。

## 改动

| 项 | 文件 | 改动 |
|---|---|---|
| P13 | `server/agent/excel/subagent/validator_agent.py:703` `_validate_forward_refs_llm` | produced 收集从 `"id" in kl or 编号` 启发式 → **relation `to_column` 声明 PK 列**：先建 `producer_pk_cols: dict[to_stem, set[to_column]]`，再仅收 intent fields 中匹配 `to_column`（via `_field_matches_fk`）的 concrete 值。消 model_id/effect_id 等非主键 id 字段污染 produced 集 → 与 P12 叠加使前向引用"已产出"判定失真（假阴性：本应触发 build 的 LLM 裁决被跳过）+ 行为不确定。 |
| 测试 | `server/tests/test_validator_forward_refs_p13.py`（新增） | 9 测：仅声明 PK 列收集 / 真 PK 跳 LLM / 无 fk_edges 早返 / 无 parser 早返 / 占位符跳 / `<auto>` 跳 / orphan producer 不收 / exists 不产 issue / 空 verdict 静默降级。 |

## 确定性验证

```
python -m pytest server/tests/test_validator_forward_refs_p13.py -v
=> 9 passed in 2.43s

python -m pytest server/tests/test_validator_forward_refs_p13.py \
               server/tests/test_produces_inference_p0.py \
               server/tests/test_validate_agent_two_layer.py \
               server/tests/test_parse_agent.py \
               server/tests/test_decompose_agent.py \
               server/tests/test_subagent_roles.py \
               server/tests/test_execute_agent.py -q
=> 128 passed, 9 failed in 25.48s
```

9 fail 全为 `test_decompose_agent.py` 预存红（DecomposeAgent mock 路径 `decompose` 返 0，基线即红，与本批无关）。零新红。

## 量化

| 指标 | before | after | delta |
|---|---|---|---|
| `_validate_forward_refs_llm` 直接单测 | 0（无） | 9 | +9（新增覆盖） |
| 相关回归 PASS | 128（含预存 9 红） | 128（含预存 9 红） | 0（零新红） |
| 非 PK id 字段（model_id）污染 produced 集 | 是 | 否 | 消除 |
| 假阴性（本应 build 的 LLM 裁决被跳过） | 是 | 否 | 消除 |

## 根因归因

| 修复项 | 贡献 | 证据 |
|---|---|---|
| P13 relation to_column | 消 produced 集收非主键 id 字段（model_id/effect_id/grid_id）→ 与 P12 叠加使前向引用"已产出"判定失真 | `test_only_declared_pk_col_collected`（consumer 引用 model_id 值 1024 → 触发 LLM，旧实现误判本批产出跳过）+ `test_pk_col_match_skips_llm`（真 PK 引用仍正确跳 LLM） |
| P13 orphan producer 不收 | 非 relation 声明 producer 的 intent id 字段不进 produced（无 consumer 引用，无意义） | `test_producer_not_in_relations_not_collected` |

## 注意事项

- P13 在 opt-in 路径（`CODEMAKER_VALIDATOR_LLM_FORWARD_REFS=1`，默认 off）。O2 后 `validate_two_layer`（4-step 主线）不调该方法，仅 fallback `validate()` 主入口在 opt-in 时触发。低频但补齐 P12 语义——否则 P12 收紧 `_field_matches_fk` 后，produced 收集仍用 `"id" in kl` 启发式会重新引入假阳性/假阴性。
- P14（LLM 不可达静默返 `""` 放行）仍在该方法内，默认 off 不触发，留待 P12/P13 后决定是否彻底删该方法。
- LLM e2e `table_case_eval.py` 未跑：R7 阻断（serve agentic LLM 慢/贵，非 excel-agent code 可修，ledger §2.1）。
- `test_decompose_agent.py` 9 预存红：DecomposeAgent mock 路径 `decompose` 返 0，与本批无关，留待 DecomposeAgent 专项修复。
- validator P0 第一批收尾：P10/P11/P12/P13/P17 全 ✅；P9 降级（4-step 不触发）；P14 待决（opt-in 默认 off）。
