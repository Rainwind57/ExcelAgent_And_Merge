# item 表填表知识

- 新增道具时 `item_id` 取当前段最大 ID +1，禁止硬编码。
- `quality` 品质列只能填 1-5：1凡品 / 2良品 / 3上品 / 4珍品 / 5绝品。
- `item_type` 决定子表：1资源 / 2礼包→Chest / 3药品→Potion / 4宝石→Gem / 6装备→Equipment。
- 列表列（如 `spell_ids`）用逗号分隔串，禁止 JSON 数组。
- 装备类新增需同步建 ItemBase + Equipment 两行。
