"""管道 Step5 规则终检器（兼容入口）。

方案 A1 双骨架统一：本模块已迁移至 ..engine_core.verifier，此处仅保留
shim re-export 维持原 import 路径（pipeline.verifier / ..pipeline.verifier）
继续可用，行为与迁移前完全一致。新增代码请直接 from ..engine_core.verifier。
"""
from ..engine_core.verifier import *  # noqa: F401,F403
from ..engine_core.verifier import PipelineVerifier  # noqa: F401  显式 re-export 主符号
