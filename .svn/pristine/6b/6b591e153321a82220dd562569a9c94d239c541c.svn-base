# Excel-Agent 减硬编码优化 TODO

> 来源：全链路硬编码盘点（16 节 / 37 待办）。
> 总策略：从「11 模板 + 多 yaml 手维护 + 关键词闸门」→「schema / 关系图 / LLM 驱动」。
> 迁移序按风险 / 收益排序，P0 先行。

---

## 优先级总览

| 级别 | 待办 | 节 | 主题 |
|------|------|----|------|
| P0 | #26 | §十 | StepAIEnhancer._ensure_session 隔离 |
| P0 | #16 | §六 | 回退 scoped-decompose 至 merge-only 稳定版 |
| P0 | #3  | §一 | 双命名空间（agent.* vs server.agent.*）收敛 |
| P1 | #9  | §四 | alias_mapping.json 自动生成 |
| P1 | #19 | §七 | ValidatorAgent 接真 LLM 裁决 |
| P1 | #29 | §十一| repair_playbook 细分错误 LLM 化 |
| P1 | #38 | §十八 | Step6 汇总输出失败总结（具体错误所在） |
| P1 | #39 | §十八 | 错误中断+交互纠正（避免全盘重改） |
| P2 | #6  | §三 | cross_table_splitter 11 模板灰度替换 DecomposeAgent |
| P2 | #24 | §九 | table_resolver / table_locator 合一 |
| P2 | #23 | §九 | .snapshots 清理 |
| P3 | #36 | §十六| 全量用例基线表 |

---

## §一 入口 / 路由层 AgentService.chat (agent_service.py:1602)

**现状**
- `should_trigger_pipeline`(1618) 关键词闸门切管道 vs 旧 CRUD。
- `_try_intercept_add_form`(1728) regex 拦截表格化录入。
- `router.chat`(1754) LangGraph：`classify` LLM 分类 qa/crud。
- 短句确认 `_AFFIRM/_DENY`(1677-1679) 硬编码关键词元组。

**待办**
1. 【减硬编码】删 `_AFFIRM`/`_DENY` 词表 → 复用 `classify` LLM 节点判确认意图。
2. `should_trigger_pipeline` 关键词迁 yaml + 文件路径检测保留；或让 `classify` LLM 兼任管道判定。
3. 【P0 关键】修双命名空间：启动脚本固定单一 `sys.path`（`pip install -e .` 或 `conftest.py` 收口），`agent.*` 与 `server.agent.*` 合一。

---

## §二 意图拆分 multi_intent_splitter.py

**现状**：`_SEPARATORS`/`_ACTION_PATTERNS` 纯 regex，通用低耦合。

**待办**
4. 引号保护扩展到中文全角括号 `（）【】`；序列号保护保持。
5. （非硬编码问题，低优先级）

---

## §三 跨表模板拆分 cross_table_splitter.py ⭐⭐⭐

**现状**：`detect_cross_table_action`(230) 关键词闸门 + 11 模板 `_build_*_intents`：
`npc_teleport`(486) / `npc_composite`(548) / `npc_combat`(654) / `npc_reward`(691) /
`npc_dialogue`(739) / `item`(917) / `mail`(1068) / `quest`(1113) / `pet`(1168) /
`school_ability_spell`(1240) / `combat_reward`(1320) / `residence_building`(1381)。

**待办（核心方向）**
6. 【减硬编码最大动作】DecomposeAgent(LLM) 逐步替换 11 模板：
   - 阶段1：保留 `detect_cross_table_action` 识别已知模式 → 模板产 baseline；DecomposeAgent 补模板未覆盖表。
   - 阶段2：每个 `_build_*_intents` 灰度替换为「LLM 出 op + produces_inference 关系图补 consumes 占位符」，验证不回归后删模板。
   - 阶段3：删 `detect_cross_table_action` 关键词闸门 → LocatorAgent 候选 + DecomposeAgent 全权分解。
7. 模板 produces/consumes 占位符规则抽数据（`table_relations.json` 已驱动 `produces_inference`）。
8. `detect_cross_table_action` 的 `_ADD_PREFIX` 等关键词迁 `parser_config.yaml`。

---

## §四 表定位层 LocatorAgent + table_locator.py + alias_mapping.json + _table_index.json

**现状**：5 级匹配；`alias_mapping.json` 4KB 手维护（本轮修了 奖励→formula_nested 错路由）。

**缺口**
- `alias_mapping.json` 手维护 → 漂移。
- 76MB `_table_index.json` 启动慢、serve 端读它引爆 R7。
- `LocatorAgent._llm_resolve` 复杂输入路径被 gate 禁用。

**待办**
9. 【P1 减硬编码】`alias_mapping.json` 改自动生成：`index_builder_hints.yaml`(2.8KB) stem→域 + `_table_index` sheet/header 反查自动派生；人工只维护 `index_builder_hints.yaml`。歧义词由 `_llm_resolve` 裁决。
10. `_table_index.json` 分片 / 懒加载。
11. LocatorAgent 解耦 DecomposeAgent 召回：允许 LLM 在候补外提议表（index/AliasMapping 校验防幻觉）。

---

## §五 LLM 分解 DecomposeAgent decompose_agent.py

**现状**：每候选表并行 ThreadPoolExecutor + 隔离 session + retry。被 serve 端 R7 拖累。

**待办**
12. `_to_split_intents`：LLM 返回单 dict → 包装成 `[dict]` 接收。
13. `_build_prompt` schema_block Token 预算裁剪（≤3 sheet × ≤12 col）。
14. 每 prompt 显式注入「哪些列指向他表新 ID」提示（FK 图），引导 LLM 产 consumes 占位符。
15. retry 改指数退避 + per-stem 并发上限（`CODEMAKER_DECOMPOSE_WORKERS`）。

---

## §六 合并 / 兜底 agent.py:3665（本轮新加但回归了）⭐ P0

**现状**：加了「模板先跑 → LLM 仅补未覆盖表」scoped-decompose，实测回归（ok=False / 0 op / matched=0）。

**待办**
16. 【P0 紧急】回退 `core/agent.py:3665` 到上一稳定版（merge-only：LLM 全候选 + merge 模板补漏），先恢复基线 `EXIT=0 matched≥1`。
17. 重做 scoped-decompose：LLM 块放回原 try/except（不裸抛 → 不引发「失败」），失败静默降级仅模板；先 `--no-raise` 验证零回归再上 RAISE。
18. `agent.py:3680` except（静默吞 chain 异常）：调试期保留 `CODEMAKER_AGENT_CHAIN_RAISE=1`，上线关。

---

## §七 校验 ValidatorAgent validator_agent.py

**现状**：纯规则 4 函数；docstring 称 LLM 裁决但 `_call_llm` 不存在 → 假实现。

**待办**
19. 【P1 减硬编码+加严】真接 LLM 裁决：引用前向校验类（reward_id/quest_id/item_id 引用未建目标）让 LLM 判「需补建还是已存在」；rule-only 保留快路径。
20. 4 个 rule 函数行为写可关闭开关（env），便于 A/B 对比 LLM 增益。

---

## §八 拓扑 / 排序 OperationOrchestrator._topo_order + produces_inference

**现状**：Kahn 拓扑 + 关系图驱动 produces 标签 + `<label>` 占位符替换。通用严谨不动。

**待办**
21. Step4.5 显式断言：每个 `<label>` placeholder 执行前应已被替换；未替换即执行 → warning + fallback（读已存在同 sheet 同名行作引用）。
22. `produces_inference.infer_produces_consumes` 触发阈放宽到 ≥1 add。

---

## §九 表 / 列解析 _resolve_table + column_matcher

**现状**：多策略阶梯；手动维护 `column_aliases.yaml`(121KB) / `row_aliases.yaml`(25.8KB) / `table_context.yaml`(39KB)。`.snapshots/` 五套副本堆积。`TableResolver`（旧余弦）与 `TableLocator`（5 级新）并存。

**待办**
23. 【P2 清理】删 `.snapshots/` 多套历史，保留最新 + 主干 yaml；`.snapshots` 加 `.gitignore`。
24. 【P2 减硬编码】合并 `table_resolver` 与 `table_locator`：保 5 级作单一来源，删旧余弦路径；`_resolve_table` 不再 fallback 第二实现。
25. `column_aliases.yaml` 拆 per-table 文件按需加载；`column_matcher` BoW 余弦阈值可配置开关。

---

## §十 Step3 计划 / Step4 校验 / StepAIEnhancer ⭐ P0

**现状**：`_phase_plan`(4749) / `_phase_validate`(4795) / `_phase_plan_validate_merged`(4875)；`StepAIEnhancer._ensure_session`(109) 用 `parser.directory`(=resources) → 继承 R7。

**待办**
26. 【P0 关键】`StepAIEnhancer._ensure_session`(step_ai_enhancer.py:109) 改用隔离空目录（复用 `base.py _isolated_empty_dir`）——消除 R7 对 Step3/4/5 的拖累。
27. 评估 Step3/4 各 AI enhancer 实际收益（A/B 开关 `CODEMAKER_AI_ASSIST`），无效关掉省 LLM。

---

## §十一 Step5 执行 _phase_execute + verify-repair

**现状**：写库 + verify-repair loop + `repair_playbook.py`（error_type→strategy 规则）。
`agent.py:5106/5118` `from .id_scope` / `from .ref_integrity` —— 两文件 MISSING（重构挪走/删了），必然 ImportError（靠 try 兜底静默跳过）。
`engine/` 下 `id_scope.py` / `ref_integrity.py` 存在但 import 指错路径。

**待办**
28. 【修缺口】`agent.py:5106/5118` 改 `from ...engine.id_scope import` / `from ...engine.ref_integrity import`（3 dot 上溯到 server/agent 再 engine）；或 engine 模块挪到 excel 侧统一。
29. 【P1 减硬编码】`repair_playbook` 大类错误（missing field / type mismatch / enum invalid / pk conflict）保留规则；细分错误 → LLM 看读后校验结果做修复决策（复用 ValidatorAgent LLM 通道）。
30. `required_fields.yaml` README 宣称但缺 → 落地；由 index 非空列自动生成。

---

## §十二 回填 _backfill_forward_refs

**待办**
31. 加日志输出回填次数与涉及列，便于诊断跨表链完整性。

---

## §十三 Step6 汇总 _phase_summarize

**待办**
32. 全局 summary 也走隔离 session（同 §十 #26）；否则 R7 对汇总也卡。

---

## §十四 skill 自学习 skill_updater.py + skills 目录

**现状**：`L2_runtime/table_relations.runtime.json`(13KB) co-occurrence 自动学到（减硬编码范例）；`.snapshots/` 五套堆积。

**待办**
33. `.snapshots` 清理 + 保留最新（见 §九 #23）。
34. `anti_patterns.yaml`(85KB) 接入 ValidatorAgent 实际使用（看是否仅收集未生效）。

---

## §十五 备份 / 回滚 backup_audit.py

**待办**
35. 确保 `table_case_eval` / multi 并发时审计日志无竞态写截断（line-buffered append）。

---

## §十六 测试 / 评估 table_case_eval.py

**现状**：diff_sandbox + match_case 评估指标合理；只跑死单条。无全量通过率基线表。

**待办**
36. 【P3 泛化性证据】跑 `server/tests/table_case_eval.py` 全量用例，输出每条 matched/effective + 总通过率；作回归基线。
37. 加 `run_one_case.py` 用 `--no-raise` 模式做 CI smoke（不卡成 EXIT=2）。

---

## 执行进度

| # | 状态 | 备注 |
|---|------|------|
| #26 | ✅ 已落地 | step_ai_enhancer._ensure_session 改用 _isolated_empty_dir,env `CODEMAKER_AI_ENHANCER_ISOLATE_CONTEXT=1`(默认开)。消除 R7 对 Step3/4/5 拖累。py_compile 通过。 |
| #16 | ✅ 根因修复(非回退) | 真根因非"scoped-decompose 设计",是 agent.py:3692 `from .subagent.locator_agent` 单点 import 错(应 `..subagent`)→ ImportError 被 L3719 try/except 静默吞 → LLM 链整体降级 → matched=0。改 2 点。对照 L523-525 既有约定。 |
| #28 | ✅ 已落地 | agent.py:5127/5139 `from .id_scope`/`.ref_integrity` 改绝对 `from engine.id_scope`/`from engine.ref_integrity`(匹配同文件 L1835 + 全仓 71 处约定)。恢复写后 id_scope + ref_integrity 校验(原被 try 静默跳过)。 |
| #12 | ✅ 已落地 | decompose_agent._parse_json_array 加单 dict fallback(数组解析全败才试 `{...}` → 包装 `[dict]`)。数组路径未动。直接调用验证:数组/裸 dict/fenced dict 三态均正确解析。 |
| 验证 | ✅ 已修复 | `test_decompose_agent.py` 5 fail(R8g per-candidate 翻倍)已修:根因是 R8g 把 DecomposeAgent 改每候选一 prompt,但测试 mock 对每候选返同一全局数组 → intents 翻倍。修法:MockClient 加 `_decompose_calls` 计数 + threading.Lock,首次 DecomposeAgent 调用返全数组(模拟 LLM 一次产全部 op),后续候选返空数组,避免翻倍;多候选 ThreadPoolExecutor 并发用 lock 保原子。测试数据零改动。验证:11 passed/0 failed;208 passed 核心批次零回归。DecomposeAgent 确定性测试信号恢复,为后续 #6 模板替换提供回归护栏。 |
| #22 | ⏸ 暂缓 | produces_inference 阈值 `<2` 早返回有据(单 add 无 consumer 可替换)。放宽到 ≥1 只标 produces 无消费者,收益不清且可能加噪。属行为变更,需 eval 验证,避免重蹈 R8 回归。 |
| #3 | ✅ 已落地 | 双命名空间收敛(conftest 收口,非 pip install):① conftest.py 集中加 `server/` 到 sys.path(幂等);② 3 个 `server.agent.*` 异类文件改 `agent.*` + sys.path 仓库根→`server/`:test_decompose_agent(4 import)、test_parse_layer(17 import replace_all)、run_one_case(7 agent+1 services+4 tests import + L305 内联)。验证:仅 server/ 在 path 时 `server.agent.*` 不可解析(ModuleNotFoundError)→ 双载入根除;257 passed/0 failed 核心批次零回归;test_parse_layer 全过。 |
| #9 | ✅ 已落地 | alias 自动生成(安全增量,非删 json):① `AliasMapping.autogenerate()` 从 `index_builder_hints.yaml` 的 stem_to_domain + resources/ 扫描派生 alias(每个 stem 域标签→xlsx);② `load()` 改 merge(autogen baseline + json 覆盖);③ `__main__` 审计输出。验证:autogen 54 条 / merged 139 条 / json-only 85 条;关键「法宝」冲突 autogen→fabao.xlsx 但 json→item/item.xlsx(R4 #17 手修)→ json 覆盖保手修不回归;182 passed/5 failed(5 全预存 R8g per-candidate 翻倍,零新增回归)。后续:把 85 json-only 迁进 hints 即可删 json。 |
| #19 | ✅ 已落地(opt-in) | ValidatorAgent 接真 LLM 前向引用裁决(复用继承的 SubAgent._call_llm_raw,隔离 session 免 R7)。新增 `_validate_forward_refs_llm`:检测 consumer FK 字段的 concrete id 引用未在本批 produces → `_llm_judge_forward_ref` 让 LLM 判「需补建还是已存在」,「build」→ 报硬 issue(含「未建」)。env `CODEMAKER_VALIDATOR_LLM_FORWARD_REFS=0` 默认 off(opt-in,不回归)。验证:env 开关实效(default suppress→1 fixes/intents=1,disabled→0/2);LLM 路径 on 时不崩(mock 返空→graceful 无假 issue);128 passed/5 预存 fail 零新增回归。需真 LLM eval 验增益。 |
| #20 | ✅ 已落地 | 4 个 rule 函数行为写可关闭开关:`CODEMAKER_VALIDATOR_SUPPRESS_OVER_PRODUCE`/`_ALIGN_PRODUCES`/`_CONSUMES_MATCH`/`_FK_COVERAGE`(均默认 on)。便于 A/B 对比 LLM 裁决增益(关 rule 看纯 LLM 效果)。验证:suppress off→不再合并冗余 op(intents 2 保留)。 |
| #29 | ✅ 已落地(opt-in) | repair_playbook 细分错误 LLM 化:① `RepairTaskCtx` 加 `llm_caller` 字段(agent 注入 StepAIEnhancer._call_llm,R7 隔离,与 ValidatorAgent 同通道);② `RepairPlaybook.apply_llm_subtype` + `_build_llm_subtype_prompt`:看 err 信号(error_type/failed_col/failed_val/root_cause/semantic_issues)+ ctx schema(headers/col_types)产结构化 fix_payload(column_remap/value_coerce/allocate_new_id/clear_pk/...);③ agent.py verify-repair 循环 ESCALATE_LLM 时 env `CODEMAKER_REPAIR_LLM_SUBTYPE=0` 默认 off,开启后先试 LLM 细分修复(RE_EXECUTE→_apply_repair_fix+re-verify),失败回落 _run_react_repair。大类错误 Level 1 规则保留快路径。验证:5 mock 场景全稳健(good→RE_EXECUTE/garbage→回落/boom 不崩);176 passed/5 预存 fail 零新增回归。需真 LLM eval 验增益。 |
| #23 | ✅ 已落地 | .snapshots 清理 + retention + .gitignore。根因:仓库根 `.snapshots/` 是 `workflow.py create_snapshot_sync` 的工作流快照(backup resources xlsx),无 retention → 堆积 73 份。修:① workflow.py 加 `SNAPSHOT_KEEP`(env `CODEMAKER_SNAPSHOT_KEEP=5`)+ `_prune_snapshots`(按 mtime 降序留最新 N,backup_before_* 名前缀打乱字典序故用 mtime),接入 `create_snapshot_sync` 末尾;② 建仓库根 `.gitignore`(`.snapshots/` 覆盖根+skills 嵌套);③ 一次性 prune 清存量 73→5(删 68 份)。skills/.snapshots(skill_updater)已 SNAPSHOT_KEEP=5 自管,无需动。验证:workflow.py 编译通过;12 snapshot/workflow 测试通过。 |
| #2 | ✅ 已落地 | `should_trigger_pipeline` 关键词迁 yaml:`_PIPELINE_KEYWORDS` 从 pipeline.py 硬编码元组 → `parser_config.yaml` 的 `pipeline_keywords` 字段,经 `_load_pipeline_keywords()` 加载(失败回退默认)。文件路径正则检测保留。验证:yaml 加载产出同原值(跨表/多张表/多表/流程/并行/剧本);170 passed 零回归。 |
| #1 | ✅ 已落地(opt-in) | `_AFFIRM`/`_DENY` 词表迁 yaml + LLM 确认裁决 opt-in:① 词表从 agent_service.py 硬编码元组 → `parser_config.yaml` 的 `confirm_affirm`/`confirm_deny`,经 `_load_confirm_keywords()` 加载(失败回退默认);② 加 `_llm_judge_confirm(text)` 方法:未命中词表的短句交 LLM 判 affirm/deny/new(复用 StepAIEnhancer._call_llm,隔离 session 免 R7),env `CODEMAKER_CONFIRM_LLM=0` 默认 off。覆盖词表未收措辞变体(行/可以啊/别删/算了)。验证:yaml 加载 17 affirm/9 deny 同原值;无 ai_enhancer 返 'new' 安全;170 passed 零回归。需真 LLM eval 验增益。 |
| #6/#24/#36 | ⏳ 待续 | 见优先级总览。多数需 LLM eval / 架构决策 / 用户对齐。 |
| #4 | ✅ 已落地 | multi_intent_splitter._QUOTE_PAIRS 加中文全角括号 （）/【】（引号保护扩展,括号内复合实体/编号不切分）。py_compile 通过。 |
| #8 | ✅ 已落地 | detect_cross_table_action 8 词表(add_prefix/entity_types/pet_types/item_types/mail_types/quest_types/school_types/quantity)迁 parser_config.yaml cross_table 节,经 _load_cross_table_keywords() 加载(失败回退默认同原硬编码)。组合判定(evolve/combat_reward/residence)保留代码。冒烟 6 用例路由全对(pet/item/mail/school/evolve/npc_dialogue);151 passed 零回归。 |
| #13 | ✅ 已落地 | decompose_agent._build_schema_block Token 裁剪可配置:CODEMAKER_DECOMPOSE_SCHEMA_SHEETS(默认3)/_COLS(默认12),原硬编码 4 sheet×18 col。env 可放宽。 |
| #15 | ✅ 已落地 | decompose_agent retry 指数退避:CODEMAKER_DECOMPOSE_RETRY_BACKOFF(默认1s,0禁用),2^attempt×base。per-stem 并发上限 CODEMAKER_DECOMPOSE_WORKERS 已有(每候选 stem 唯一,全局上限即 per-stem 语义)。 |
| #21 | ✅ 已落地 | _phase_execute 入口 placeholder 断言:执行前扫 fields 未替换 <label> → warning + res.add_thinking + 标记 extras[_has_unresolved_placeholder]。fallback(读已存在同 sheet 同名行作引用)待后续,当前仅断言不阻断避免回归。 |
| #30 | ✅ 部分落地 | required_fields.yaml 独立文件(原嵌 column_aliases.yaml 的 required_fields 节,README 宣称但缺)。_load_required_fields 改读独立文件,回退 column_aliases 兼容。当前空(必填检查空跑),自动生成(由 index 非空列派生)留 TODO。 |
| #31 | ✅ 已落地 | _backfill_forward_refs 末尾汇总涉及列:backfilled_cols 收集 table/col,末尾 log 输出补写次数 + 涉及列列表,便于诊断跨表链完整性。 |
| #32 | ✅ 已满足(#26 覆盖) | _phase_summarize 调 _ai_enhancer.ai_summarize → _call_llm → _ensure_session(已隔离空目录,env CODEMAKER_AI_ENHANCER_ISOLATE_CONTEXT=1 默认开)。R7 对汇总无影响,无需改。 |
| #35 | ✅ 已落地 | backup_audit.record() open 加 buffering=1 行缓冲:table_case_eval/multi 并发追加时单行完整 flush,降低行交错截断。 |
| #6 | ⏳ 阶段1已就位 | 摸排:scoped-decompose #16 修复后,agent.py:3665-3718 已实现"模板 baseline + DecomposeAgent 补未覆盖表"协作(即 #6 阶段1)。阶段2(灰度替换单模板)建议从无占位符表(quest/school/combat_reward/residence/evolve)起,但需 #36 eval 基线验证不回归。占位符密集表(NPC/item/pet)替换风险高,需保 produces/consumes 占位符语义一致。 |
| #24 | ✅ 增量落地(阶段1) | TableLocator 补能力(不删 resolver,双套并存):① level6 BoW 余弦语义兜底(C_SEMANTIC=0.55,_level6_semantic,复用 column_matcher._tokenize/_bow/_cosine,对应 TableResolver 第二层余弦);② _resolve_sheet 二级消歧(level1-3 sheet=None 时填,header 余弦+行数加权+辅助sheet降权,对应 TableResolver._resolve_sheet);③ locate_all 加 level6,locate 内 best.sheet=None 调 _resolve_sheet。不引入 sheet_aliases/table_context yaml 依赖(保 Locator 精简)。验证:冒烟"新增灵兽牛马"→pet/Pet/alias/0.9(sheet 消歧填 Pet 生效);全量 573 passed,7 fail 全预存(column_matcher/codemaker_parser路径/step_ai_enhancer路径/parallel_compare/codemaker_client/sheet_aliases,非本轮引入)。后续:table_context 关键词并入 alias_mapping 后可删 resolver。 |
| #38 | ✅ 已落地 |`_phase_summarize` 加失败分支:`res.failures[]` 优先(回退 `thinking_steps.repair_failure`),显式输出 表/列/根因/已试策略。`AgentResult` 加 `failures: list[dict]` 字段。验证:py_compile+import OK;144 passed 零回归。 |
| #39 | ✅ 已落地(B 同流暂停) | 错误中断交互纠正:routers 加 `POST /reply`+`_SESSION_REPLIES` 队列+`ask` SSE 事件;`agent_service.chat_stream` 注入 `_ask_callback`(推 ask 事件+阻塞 worker 等 reply_queue,0.5s 轮询 cancel 兼容停止);`agent.py` 四处阻断点接入——verify-repair 达上限/AI verify `missing`/占位符未替换/悬空 FK,mode=field/nl/skip 续跑。前端 `AgentChatView.vue` ask-card 弹窗+`replyAsk` 独立 POST /reply(不开新 SSE 流,原流续推)。验证:`/reply` 端点双向 handoff 实测通过(空 session 拒绝+队列 put 成功);未跑全链路 e2e(需真 LLM+造阻断 case)。 |
| #40 | ✅ 已落地 | 失败项结构化:`AgentResult.failures: list[dict]`(type/table/sheet/col/root_cause/attempted_strategies/suggestion/snip/status/user_reply),verify-repair 达上限未解时 append,Step6 汇总显式 + 多意图聚合 `all_failures` 到顶层。已接进 `AgentChatResponse.failures` done payload(前端可渲染失败块,UI 块待加)。 |
| #41 | ✅ 已落地(基础分级) | env `CODEMAKER_INTERACTIVE_REPAIR` 门控已加(`agent_service.chat_stream`:default=1 开,=0 不注入 `_ask_callback` 走原 ABORT,防非交互卡死)。repair 分级:`_run_verify_repair_loop` classify 后,SEMANTIC_OUTLIER/UNKNOWN(需人工判断,自动修复徒劳)直接 break 走 post-exhaustion ask 反问,跳过 LLM 轮次;规则可自修类(type_mismatch/column_not_found/id_conflict 等)继续自动 loop 至达上限才反问。未做:更细粒度(按 repair_playbook strategy 的 needs_human 标记,而非 ErrorType 硬集合)。 |

---

## §十七 三处已知局限修复（本轮）

来源：诊断文档「已知局限」三条。逐条落地 + 测试护栏。

| 局限 | 状态 | 落地 |
|---|------|------|
| ① 单次 LLM 调用内不可中断（仅循环边界可停） | ✅ 已落地 | `CodemakerClient.prompt` 加 `cancel_event` 形参(默认 None 不改行为)。`_prompt_once_inner` 把 `urlopen`+`resp.read()` 都放 daemon 子线程，主线程每 0.2s 轮询 `cancel_event`，set 即返回 `error_type=CANCELLED`（socket 阻塞无法强 kill，daemon 随进程退出回收）。cancel_event 由 `agent_service.chat_stream` 注入 `agent._cancel_event`，`agent.run()` 开头下传至 `parser._cancel_event`/`ai_enhancer._cancel_event`/`qa_handler(_model)._cancel_event`，覆盖全部 LLM 调用点：parse/parse_multi/disambiguate/enum/decompose(locator+validator 共享 base._call_llm)/react/ai_enhancer/classify/QA。`/cancel` 端点注释同步更新。 |
| ② 第三层B 连通校验仅占位符残留，未做指向行存在性深度校验 | ✅ 已落地(opt-in) | `agent.py` Step6 前扫后新增 `_check_dangling_fk_refs` + `_fk_target_row_exists`：对每 partition result_rows 中具值(非 `<...>`)的 FK 列，按 `table_relations` 关系图找目标表/sheet/PK 列，`produced` 本批产出 ∪ 写盘 read-back 双路核验，均不存在才记悬空引用，并入 `results_for_ai` 的 ok/detail。保守策略：目标文件/sheet/列不可解析 → 不报(避免误报)。env `CODEMAKER_CONNECTIVITY_DEEP_CHECK=1` 默认关(同 #19/#29 opt-in 约定)。 |
| ③ 跨表场景端到端实测缺失 | ✅ 已落地 | 新增 `server/tests/test_cross_table_connectivity.py`：6 个深度校验确定性单测(悬空检出/预存行不误报/produced 命中不误报/占位符跳过/目标缺文件保守/无 FK 边跳过) + 3 个 cancel 机制单测(秒级取消/无 cancel 行为不变/预置取消不起线程) + 1 个 LLM-gated E2E(`quest_npc_double_option[0]` NPC+双选项+采集支线+奖励，env `CODEMAKER_E2E_LLM=1` + serve 可达才跑，断言 summary 连通性一致性而非硬 7/7 因 R7)。 |

### 测试护栏与回归
- 新增 10 测：9 确定性全过、1 E2E skip（无 LLM 环境）。
- 我引入的 mock-prompt 签名不兼容(TypeError 被 except 吞致假行为)已修：`test_decompose_agent.MockClient`/`test_circuit_breaker.FakeClient`/`test_analyze_enum_columns` lambda/`run_one_case._patched_prompt` 均补 `cancel_event` 形参。`test_verify_repair_loop._bind_vr_agent` 补绑 `_adaptive_rounds`(预存缺口，致 2 测红，一并修)。
- 确认零新增回归：`test_circuit_breaker`/`test_analyze_enum_columns`/`test_llm_sub_agent`/`test_verify_repair_loop`/`test_spec_coverage_gaps`/`test_parse_layer`/`test_pipeline_e2e`/`test_multi_table_orchestration`/`test_agent_retry_loop` 全过。
- 预存未修(非本轮引入，单独跟踪)：`test_decompose_agent` 9 测 `0 意图`——还原我的 decompose 改动后仍 0，根因 `_build_schema_block` 在测试 mock 环境返空 → jobs 空 → 0 intents（TODO 称 R8g 后 11 passed，后续 schema-block 改动致回归，与本轮无关）。

### 续修：心跳 0/0 子任务 + 0 模型调用（instrumentation gap）
现象：parse/Step1 阶段心跳一直「第 0/0 子任务，已调用 0 次模型」。
根因：① codemaker_parser(parse/parse_multi) 与 subagent(base._call_llm/_call_llm_raw + decompose _run_one 直连 client.prompt)**完全未接 `_llm_counter`**(docstring 撒谎称 parser 入口 inc)→ decompose 的 LLM 调用心跳 peek_total 读不到 → 0；② 0/0 子任务因 parse 阶段 Step5(subtask_start) 未启动，文案误导。
落地：`agent.run()` 开头把 `self._llm_counter` 下传 `parser._llm_counter`(同 _cancel_event 模式)；subagent/base 加 `_bump_llm(site)`(inc+merge_to_instance)接入 `_call_llm`/`_call_llm_raw`；decompose_agent `_run_one` 每候选 prompt 后 `_bump_llm("decompose")`；codemaker_parser 加 `_bump_llm` 并在 parse/parse_multi 调用。心跳文案：total==0 时显「意图解析中：已调用 N 次模型」(不再误导 0/0)。token 估算留 0(调用次数是心跳/eval 主指标；精确 token 需 estimate_tokens，未引入额外依赖)。验证：3 个计数器接线单测全过 + 131 passed 零回归。

### 待加强（架构方向，非本轮改）
**跨表模式硬关键词门控**：`detect_cross_table_action`(keyword)命中→模板 fast-path(跳过 parse_multi)；未命中→`parse_multi`(LLM，R7 易卡)或 per-seg `parse` 兜底；另 `agent_chain`(locator.locate)不依赖关键词，经 alias/FK 检测跨表→decompose 补未覆盖表。故非跨表内容**不是死路**(走 LLM parse_multi + 回退)，但新颖跨表措辞不命中关键词→走 parse_multi(R7 stall 风险)而非快速模板。根治=§7.7.1 scoped-decompose(TODO #6 阶段2)：locator+decompose 为主、模板为基线补漏、逐步撤关键词门。

---

## §十八 总结归纳失败总结 + 错误中断交互纠正 ⭐ P1

**来源**：样例「赏金猎人凯尔」(NPC+对话+任务+奖励跨表) AI 校验发现遗漏意图——第二段对话 interaction 记录（「去野外清剿10只赤炎虎」）规则只拆出第一段（「接悬赏吗?」），第二段未单独拆行；当前仅记入 thinking，最终 summary 只报成功项，用户不知具体漏在哪。

**现状**
- Step6 `_phase_summarize`(agent.py) 汇总只输出成功项（matched/op 数）；AI 校验/validator 标记的遗漏、未替换占位符、悬空 FK 仅落 `extras`/thinking，**不进显式失败总结** → 用户看不到「失败清单」。
- 遇错路径：verify-repair loop 自动修；修不动则整体降级/重跑，**无中途中断向用户求确认** → 每次出错倾向全盘重改。

**待办**
38. 【P1】Step6 汇总输出**失败总结**：对 AI 校验/validator 标记的失败项，汇总必须并列输出「成功总结 + 失败清单」——每条含 失败类型(遗漏意图/占位符未替换/悬空FK/校验不通过) + 涉及表/列 + 根因简述 + 原始指令片段；前端 `AgentChatView.vue` 区分成功/失败块展示。
39. 【P1】错误**中断交互纠正**：遇关键失败（遗漏意图/占位符未替换/FK 悬空无法自修），不直接全盘重改，改为**中断 + 回问用户**——输出「失败项 + 建议补全方案」（如本例：是否追加第二段对话 interaction 行？内容="去野外清剿10只赤炎虎"？），用户确认/修改后**增量续跑**（只补缺失项，不动已成功项）。需 `agent.run()` 支持 pause-resume / 增量 patch。
40. 【P2】失败总结**结构化**：失败项落 `results_for_ai.failures[]`（type/table/col/root_cause/suggestion/snip），供前端渲染与后续 LLM 自动补全复用；env `CODEMAKER_SUMMARY_INCLUDE_FAILURES=1` 默认开（可关保旧行为）。
41. 【P2】中断阈值**可配置**：哪些错误走「中断交互」、哪些走「自动 repair」由 `repair_playbook` 分级——大类规则可自修的继续自动修；需人工语义判断的（如「第二段对话应否独立行」）走中断。env `CODEMAKER_INTERACTIVE_REPAIR=0` 默认关（opt-in，避免打断非交互场景）。

