# 候选池：skill_updater 产出的待 promote 候选与冲突，验证通过才 promote 到 L2/L3。
# 文件（运行时自动生成）：
#   column_alias_candidates.jsonl   列别名候选（低置信度/纠正命中追加）
#   anti_pattern_signals.jsonl      反模式信号（ambiguous/failed_operation 计数源）
#   conflicts.yaml                  歧义漂移冲突（同 query 指向 ≥2 resolved，等人工 review）
#   quarantine/                     T10 影子写入隔离区（promote 回归失败候选，本任务未接）
