# CA 配表系统现状全貌（新同学上手读物）

- **用途**: 给没有代码权限的新同学建立完整认知，作为开发"AI 增删改查 CA 配表系统"的前置读物
- **整理日期**: 2026-07-10
- **模型**: glm-5.2
- **来源**: ca-master / ca-master-officecli 技能 + branch_externals 文档 + logic/ca 编表环境实地调研 + 8-agent 对抗式深挖验证
- **读法**: 一篇读完，建立 CA 表结构 / 编表流程 / 分支流转 / 轻享服机制 / 痛点优先级 / 脱敏方案的完整认知。本文档只提供现状，不做方案决策。

---

## 0.一句话目标

> 做一套对 G18 的 CA 配表（`.xlsx`）做**增 / 删 / 改 / 查 / merge** 的系统，由**没有代码权限的新同学**来开发。**核心目标：解决 AI 时代的填表问题**——让 AI 能可靠地改配表，不再依赖人工事后救火。最终产物形态此处不限定，本文档先认清现状再决策。

---

## 1.CA 配表是什么

### 1.1 CA 表在整体架构里的位置

```
logic/ca/*.xlsx              策划填写的 Excel 配置表（数据源，SVN 独立仓库）
      ↓  gen_code.py 编表工具链
logic/conf_data/*.c          LPC 配置文件（编表产物）
      ↓  热更或正常启动进游戏进程
游戏运行时读取配置
```

- **CA 表 = 数据源**：`logic/ca/` 下 1041 个 `.xlsx`，总大小约 7.9G。
- **conf_data = 产物**：编表把 xlsx 渲染成 `.c` 文件，游戏代码调用配置文件提供的函数（如 `get_xxx(id)`）读取配置数据。
- **CA 是 SVN 外链**：`ca/` 目录是 `svn://svn-g18.gz.netease.com/svn/design/Code/ca` 的外链，**独立版本控制，所有 logic 分支共享同一套 ca**。这一点是后面所有 merge 问题的根源。

### 1.2 一张 CA 表的内部结构（CONFIG sheet + 数据 sheet）

每张 xlsx 几乎都是**多 sheet**。第一张 sheet 固定叫 `CONFIG`，描述"这张表怎么编"；后面是若干数据 sheet。

**CONFIG sheet 的行布局**（行号是 xlsx 物理行）：

| 行 | A列(标识符) | B列 | C列 | D列 | E列 | F列 |
|----|-----------|-----|-----|-----|-----|-----|
| 1 | 表名 | 中文表名 | | | | |
| 2 | 说明 | 中文描述 | | | | |
| 3 | 额外配置 | `0`（整数） | 忽略sheet | 依赖xlsx | 后处理代码 | |
| 4 | 生成文件数量 | N(整数) | | | | |
| 5 | 生成文件1 | xxx.pat | 模式 | utf-8 | sheet名 | 输出路径.c |
| 6 | 生成文件2 | xxx.pat | 模式 | utf-8 | sheet名 | 输出路径.c |
| ... | ... | | | | | |
| N+4 | 表单配置 | ALL_SHEETS | 1(起始行) | | | |
| N+5 | 列配置 | iID | INT | NOT_EMPTY | | |
| N+6 | (留空) | cName | TEXT | | | |

**CONFIG 各部分含义**：
- **行1~4**：固定元信息行。行3 的 B 列"额外配置"填整数 `0`。
- **行5 起 N 行**：模板行，固定 6 列（A 模板名 / B 模式 / C 编码 / D sheet名 / E 输出.c 路径 / F ...）。一张表可能生成多个 .c。A 列的 `xxx.pat` 是**编表模板文件**（位于 `logic/pytool/code_gen/template/`，共 2072 个），描述「这张表的数据怎么渲染成 .c 代码」——编表时读 xlsx 数据 + 套用 .pat 模板 → 生成 `conf_data/*.c`。
- **表单配置行**：`ALL_SHEETS` 表示后面所有 sheet 都按这个起始行读；也可单独配某个 sheet 名。
- **列配置行**：每个字段一行。A=字段名、B=类型(INT/TEXT/STRING...)、C..=检查类型(NOT_EMPTY/NOT_SERVER/NOT_CLIENT/IGNORE...)。

**数据 sheet**：第 1 行是中文表头（col_desc），第 2 行起是数据。第一列是唯一 ID（通常是 id），**第一列为空的行会被整行跳过**（策划用来写注释）。

### 1.3 列定义的检查类型（读表时必须展示）

CONFIG 列定义行不止 6 列，检查相关的是**两列**，不要混在一起：

**D 列 — 非空校验**：填 `NOT_EMPTY` 表示该列必填非空，留空则不校验。实测 1035 张基准表里用了 23482 次，是最高频的检查。

**E 列 col_check — 字段归属 / 值校验**（解析源码 `xls_2_code.py` 的 `CHECK_GRID_FUNC`，第 391-420 行）。但**解析器支持的取值不等于策划真在用**——实测 1035 张基准表的 E 列统计如下：

| col_check 取值 | 实际使用次数 | 含义 / 状态 |
|------|------|------|
| `NOT_CLIENT` | **3319** | 仅服务端字段，正常读不校验。大量在用 |
| `NOT_SERVER` | **883** | 仅客户端字段，服务端跳过该列。大量在用 |

展示字段结构时，`NOT_SERVER` / `NOT_CLIENT` 标记必须标注（决定该列服务端读不读），`NOT_EMPTY` 也要标注（决定该列必不必填）。

### 1.4 批注是策划和程序共同维护的

CA 表的批注由**策划和程序共同维护**，有业务价值：表头行批注通常是取值枚举说明（如 `1=垃圾\n2=正常\n3=精英`），数据行批注是业务规则备注（如"该道具 2026 国庆下架"）。策划还会调整批注格式使其更易读。

### 1.5 表格大小分布

1041 个 xlsx 实测分布：

| 大小区间 | 数量 | 占比 |
|---------|------|------|
| <50KB | 587 | 56.4% |
| 50KB-500KB | 393 | 37.8% |
| 500KB-5MB | 56 | 5.4% |
| ≥5MB | 5 | 0.5% |

**最大的 5 张表**：`calendar-festival.xlsx`（8.4MB）、`item.xlsx`（3.4MB）等。

---


## 2.分支体系与表格流转

### 2.1 为什么 ca 没有"分支"
`ca/` 是**单 SVN 仓库 + 所有 logic 分支共享**。它不靠 SVN 分支做隔离，而是靠**目录命名约定**：

| 目录 | 服务器类型 | 隔离方式 | 核心机制 |
|------|-----------|---------|---------|
| `ca/` (根) | 常规服基准 | 全量 | 被其他目录覆盖 |
| `ca/dev/{分支名}/` | 开发分支 | 同名覆盖基准 | 可含独立新表 |
| `ca/testbranch/` | 外测服 | **全量覆盖** | 整张表替换基准 |
| `ca/trialbranch/` | trial 服 | 全量覆盖 | 整张表替换基准 |
| `ca/lpcbranch/` | lpc 分支服 | 全量覆盖 | 整张表替换基准 |
| `ca/cappedbranch/` | **轻享服** | **PATCH 融合**(增量!) | 只写要改的行，其余保留基准 |
| `ca/cappedbranch/cappedtest/` | 轻享服外测 | PATCH 融合，优先级最高 | 同上 |

**实测规模**：`ca/dev/` 下有 **380 个**开发分支目录，命名格式 `{logic分支名}_{单号}_{拼音}`，如 `ai_valentine_260227_215718_wangweilong`，其中 262 个（69%）有 `cappedbranch/` 子目录。`cappedbranch/` 下有 **178 个** `_capped.xlsx`（175 个补丁表 + 6 个独立 capped 表如 `capped_economy.xlsx`）。`testbranch/` 仅 5 个 `_test.xlsx`，`trialbranch/` 和 `lpcbranch/` 是**空目录**。

### 2.2 痛点：开发分支表格与 trunk 基准不一致
**场景**：
1. 第 1 周策划在 `ca/dev/活动A_xxx/` 填了表，基于当时的 trunk 基准
2. 第 2 周 trunk 基准变了（别人改了 `ca/item.xlsx` 加了新列、改了字段类型）
3. 代码侧也打了新分支，策划把 `ca/dev/活动A_xxx/` 的表直接挪到新分支 `ca/dev/活动A_new/`
4. **表格还是旧的，代码已经是新的** → 列定义对不上 → 编表报错或数据错位

**根因**：ca 没有分支继承关系，dev 表是"快照"，不会自动跟 trunk 基准同步。

### 2.3 痛点：merge 时的漏行丢失与编号冲突
#### 2.3.1 全量覆盖分支的"漏行丢失"（最致命，P0）

对 `testbranch/trialbranch/lpcbranch`，ca 目录下的同名表是**全量覆盖**，不是 merge：

- 编表时如果 `ca/testbranch/item.xlsx` 存在，**整张表**替换 `ca/item.xlsx` 基准
- **不是 merge，是完全替换**

**坑**：trunk 给 item.xlsx 加了 id=10500，你合入 testbranch 时只拷了自己改的部分，没复制 10500 → 编表后 10500 丢了 → **因为 testbranch 是整表覆盖，不会自动读 trunk 基准行**。

#### 2.3.2 编号冲突（P0）

1. 所有分支共享同一个 ca 仓库，靠目录名隔离
2. 不同开发分支的人**看不到对方的 `ca/dev/xxx/`**，不知道别人在用哪些编号
3. 两拨人分别用同一个 item_type（比如都选了 10500），同周合入 test_branch：策划合并表格**有时序**，先合并的正常，后合并的要调整编号顺序。调整编号会**影响代码**（代码里引用了具体编号），出现疏忽会导致**线上 bug**

#### 2.3.3 编号管理现状（全人工，无程序化消费）

- `ca/id_mgr.xlsm`（363KB）：4 个 sheet（说明/CONFIG/SETTING/各模块编号占段表），**全代码库零 Python 引用**，靠人工维护、人工查冲突。
- **编表时唯一性校验**：只做**单文件单 sheet 运行时**检查（`xls_2_code.py:1539-1544`），由 CONFIG 的 `Z_ex_conf` bit flags 控制（0x01 跳过跨 sheet、0x02 跳过 sheet 内、0x04 跳过全局 sheet 名、7 全跳过）。
- **capped 融合**：slave 和 base**各自**做 ID 唯一性检查，**不跨文件**。
- **跨表编号冲突完全无检测**（fashion.FASHION 和 item.ITEM 的 ID 冲突编表时不会被发现）。
- **基础跨表存在性校验**有：`CHECK_XLS_KEY`（`xls_2_code.py:391-421`）验证引用值在被引用表作为 key 存在——但只是"存在性"校验，不做跨表联动填充。

### 2.4 merge 要做三方合并
merge 核心算法是**三方合并（base + theirs + ours）**，不只是编号冲突检测：
- 两人改同一张表的**同一行不同列** → 列级冲突，可自动合并
- 两人改**同一行同一列** → 单元格级冲突，需人工裁决


---


### 2.5 公式缓存问题（P0，写表基础问题）


CA xlsx 部分单元格是 Excel 公式而非直接写入值。Excel 会算出结果写入缓存；编表工具链用 xlrd 读，xlrd 读公式单元格**返回缓存值**，无缓存则返回空。而 AI 用 openpyxl 改表（load→改→save）后，因 openpyxl 无公式引擎，**save 会清空缓存值**（公式文本保留）→ xlrd 读到空 → 编表输出 0/空。实测：原始 `match_dan.xlsx` 365 个公式缓存全在，openpyxl save 后归零（0/365），xlrd 读该列从 `50.0` 变 `''`。

**影响面**：随机抽 30/1041 张表（765,192 单元格），仅 785 个公式单元格（0.1%），集中在 `match_dan.xlsx` 一张（外推约 70/1041，~6.7%）。影响面窄但属于**写表基础问题**——任何用 openpyxl 改含公式表的流程都会触发，优先级 P0。

**修复**：事后用 Windows Excel 打开重存即可重算写回缓存。Linux 端可研究 libreoffice headless 或 officecli（Apache 2.0、350+ 函数引擎、写入即求值）重算，选型待定。铁律：save 后 `load_workbook(data_only=True)` 重读对比，不一致则标记人工修复。


## 3.轻享服 / 外测配表格式（PATCH 机制）

### 3.1 _capped.xlsx 是补丁文件，不是全量表

`_capped.xlsx` 是**增量补丁**：
- **没有 CONFIG sheet**（最重要识别规则！字段结构来自 trunk 基准 `ca/xxx.xlsx` 的 CONFIG）
- 只有 `PATCH_CONFIG` + 数据 sheet
- 数据 sheet 里**只写需要改动的几行**，不复制全表

### 3.2 PATCH_CONFIG sheet 格式

两列无表头，从 A1 开始：

| A列 | B列 |
|-----|-----|
| 目标 sheet 名 | 融合方式（`PATCH_GEN` 行级增量 / `SHEET_GEN` sheet 级全量覆盖） |

`PATCH_GEN` 最常用（按 ID 匹配改行），`SHEET_GEN` 极少用（整 sheet 覆盖）。具体融合规则由 CA 编表管线实现，通用系统不需要深入。

### 3.3 轻享服的 5 个坑（merge 系统必须处理）

| # | 坑 | 后果 |
|---|----|------|
| 1 | sheet 名与基准 xlsx 对不上 | 融合结果不含该 sheet |
| 2 | 忘了在 PATCH_CONFIG 登记新 sheet | 该 sheet 不参与融合（最常见"改了没生效"原因） |
| 3 | PATCH_CONFIG sheet 缺失 | 整个文件融合退化或失败 |
| 4 | 给 _capped.xlsx 加了 CONFIG sheet | 格式破坏 |
| 5 | 同名 sheet 重复，**先写者优先** | 反直觉（多数人以为后者覆盖），后者静默忽略 |

### 3.4 列配置联动陷阱

trunk 主表 CONFIG 加列时，若该表在 `ca/cappedbranch/` 下也有 `_capped.xlsx`，**增量文件的列配置必须与主表一致**，否则编表列不匹配报错或数据错位。这是结构变更工作流要解决的核心（见 §5.1）。

---

## 4.现有 AI 操作方式（ca-master 技能）的局限

当前 AI 改表走 `ca-master` 技能，存在以下问题：

1. **依赖 openpyxl**：公式缓存丢失风险（见 §2.5），需要人工事后 Excel 修复。
2. **写表姿势严格**：必须 `load_workbook → 改单元格 → save`，严禁 `Workbook()` 重建、严禁 `iter_rows(values_only=True)`、严禁 `data_only=True` 再 save、严禁 pandas 读写——任一错误都会丢批注/样式/公式。
3. **merge 全靠人工**：xlsx 源文件层面无任何半自动工具，大表 merge 就是"人工逐行对比"，100 行 diff 怎么展示、冲突怎么处理，全无工具支持。
4. **编号冲突无自动检测**：靠 `id_mgr.xlsm` 人工查（全代码库零 Python 引用），跨表编号冲突完全无程序化校验（编表管线不检测）。

---

## 5.痛点优先级总表 + 待解功能

> **核心转变**：以下痛点的「当前规避」大多是 **QA 平台的后置检测**——问题已经提交、编表已出错后才报警，再回头人工修。新系统的目标是在**提交前**就拦截这些问题（merge 漏行 / 编号冲突 / 结构漏同步 / 公式缓存丢失等），把"事后救火"变成"事前预防"。

### 5.1 痛点优先级（按不可逆程度排）

| 优先级 | 痛点 | 不可逆程度 | 影响范围 | 当前规避 |
|--------|------|----------|---------|---------|
| **P0** | merge 问题（testbranch 漏行丢数据 / 跨分支编号冲突 / dev→trunk 漏拷行） | **不可逆**（数据静默消失 / 后提交覆盖） | 中 | QA 平台检测+报警 + 手动改编号；**merge 漏拷行待 AI 介入（最大痛点）** |
| **P0** | 公式缓存丢失（写表基础问题） | **可修复**（事后 Excel 打开重存） | **窄但影响大**（仅 match_dan.xlsx 等 ~70 张，但触发即丢数据） | Windows Excel 修复 |
| **P1** | 结构变更漏同步（加列没同步到 _capped / dev） | 可修复但易漏 | 中（结构变更时） | QA 平台检测 |
| **P1** | 改表丢批注（旧脚本写表后批注丢失） | 不可逆（批注丢失找不回） | 中 | 无 |
| **P2** | 轻享服 PATCH_CONFIG 5 个坑 | 可修复 | 中 | 靠人记规则 + QA 平台检测 |

---

## 6.脱敏环境方案

### 6.1 为什么需要脱敏

新同学没有代码权限，不能直接看 `logic/ca/` 真实配表（含真实运营数据、玩家可识别信息、商业敏感配置）。需要构造一套**结构真实、数据脱敏**的样本环境。

### 6.2 适合做样本的表

**小型典型表**（13KB 左右，结构完整、易理解）：
- `all_server_red_packet_limit.xlsx`（13.6KB）
- `mount_gift.xlsx`（13.3KB）
- `collect_book.xlsx`（13.3KB）
- `energy_exchange_info.xlsx`（13.2KB）
- `anniversary_gift.xlsx`（13.0KB）
- `chat_channel.xlsx`（13.0KB）

**中型表**（40-45KB，含多 sheet、有 PATCH 场景）：
- `org_cj_gh.xlsx`（44.9KB）
- `beaststory24.xlsx`（44.8KB）
- `capped_economy.xlsx`（44.6KB）— 轻享服样本

**超大型表**（≥3MB，测大表场景：读写性能、merge 逐行对比、内存占用）：
- `calendar-festival.xlsx`（8.4MB）— 最大的表
- `item.xlsx`（3.4MB）— 道具表，行数多、列定义复杂

**已有测试目录**：
- `ca/qa_test/`（含 btxml/check/check_branch/dont_delete/taskedit/test 子目录）
- `ca/Schema/`（有 `mall.category_trade.schema.yml` / `price.Price.schema.yml` / `scene_npc.NPC.schema.yml` 三个 schema 样例）

### 6.3 脱敏要处理什么

1. **真实运营数据**：道具名称、活动奖励数值、商品价格 → 替换为占位符（如"测试道具001"）。
2. **玩家可识别信息**：白名单 uid、账号相关 → 替换为假 uid。
3. **商业敏感配置**：抽奖概率、付费档位 → 用测试数值。
4. **保留结构**：CONFIG sheet 结构、字段类型、批注格式、PATCH_CONFIG 机制**必须原样保留**（这些是要测试的核心）。

### 6.4 脱敏环境应包含

- 一套脱敏的基准表（`ca/` 等价物）
- 一套脱敏的开发分支表（`ca/dev/sample_xxx/`）
- 一套脱敏的轻享服增量表（`ca/cappedbranch/sample_capped.xlsx`）

### 6.5 交付方案（选定场景 B：只读写 xlsx）

**选定场景 B**：脱敏环境只做 xlsx 读写，**不跑编表验证**。无需 `code_gen/`、无需 python2、无需 .pat 模板、无需解决 pysvn，表脱敏难度低（结构正确即可），工作量 0.5-1 天。

**全量脱敏 1041 张表不可行**（字段差异大、类型约束多）。只能选 5-20 张典型表手工脱敏，或用自动化脚本批量"结构保留+数据替换"。

**最轻量方案**：`svn export` ca 仓库 → 自动化脱敏脚本（保留 CONFIG+表头，数据行替换为假数据）→ 打包给新同学。脱敏脚本注意：
- 枚举值列（如物品分类 `iron`）不能随机替换，否则结构不合法 → 需白名单（可从 `AllTableConfigs.yml` schema 推导）
- 合并单元格直接遍历赋值可能破坏
- 公式列（虽然只有 match_dan.xlsx）需特殊处理

---

## 7.新同学上手检查清单

**理论理解类（读完本文档即可，不依赖权限）**：
- [ ] 理解 CA 表结构（CONFIG sheet + 数据 sheet）、编表流程（xlsx → .c）
- [ ] 理解 ca 是 SVN 外链 + 目录隔离，没有真分支
- [ ] 理解全量覆盖（testbranch 等）vs PATCH 增量（cappedbranch）的差异
- [ ] 理解公式缓存问题是**写表基础问题**（openpyxl save 会清空公式缓存，~70 张表主要是 match_dan.xlsx），优先级 P0
- [ ] 知道 officecli 是**开源（Apache 2.0）跨平台方案**，公式引擎能在 Linux 端重算（解决跨平台）；但**并发无锁**，Web 后端要用 resident + per-file mutex。选型待定（openpyxl / libreoffice / officecli 三选一或组合）
- [ ] 知道 id_mgr.xlsm 是纯人工维护的编号管理总表，编表管线零引用——新系统可填补
- [ ] 知道表格大小分布（56.4% <50KB，5 张 ≥5MB）
- [ ] 理解 merge 要做三方合并（base+theirs+ours），不只是编号冲突检测
- [ ] 理解痛点优先级：P0 = testbranch 漏行丢数据 + 编号冲突；P1 = 结构变更漏同步 + merge 遗漏

**动手操作类（依赖脱敏环境交付，见 §6）**：
- [ ] 能用 openpyxl 读一张表的结构、改一个单元格、写一个 _capped.xlsx（含公式缓存自检）
- [ ] 构造测试数据测一遍：公式缓存保护 / PATCH_CONFIG 优先规则 / 三方合并 / 编号冲突检测
- [ ] 自行确定第一期功能边界、技术选型、脱敏环境方案（本文档只提供背景，不替你做决策）
