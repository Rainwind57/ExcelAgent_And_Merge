# Excel-Agent × Merge 答辩 PPT 逐页脚本（整合重排版）

> **本文件用途**：在现有 43 页 PPT（36 页内容页）基础上，**插入本轮 5 项工程优化 + Skill 安全阀说明**，并对全部内容页**重新排序**，使"背景→架构→Step1→Step2→Step3→Step4→自学习→本轮优化收束→指标汇总"形成单向递进的讲解链条，Merge 章节同理。
> **不是新写内容**：每页文字均取自现有 PPT 原文（见 `PPTX当前内容与可补充创新点.md` 第一部分）或已跑出的压测报告，本文件只做**排序 + 缝合 + 补页**。
> **页号规则**：下文"新页号"为建议定稿页号；"原页号"标注取自现有 PPT 第几页，`NEW` 表示全新页面。
> **总页数**：40 页内容页（原 36 页 + 新增 4 页：本轮优化×2 + Skill安全阀细化×1 独立成页 + 展望×1）+ 结束页。原第 37-43 页模板说明/图表示例页不计入，按需保留在文件末尾作备用版式。

---

## 总体逻辑链（先讲这个，方便你判断顺序是否合理）

```
业务背景(为什么难) → 任务目标(要做成什么)
        │
        ▼
Excel-Agent 架构总览(怎么分层解决)
        │
        ▼
Prompt工程(所有Step共用的约束手段)
        │
        ▼
Step1解析 ──▶ LocatorAgent定位 ──▶ rapidfuzz模糊匹配 ──▶ DecomposeAgent拆解 ──▶ produces/consumes契约 ──▶ 质量门禁
        │        （这一串是 Step1 内部的技术递进：先定位表→再模糊兜底→再拆JSON→再建跨表契约→最后二次校验）
        ▼
Step2校验(三层校验 → 写盘前硬门禁 → 交互修正)
        │        （校验失败不是终点，是可恢复的编辑循环）
        ▼
Step3执行(确定性写盘，不再调模型)
        │
        ▼
Step4汇总(成功/失败归因 → 反模式沉淀)
        │        （Step4 产出的反模式，正是 Skill 系统的原料）
        ▼
Skill系统(知识分层 → 生成审核机制/安全阀 → 效果数据)
        │        （Skill 系统跑起来后，本轮又发现并修了 5 个更细的口子）
        ▼
本轮工程优化(占位符闭环 → 命名漂移 → 聊天降噪/成本 → Step4口径)
        │
        ▼
Excel-Agent 指标汇总收束
────────────────────────────────────────────
Merge 架构总览(两种模式) → 模拟生产环境(SVN目录)
        │
        ▼
merge-base定位(先找到"共同祖先"，否则无法比较)
        │
        ▼
Compare全流程(四层比较 + 稀疏化/向量化性能)
        │        （比较出的差异要落地，才到下面三步）
        ▼
ID重映射与引用完整性 → 公式缓存保护 → fast-apply写回
        │        （规则搞不定的，AI来兜底建议）
        ▼
AI辅助冲突处理
        │        （最后是让全流程在大表/多表下也跑得动）
        ▼
大表并行性能优化 → Merge 指标汇总收束
────────────────────────────────────────────
验证结论(回归/可靠性) → 展望(规划中的创新点) → 总结 → Q&A
```

> 这条链条的关键调整：把原 PPT 里散落在 P10/P11（LocatorAgent+rapidfuzz）之后又跳到 P20-22（Skill）中间夹着的顺序问题理顺——**LocatorAgent/rapidfuzz/DecomposeAgent/produces-consumes 全部收在 Step1 之下**，不再单独跳章；**Skill 系统紧跟 Step4** 讲（因为 Skill 的原料就是 Step4 的反模式），而不是隔了很多页之后再讲；**本轮 5 项优化放在 Skill 之后、指标汇总之前**，作为"系统还在持续变严谨"的收束，而不是突然插在中间打断主线。

---

# 第一章 · 封面与业务背景（新页 1-4）

## 新页 1｜封面（= 原 P1，不变）

**居中大字**：
- AI 配表助手
- ——自然语言驱动配表 + 三方合并 Merge
- 吴植贤｜2026年9月4日

---

## 新页 2｜目录（= 原 P2，不变）

| # | 章节 | 一句话 |
|---|---|---|
| 01 | 业务背景与任务目标 | 为什么做 |
| 02 | Excel-Agent：4-Step 多 Agent 配表流水线 | 怎么改表 |
| 03 | Merge：面向 SVN 目录分支的三方合并 | 怎么合表 |
| 04 | 验证、展望与总结 | 做到什么程度、还能往哪走 |

---

## 新页 3｜业务背景：真实配表的复杂性（合并原 P3+P4，去掉重复表格）

**一句话定位**：复杂 Excel 配表不是单表编辑问题，而是跨表、跨目录、跨版本的一致性工程。

**三维复杂度**：

| 维度 | 说明 |
|---|---|
| 规模复杂 | 表多、字段多、跨表引用密集；活动、奖励、技能、NPC、刷新点常跨多表 |
| 表达复杂 | 用自然语言描述目标，通常不会精确写表名、列名、枚举值和外键链 |
| 协作复杂 | SVN 目录分支和外链并存；多人并行改 Excel，人工合并容易漏行、丢公式 |

**问题→后果表**（原 PPT 第4页两张重复表格，定稿时只保留一张）：

| 编号 | 存在问题 | 后果 |
|---|---|---|
| 1 | Merge 过程出现漏行丢数据情况 | 数据会静默消失 |
| 2 | 跨分支编号冲突，多分支创作过程新建同一编号 | 后提交的进行了覆盖导致线上bug |
| 3 | 公式缓存丢失 | 编表会出现输出0或者空 |
| 4 | 改表慢、易错 | 效率低、易漏外键 |
| 5 | AI 误写不可逆 | 写坏数据找不回 |

> ⚠️ **顺手修**：原 PPT 第4页有两张完全一样的表格，定稿删掉一张。

---

## 新页 4｜任务目标（= 原 P5，不变，紧跟背景页顺理成章）

| 引擎 | 目标 | 关键约束 |
|---|---|---|
| Excel-Agent | 输入自然语言 → 结构化意图 + 多表配表 | 存在校验、可执行、可跟踪的结构化意图 |
| Merge | 目标目录分支模式实现 Excel 三方合并 | 保护公式、ID引用、冲突可解释性 |

**讲法**：上一页 5 个问题里，1/2/3/5 对应 Merge 要解决的，2/4/5 也是 Agent 要收敛的——两个引擎其实是同一套"防止数据说谎"目标下的两条技术路线。

---

# 第二章 · Excel-Agent 4-Step（新页 5-24）

## 新页 5｜章节封面（= 原 P6）

**大字**：二、4-Step Excel-Agent

---

## 新页 6｜Excel-Agent 总体架构与定位（= 原 P7，不变）

**定位**：面向用户复杂输入，能够将复杂输入解析并正确应用到表格，不要求用户先知道表名和字段名。系统先定位候选表，再让 LLM 在真实 schema 下拆解 JSON，随后由规则层做质量门禁。最终目标是让 Step1 的 JSON 足够准确，因为后续 Step2/Step3/Step4 都依赖它。

**架构总览**：LangGraph 编排 TableAgent 4-Step：

| Step | 职责 |
|---|---|
| 1、解析 | 分段→定位→schema注入拆解→语义计划编译 |
| 2、校验 | 根据业务规则进行字段/类型/依赖等的校验 |
| 3、执行 | 依赖顺序确定性写盘、ID分配、引用回填 |
| 4、汇总 | 变更归纳、错误总结、自学习反模式 |

**讲法**：下面按 Step1→Step2→Step3→Step4 顺序展开，每个 Step 讲完就带出它的性能数据，最后落到 Skill 系统和本轮优化。

---

## 新页 7｜Prompt 工程：约束输出协议（= 原 P8，不变，放在 Step1 之前因为是所有 Step 共用手段）

| 组件 | 内容 | 作用 |
|---|---|---|
| 角色设定 | 设定为Excel配表拆解专家 | 限定任务边界 |
| 真实schema注入 | 表名、sheet、列名、类型、外键FK等 | 防止出现幻觉字段 |
| 输出格式 | json | 后续json格式可执行 |
| 反例约束 | 约束不输出不存在列，不把叙述碎片当字段值 | 降低污染 |
| 示例 | 给出跨表引用的例子 | 提升结构稳定性 |
| 截断策略 | 只注入schema | 降低token使用，防止超时 |

**指标**：

| 优化点 | 优化前 | 优化后 |
|---|---:|---:|
| Prompt上下文缩减 | 全量表索引76.11MB | 单任务聚焦schema 510字符，约159token |
| 输出格式约束 | 自由文本，解析不稳定 | 固定JSON，可执行可校验 |

---

## 新页 8｜Step1 解析：全链路核心（= 原 P9，不变）

**一句话**：Step1 是全链路核心，自然语言一旦拆错，后面校验和执行都会被拖下水。

| 操作 | 技术说明 |
|---|---|
| 分段 | 规则分段 |
| 候选表定位 | 规则+检索+关系扩展 |
| schema注入 | 注入真实表头、类型和FK |
| LLM拆解 | DecomposeAgent输出结构化intents |
| produces推断 | FK图+主键规则，建立起跨表依赖关系 |

**指标**：

| 指标 | before | after | 变化 |
|---|---:|---:|---:|
| 定位功能 | 0.6667 | 0.8056 | +20.8% |
| 覆盖度 | 0.6111 | 0.7500 | +22.7% |

**讲法**：Step1 内部有 4 个技术点要展开——候选表定位怎么做（下一页）、模糊匹配怎么兜底（再下一页）、LLM拆解怎么防幻觉（再下一页）、跨表依赖怎么传递（再下一页）。

---

## 新页 9｜增强：LocatorAgent 三级定位（= 原 P10，紧接 Step1，不再跳章）

**一句话**：用户不会准确说出真实表名，系统要把"灵兽、宠物、饕餮"映射到真实表。

| 通道 | 技术 | 作用 |
|---|---|---|
| L1 规则通道 | 表名/中文表头/别名/字段关键词 | 直接命中可以零成本 |
| L2 模糊通道 | rapidfuzz混合指标 | 拼写、命名等变体兜底 |
| L3 关系扩展 | 外键(FK)一跳进行扩展 | 命中主表带上绑定表 |
| L4 RAG | BM25语义召回 | 规则未命中时补召回语义近邻 |

---

## 新页 10｜增强：rapidfuzz 混合指标算子性能（= 原 P11，紧接上一页）

```text
rapidfuzz_score = WRatio×0.5 + token_set_ratio×0.3 + partial_ratio×0.2
```

| 指标 | 作用 |
|---|---|
| WRatio | 综合编辑距离、长度差、部分匹配，适合通用相似度 |
| token_set_ratio | 忽略词序和重复词 |
| partial_ratio | 长字段里包含短查询时仍能够命中 |

**性能对比**：

| 指标 | 纯Python实现 | rapidfuzz C++算子 |
|---|---:|---:|
| 编辑距离计算 | 2781.54ms | 192.16ms |
| 单次匹配平均 | 0.0093ms | 0.0006ms |

---

## 新页 11｜增强：DecomposeAgent schema 注入拆解（= 原 P12）

**输入**：候选表真实结构+多级表头+字段类型+外键FK+填表规则
**输出**：结构化 JSON 的 intents

**样例**：
```
输入：Ability表格 row1列名、row2类型、字段类型、外键、填表规则
输出：{action:add, table:Ability, fields:{id:100,name:"TEST"}, produces:[<aid>]}
```

- schema注入：模型只能从真实列名中进行选择，从源头解决幻觉列名问题
- few-shot：附上正确ID/占位符示例，禁止伪造占位符如 `<resolved_form_xxx>`，超时降级逐表格重拆
- 规则安全网：LLM产生intent小于2条或者畸形时回退模板

**指标**：

| 指标 | before | after |
|---|---:|---:|
| 字段精准率 | 0.5185 | 0.6917 |
| 严格通过率 | 0.1667 | 0.3333 |

---

## 新页 12｜增强：produces/consumes 依赖契约（= 原 P13）

**一句话**：跨表新增时，后续表依赖前面新建行的 ID，需要靠依赖契约确定，而不是靠模型记忆。

**样例**：新增活动activity_id=3001，新增奖励reward_id=30010绑定activity_id=3001，新增邮件mail_id=5001附件reward_id=30010。

| 术语 | 含义 | 作用 |
|---|---|---|
| produces | 本意图产生的占位符 | 记录生成的ID |
| consumes | 本意图消费的占位符 | 记录使用的ID |
| 占位符placeholder | 写表时临时标记 | 执行时替换为真实ID |

**指标**：

| 指标 | before | after |
|---|---:|---:|
| quest_npc引用一致 | 0.62 | 1.00 |
| quest_npc耗时 | 134s | 107s |

---

## 新页 13｜Step1 质量门禁：schema-aware 二次编译（= 原 P14）

**一句话**：LLM 产出 intents 后，ParseAgent 继续做 schema-aware 编译，进行再一次检验，收尾 Step1。

| 检查内容 | 修复方式 |
|---|---|
| 删除孤立add intent、同sheet稀疏影子重复 | 删除 |
| 字段不在schema、sheet或name混用、大小写差异 | 修正到真实列 |
| 检查produces和consumes | 从已存在资源或新行解析出真实ID |
| 自产自销、依赖环 | 阻断执行 |

**指标**：

| 优化项 | before | after |
|---|---:|---:|
| type_mismatch | 2 | 1 |
| 失败trace归纳反模式 | 0 | 4类 |

**讲法**：Step1 到此结束——从"分段定位"到"依赖契约"到"二次编译"，交给 Step2 的已经是尽量干净的 JSON。

---

## 新页 14｜增强：Step2 L0/L1/L2 三层校验（= 原 P15）

| 层级 | 方法 | 处理内容 |
|---|---|---|
| L0 规则闸 | 纯代码 | 类型、PK、FK、占位符等 |
| L1 AI裁决 | 1次LLM | NL覆盖度、值合理性 |
| L2 人工gate | 交互卡片 | 用户改字段/值后续跑 |

---

## 新页 15｜Step2：写盘前硬门禁（= 原 P16）

Step2 是执行前的硬门禁：
- 字段是否存在，中文表头是否能映射到真实列
- 值类型是否可转换，枚举值是否合法
- FK 是否指向正确表和正确主键，placeholder 是否有 producer
- Step1 质量报告如果有 hard error，阻断写盘并生成可交互修正

---

## 新页 16｜Step2 交互修正：从报错到可恢复（= 原 P17）

- 当前已有列名批量修正卡片，下一步应升级为"全字段可编辑表格"
- 用户可以改列名、改值、删除字段或删除整条intent，然后 POST `/api/agent/reply` 续跑
- 后端应把用户编辑回写到原intent，再重新跑Step2，而不是只绕过错误

**讲法**：校验失败不等于流程失败——这是设计上的关键主张，Step2 结束后进入 Step3 确定性执行。

---

## 新页 17｜Step3：确定性执行（= 原 P18）

**一句话**：Step3 不再调用模型，只做可解释写盘。

| 动作 | 技术 |
|---|---|
| 拓扑排序 | 按produces和consumes依赖执行先后 |
| ID分配、占位符替换 | 新增行分配主键，替换后续placeholder |
| 失败结构化归因 | 缺字段、PK冲突、类型错误等 |
| 可恢复边界 | 失败停于可恢复点，避免半写入污染 |

**指标**：

| 测例 | before | after |
|---|---:|---:|
| 复杂样例 | 204s | 112s |

---

## 新页 18｜增强：Step4 结果汇总与自学习闭环（= 原 P19）

**一句话**：Step4 汇总最终成功或失败情况，同时将失败的情况进行沉淀。

| 功能 | 说明 |
|---|---|
| 汇总结果 | 成功数+失败数+写入行+预览 |
| 失败归因 | 定位失败发生在哪个层 |
| anti_patterns | 从失败trace归纳反模式 |
| mini_regression | 失败样例进入小回归集 |

**讲法**：Step4 产出的"反模式"不是写完就扔——它是下一页 Skill 系统的原料。

---

## 新页 19｜Skill 系统①：四层知识结构（= 原 P20）

**一句话**：把列名、如何定位行、哪些操作历史出错沉淀成可查询规则，Agent 与 Merge 共用同一份知识源。

| 层级 | 目录 | 内容 | 生成方式 |
|---|---|---|---|
| L0 人工/根目录 | skills/*.yaml | sheet别名、引导动词、列短形式 | 人工+自动混合 |
| L1 自动派生 | L1_derived/ | 列别名、行定位规则、多sheet消歧、类型约束 | 扫描resources/*.xlsx自动生成 |
| L2 运行时 | L2_runtime/ | 运行时学到的列别名(带hits/confidence)、跨表关系权重 | 从evidence promote写入 |
| L3 反模式 | L3_anti_patterns/ | 失败模式规则：歧义列/类型约束/失败操作/语义模式 | 规则频次升级+AI归纳 |

---

## 新页 20｜Skill 系统②：生成审核机制 + 转正安全阀（= 原 P21，补充安全阀细节，NEW 内容并入）

**转正/升级阈值**：

| 判定 | 阈值 |
|---|---|
| 列别名候选转正 | 同一张表/同一分页里，用户把某词说成某列，7天内命中够3次、把握在30%~75%之间 → 才收进知识库 |
| 反模式升级active | 某列容易认错，7天内出现≥3次 → 自动改成只许精确匹配 |
| 反模式升级pending_review | 某操作失败，30天内出现≥2次 → 先标记"待人工审核" |
| AI归纳反模式转正 | 无人工标注兜底，门槛更高——多命中3次才生效 |
| 冲突消解 | 同一说法被两种标准列名抢着对应 → 不自己决定，交人工判断 |
| 衰减 | 60天未用自动移除；可信度<30%先"休眠" |

**★ NEW 补充：转正安全阀（代码机制，回答"自学习会不会失控"）**

- `skill_updater.py::promote_with_guard`：候选 → 快照 → 写盘 → mini回归(30样本, lift≥0.05) → 通过才转 active；**不通过自动回滚 + 隔离区(quarantine) 30天**。
- AI 归纳的反模式比确定性规则门槛更高（需多命中3次），因为没有 ground-truth 兜底。

**讲法**：Skill 系统不是"学了就用"，是"学了先在隔离区试跑，回归不掉分才敢转正"——这也是为什么下一页的效果数据敢摆出来。

---

## 新页 21｜Skill 系统③：效果数据（= 原 P22）

| 数据点 | 指标 | before | after |
|---|---|---:|---:|
| skill简版30样例 | 定位成功率 | 0.933 | 1.000 |
| skill简版30样例 | 精确命中 | 0 | 1.00 |
| skill简版30样例 | 严格综合 | 0.704 | 0.959 |
| skill简版30样例 | 平均耗时 | 533ms | 321ms |
| skill真写盘8样例 | 定位/覆盖/精准 | 0.472/0.417/0.497 | 0.639/0.583/0.590 |
| Step4反模式沉淀(模拟回放) | 同类失败预警覆盖 | 0/8(0%) | 8/8(100%) |
| Step4反模式归纳(真实e2e) | 8条失败trace→4类反模式 | — | 命中PK冲突/seed缺失/conv_id定位/类型转换 |

**讲法**：Skill 系统跑起来后，还有一批更细的口子是本轮才发现并修的——下面两页展开。

---

## 新页 22｜NEW｜本轮工程优化①：占位符闭环 + 命名漂移修复

> 背景一句话：这是围绕"对话树占位符闭环"和"命名风格漂移误报"做的 2 项改动，直接补强上一页 Step1/Step3 的薄弱点。

| # | 创新点 | 模块 | 指标 | before | after |
|---|---|---|---|---:|---:|
| 1 | 占位符命名归一化，消除产物/消费引用命名漂移误报 | `validator_agent._norm_name` | FORWARD_REF_BROKEN 假阳性（6组应匹配样例） | 5/6 误报 | **2/6 误报** |
| 2 | 嵌套占位符递归扫描（dict/list任意深度） | `step3_execute_subagent` | 8条混合样例漏检嵌套占位符 | 7处漏检 | **0处漏检** |
| 2b | 循环依赖回填（forward-ref backfill） | 同上 | 5节点链式依赖，可执行节点数 | 1/5 | **5/5** |

**讲法**：#1 解决的是"同一个ID，LLM 一会儿写 `newConvId` 一会儿写 `new_conv_id`，系统误判成两个东西"；#2 解决的是"占位符藏在 dict/list 里第二层，旧逻辑扫不到"；#2b 解决的是"执行顺序被打乱时，回填一轮就能补齐"。

**诚实边界**：#1/#2 是直接 import 仓库真实函数跑的；#2b 是简化状态机模拟（真实 `_backfill_forward_refs` 依赖 Agent 实例上下文，单独调用成本过高）。

> 证据：`tools/simulate_validator_forwardref_norm.py`、`tools/simulate_placeholder_nested_backfill.py`

---

## 新页 23｜NEW｜本轮工程优化②：聊天降噪 + 成本控制 + Step4 口径修复

| # | 创新点 | 模块 | 指标 | before | after |
|---|---|---|---|---:|---:|
| 3 | 聊天区用户可见内容过滤+摘要+截断 | `agent_service` chat_stream | 25条混合会话展示行数/噪音占比 | 24行/66.7% | **8行/0%** |
| 4a | 分组硬上限，控制LLM子调用成本 | `decompose_agent` | 12候选实际跑的分组数(=LLM子调用次数) | 3 | **2** |
| 4b | dict列lint修复合法嵌套字段误清空 | `decompose_agent._lint_split_intents` | 3个合法dict列场景误清空率 | 100% | **0%** |
| 5 | Step4 `all_ok`口径修复 | `step4_conclude_subagent` | 8种(s1,s2,s3)组合中判定错误场景数 | 3/8错误 | **0/8错误** |

**讲法**：#3 是回到新页18 Step4"汇总结果"的展示层——内部调试噪音不该甩给用户；#4a/#4b 是回到新页11 DecomposeAgent 的成本和正确性双修；#5 是回到新页18 Step4 的判定逻辑本身——`all_ok` 曾经只看 Step3 一个信号，现在真实反映三步综合结果。

**诚实边界**：#4b/#5 是真实函数直接调用；#3 是从生产源码 AST 提取函数体后原样加载执行（非重写等效实现）；#4a 是按 git diff 复原的分组骨架模拟。

> 证据：`tools/simulate_agent_service_noise_filter.py`、`tools/simulate_decompose_chain_cost.py`、`tools/simulate_step4_allok_drift.py`

---

## 新页 24｜Excel-Agent 章节指标汇总（收束页，NEW 结构，内容取自各页已出现数字）

| 指标 | before | after | 提升 |
|---|---:|---:|---:|
| 一次配表LLM调用 | 6步线性23+次 | 4-Step 3-5次 | -80% |
| 定位功能 | 0.6667 | 0.8056 | +20.8% |
| 字段精准率 | 0.5185 | 0.6917 | +33.4% |
| quest_npc引用一致 | 0.62 | 1.00 | 达标 |
| Skill简版定位成功率 | 0.933 | 1.000 | +7pts |
| 本轮：占位符假阳性 | 5/6 | 2/6 | -50% |
| 本轮：嵌套占位符漏检 | 7 | 0 | 归零 |
| 本轮：聊天噪音占比 | 66.7% | 0% | 归零 |

**讲法**：从 Step1 到本轮优化，主线是同一句话——"少问模型、少读少写、先校验后执行、失败可恢复"，现在再加一条"每一环都不说假话"。

---

# 第三章 · Merge 三方合并（新页 25-36）

## 新页 25｜章节封面（= 原 P23）

**大字**：三、Merge 合并方案

---

## 新页 26｜Merge 架构与总体流程（合并原 P24+P25）

**两种模式**：

| Merge模式 | 过程 |
|---|---|
| 跨分支合并（分支合并） | 通过From分支与To分支比对合并到To分支 |
| 跨分支合并（合并回trunk） | 分支合回trunk，内容集成在trunk分支 |
| 目录合并 | 将子目录的生产内容合并回trunk分支 |

**总体流程（七步）**：
1. 选择From/To两个目录
2. 通过SVN copyfrom定位 merge-base
3. 提取Excel数据、表头、公式、样式和文件元信息
4. 比较变更并识别冲突
5. ID重映射与引用更新
6. AI辅助冲突建议
7. LibreOffice重算、公式保护、安全写回

**讲法**：两套Merge模式除了分支/base确定上有差异，核心diff逻辑一致。下面按这七步的顺序展开。

---

## 新页 27｜模拟生产环境：demo_svn 目录结构（= 原 P26）

SVN demo 目录模拟真实项目结构：trunk、branches配表目录。重点不是复制 Git 分支，而是在 SVN 的历史信息里还原"共同祖先"，这样 Merge 才能判断谁改了什么，而不是简单覆盖文件。

```
merge/svn/demo_svn/
├── repo/                      # svnadmin 仓库（file:// 直连，无需 svnserve）
└── wc/                        # 工作副本
    ├── trunk/                 # 74 张 + subdev_1/2/3 子目录
    │   ├── subdev_1/          # ability/const/item_drop/monster/reward（5 私有表）
    │   ├── subdev_2/          # const/fabao/guild/mail/tips（5 小表）
    │   └── subdev_3/          # ability/reward/map/interaction/world_buff（5 小表）
    └── branches/
        ├── dev1/              # 全量（跨分支 source/target）
        ├── dev2/              # 全量
        ├── dev3/              # 小表（删了大表，改 ability/reward/tips）
        └── dev4/              # 小表（删了大表，改 const/fabao/guild）
```

| 情况 | copyfrom-path | 标记 |
|---|---|---|
| 正常切分支 | trunk@r1 | inferred=false |
| 新建 | 路径本身 | inferred=true |

---

## 新页 28｜SVN merge-base 定位（= 原 P27）

**一句话**：在选择分支/目录后，找到 base 分支与 base 版本的核心方法——copyfrom 反查。

1. From分支和To分支各自寻找自身的copyfrom
2. 沿着copyfrom链上溯，寻找完整的祖先链
3. 两条copyfrom链交叉寻找最近公共祖先(LCA)

**性能优化链（A→E 递进）**：

| 版本 | 方法 | 解决问题 | 性能提升 |
|---|---|---|---|
| A | Python三重循环 | —— | 10w行×100列会卡死 |
| B | 根据哈希建立row_idx_maps | O(n)线性扫描卡死 | 哈希定位实现O(n)→O(1) |
| C | 使用sparse稀疏化 | 全表建立哈希内存过大 | 内存从GB级降至MB级 |
| D | numpy向量化 | 逐单元格python函数调用消耗高 | 纯数据大表提升10-50倍 |
| E | 公式、批注在sheet回退逐单元格循环 | 解决公式、批注向量化出错的问题 | 保证性能同时保证正确性 |

**指标**：

| 优化 | before | after |
|---|---:|---:|
| SVN rev查询 | 每张表单独查一次，总计191s | 一次递归查询全目录，缩减为0.2s |

---

## 新页 29｜Compare 全流程：四层比较 + 稀疏化向量化（合并原 P28+P29）

**四层比较**：
- 文件层：新增、删除、重命名、移动
- Sheet层：新增sheet、删除sheet、表头变化
- 行列层：主键匹配、行新增删除、字段变更
- 语义层：公式、外键引用、ID冲突和可合并性判断

**技术与优化**：

| 技术 | 解决问题 | 效果 |
|---|---|---|
| 哈希索引 | list.index线性扫复杂度O(n) | 行定位O(1) |
| sparse diff | 全表物化100列 | 只保留变化格 |
| numpy向量化 | 逐格归一化的问题 | 批量进行比较 |
| 公式、批注回退 | 向量化无法理解公式、批注 | 保证了正确性 |

**指标**：

| 指标 | before | after |
|---|---:|---:|
| 大表compare | 平均48s | 平均6.4s |
| 10万行全链 | 卡死 | 10.4s |
| 提交数据量 | 46.4MB | 0.1MB |

---

## 新页 30｜ID 重映射与引用完整性（= 原 P30）

同主键多分支新增时会出现主键冲突，对于后来者分配新的ID，随后同步更新引用列，避免只改主表ID却留下悬空外键，保证引用一致性。

**讲法**：这一层和 Excel-Agent 思路一致——先建立依赖契约（对应新页12），再执行替换，否则会出现引用链断裂。

---

## 新页 31｜公式缓存保护（= 原 P31）

Excel文件内部公式和缓存值分离，直接写盘容易造成缓存丢失或公式失效。

- 系统通过zip扫描excel文件底层的XML字节流，查找`<f>`公式标签判断表格中是否存在公式，定位changed/lost公式
- 10w行的表格，openpyxl全量读取需要6.1s，使用zip扫描降到0.05s

**校验与修复全流程**：
1. 表中无公式则走最快速路径
2. 保存后文件读回，与保存前快照做比较：缓存值消失→标记lost；值不一致但正常更新→标记changed
3. changed情况无需修复
4. lost情况调用LibreOffice重算，再度校验

**设置保护**：60s超时上限（避免单文件卡死全流程）+ 文件互斥锁（保护文件不被损坏）+ 重算结果先输出到临时目录再替换源文件

---

## 新页 32｜公式缓存与 fast-apply（= 原 P32）

**公式单元格冲突解决**：
- 系统识别为公式列(diff_type=formula)
- 公式写法一致但缓存值不一致 → 不冲突；引用输入值变化 → 标记formula_changed，调用内置求值器预览重算结果
- 公式写法不一致 → 视为公式冲突，界面展示原有公式供用户比对选择

**选择写回**：用户选择公式版本后打上formula_resolved标记，apply阶段据此写回单元格。

**fast_apply 快路径**：对大数据量表格，用zip+XML直接修改目标单元格，不触碰公式单元格，不破坏公式缓存——xlsx本质是zip包，zipfile打开只读需修改的sheet XML，lxml/ElementTree定位具体单元格，改完的XML放回新zip，避免openpyxl全量读写耗时。

**指标**：

| 指标 | before | after |
|---|---:|---:|
| apply处理 | 19.6s | 4.3s |
| 序列化 | 4.9s | 0.3s |

---

## 新页 33｜AI 辅助冲突处理（= 原 P33）

**一句话**：规则搞不定冲突时，AI 只提供建议，不进核心策略。

**完整链路**：点击AI建议 → 前端发送请求 → 调用线程池运行 → 根据LLM输入拼prompt → LLM返回JSON → 防幻觉校验

**置信度因子**：
- `llm_conf`（LLM自评）、`rev_conf`（修订号领先度越高取值越高）、`value_conf`（取值越合理越高）

**参考因子**：

| 因子 | 作用 |
|---|---|
| SVN rev/SVN date | 版本越新倾向于采纳 |
| 列名语义 | 判断值是否合理 |
| 单元格值 | 判断值是否异常 |
| 基准值 | 提供base值供参考 |

**并行优化技术**：
- 缓存优先：缓存键由表名+sheet名+列名+版本哈希组成，命中且未过期直接返回
- 批量LLM：未缓存的单元格拼进prompt，一次调用返回json，按index定位
- 回退并行：批量失败时用线程池并发逐格调用

输出json：建议保留来源、理由和置信度，最终由用户确认。

---

## 新页 34｜大表格并行性能优化（= 原 P34）

Compare和写回阶段按文件/Sheet粒度并行：CPU密集比较用ProcessPool，I/O和解析任务用ThreadPool，worker上限跟CPU/文件数/内存占用绑定，避免并行越多越慢；fast_apply让无冲突低风险文件快速落盘。

| 优化项 | 受益 |
|---|---|
| calamine Rust读引擎 | 222次打开表格时长从17.5s优化到0.8s |
| svn info批量预填 | 192s→0.2s |
| ProcessPool/ThreadPool并行 | 8个表格时长从3.69s优化到1.84s |
| fast_apply快路径 | 25MB的sheet XML序列化从3.4s优化到0.98s |
| 未变更表跳过 | 可以跳过占大多数的表格体 |

---

## 新页 35｜Merge 章节指标汇总（收束页，NEW 结构，内容取自各页已出现数字）

| 环节 | before | after | 提升 |
|---|---:|---:|---:|
| SVN rev查询 | 191s | 0.2s | ~950× |
| 大表compare | 48s | 6.4s | 7.5× |
| apply处理 | 19.6s | 4.3s | 3.8× |
| 提交数据量 | 46.4MB | 0.1MB | -99.8% |
| 公式检测(10w行) | 6.1s | 0.05s | ~120× |
| calamine读取(222次) | 17.5s | 0.8s | 21.9× |
| ProcessPool并行(8表) | 3.69s | 1.84s | 2.0× |
| 10万行全链 | 卡死 | 10.4s | 从不可用到可用 |

**讲法**：从merge-base定位到Compare到写回到AI建议，主线是同一句话——"可信Base+稀疏diff+引用保护+安全写回"，AI只做建议不做决策。

---

# 第四章 · 验证、展望与总结（新页 37-40）

## 新页 37｜当前验证结论（= 原 P35）

- 最新代码合并后，核心回归通过71项；此前相关agent回归通过109项
- 这次更新对智能化有正向价值：enum resolver、schema-aware parse修复、locator调整都在减少模型幻觉
- 剩余风险集中在复杂链路：跨段代词、省略主语、枚举口语化、资源缺必填字段

---

## 新页 38｜NEW（可选）｜展望：规划中的创新点

> 时间紧可跳过此页仅口头提及；时间够则展开讲，体现"设计完成，量化数据待补"的工程严谨性。

| 创新点 | 一句话 | 数据现状 |
|---|---|---|
| Replan-on-failure（失败后自动重规划） | Step5执行失败聚合failures+剩余子任务，触发1次LLM重规划后重跑(上限2轮) | 已落地+20个单测，e2e阻塞于外部serve环境 |
| 编号账本跨分支查重 | 多分支同时新建同一ID时实时查重+建议下一空闲号 | 对应新页3痛点"跨分支编号冲突"，单测验证，无真实多分支压测数字 |
| BM25语义召回(RAG阶段一) | LocatorAgent规则+模糊都未命中时用BM25语义召回兜底 | 已并入新页9"L4 RAG"，暂无召回率A/B数据 |
| Schema血缘联动 | 扫描多分支列定义，标出trunk有列但其他分支缺的情况 | 定性能力(发现问题)，非性能类指标 |

---

## 新页 39｜总结（= 原 P36）

- Excel-Agent 的正确方向不是堆prompt，而是"LLM理解+schema编译+规则校验+确定执行"
- Merge 的正确方向是"可信Base+稀疏diff+引用保护+安全写回"
- 本轮额外验证：占位符协议、命名匹配、聊天展示、LLM成本、结果口径——五个小口子的修复，让流水线"每一环都说真话"
- 后续重点：全字段交互修正、复杂链路上下文继承、经验沉淀、更多端到端回归和可观测指标

---

## 新页 40｜结束页

**THANK YOU**

---

# 附：原 P37-43（模板说明/图表配色示例，非内容页，定稿时按需保留或删除）

- P37 模板使用说明（联系人信息）
- P38-42 图表配色建议 / 柱状图×2 / 饼状图示例
- 若正式提交给公司模板规范要求保留，建议放在结束页之后作附录，不影响正文页码。

---

# 变更对照表（新旧页号速查）

| 新页号 | 内容 | 原页号/来源 |
|---:|---|---|
| 1-2 | 封面/目录 | P1-P2 |
| 3 | 业务背景(合并去重) | P3+P4 |
| 4 | 任务目标 | P5 |
| 5-18 | Excel-Agent架构→Step1→Step2→Step3→Step4 | P6-P19（顺序不变，仅确认LocatorAgent系列紧跟Step1） |
| 19-21 | Skill系统 | P20-P22 |
| 22-23 | 本轮工程优化×2 | NEW（`bench/ppt_*.md`） |
| 24 | Agent指标汇总 | NEW(收束，数字取自前页) |
| 25 | Merge章节封面 | P23 |
| 26 | Merge架构+流程(合并) | P24+P25 |
| 27-34 | 环境→base定位→Compare→ID重映射→公式保护→fast-apply→AI建议→并行性能 | P26-P34（顺序不变） |
| 35 | Merge指标汇总 | NEW(收束) |
| 37 | 验证结论 | P35 |
| 38 | 展望 | NEW |
| 39 | 总结 | P36 |
| 40 | 结束页 | P43 |
