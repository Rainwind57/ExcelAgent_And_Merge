# PPTX 全文导出：网易互娱通用PPT模板（含保密）_ExcelAgent答辩优化版.pptx
总页数：43


================================================================================
## 第 1 页
================================================================================
AI配表助手

——自然语言驱动配表+三方合并Merge

吴植贤
2026年9月4日


================================================================================
## 第 2 页
================================================================================
目录

01 业务背景与任务目标
02 Excel-Agent：4-Step 多 Agent 配表流水线
03 Merge：面向 SVN 目录分支的三方合并
04 总结归纳

02


================================================================================
## 第 3 页
================================================================================
一、业务背景

复杂 Excel 配表不是单表编辑问题，而是跨表、跨目录、跨版本的一致性工程。

03


================================================================================
## 第 4 页
================================================================================
业务背景：真实配表的复杂性

规模复杂

表达复杂

协作复杂

表多、字段多、跨表引用密集；
活动、奖励、技能、NPC、刷新点常跨多表。

用自然语言描述目标，通常不会精确写表名、列名、枚举值和外键链。

SVN 目录分支和外链并存；多人并行改 Excel，人工合并容易漏行、丢公式。

[表格]
编号 | 存在问题 | 后果
1 | Merge过程出现漏行丢数据情况 | 数据会静默消失
2 | 跨分支编号冲突，多分支创作过程新建同一编号 | 后提交的进行了覆盖导致线上bug
3 | 公式缓存丢失 | 编表会出现输出0或者空
4 | 改表慢、易错 | 效率低、易漏外键
5 | AI 误写不可逆 | 写坏数据找不回

[表格]
编号 | 存在问题 | 后果
1 | Merge过程出现漏行丢数据情况 | 数据会静默消失
2 | 跨分支编号冲突，多分支创作过程新建同一编号 | 后提交的进行了覆盖导致线上bug
3 | 公式缓存丢失 | 编表会出现输出0或者空
4 | 改表慢、易错 | 效率低、易漏外键
5 | AI 误写不可逆 | 写坏数据找不回

04


================================================================================
## 第 5 页
================================================================================
任务目标

[表格]
引擎 | 目标 | 关键约束
Excel-Agent | 输入自然语言->结构化意图+多表配表 | 存在校验、可执行、可跟踪的结构化意图
Merge | 目标目录分支模式实现Excel三方合并 | 保护公式、ID引用、冲突可解释性

05


================================================================================
## 第 6 页
================================================================================
二、4Step Excel-Agent

06


================================================================================
## 第 7 页
================================================================================
Excel-Agent 总体定位

Excel-Agent是面向用户提供复杂输入，能够将复杂输入进行解析，正确应用到表格的一个工具，不要求用户先知道表名和字段名。
系统先定位候选表，再让 LLM 在真实 schema 下拆解 JSON，随后由规则层做质量门禁。
最终目标是让 Step1 的 JSON 足够准确，因为后续 Step2/Step3/Step4 都依赖它。

架构总览：
LangGraph 编排 TableAgent 4-Step,具体如下：

[表格]
Step | 职责
1、解析 | 对输入执行：分段->定位->schema注入拆解->语义计划编译
2、校验 | 根据业务规则进行字段/类型/依赖等的校验
3、执行 | 依赖顺序确定性写盘、ID分配、引用回填
4、汇总 | 变更归纳、错误总结、自学习反模式

07


================================================================================
## 第 8 页
================================================================================
Prompt工程

[表格]
组件 | 内容 | 作用
角色设定 | 设定为Excel配表拆解专家 | 限定任务边界
真实schema注入 | 表名、sheet、列名、类型、外键FK等 | 防止出现幻觉字段
输出格式 | json | 后续json格式可执行
反例约束 | 约束不输出不存在列，不把叙述碎片当字段值 | 降低污染
示例 | 给出跨表引用的例子 | 提升结构稳定性
截断策略 | 只注入schema | 降低token使用，防止超时

[表格]
优化点 | 优化前 | 优化后
Prompt上下文缩减 | 全量表索引76.11MB | 单任务聚焦schema510字符，约159token
输出格式约束 | 自由文本，解析不稳定 | 固定JSON，可执行可校验

09


================================================================================
## 第 9 页
================================================================================
Step1：全链路的核心

Step1：自然语言->可执行计划intent

技术内容









指标优化

[表格]
操作 | 技术说明
分段 | 规则分段
候选表定位 | 规则+检索+关系扩展
schema注入 | 注入真实表头、类型和FK
LLM拆解 | DecomposeAgent输出结构化intents
produces推断 | FK图+主键规则，建立起跨表依赖关系

[表格]
指标 | before | after | 变化
定位功能 | 0.6667 | 0.8056 | +20.8%
覆盖度 | 0.6111 | 0.7500 | +22.7%

09


================================================================================
## 第 10 页
================================================================================
增强：LocatorAgent三级定位

定位方式：优先规则->检索->关系扩展，当出现歧义时由模型裁决，旨在表名实现模糊匹配

[表格]
通道 | 技术 | 作用
L1 规则通道 | 表名/中文表头/别名/字段关键词 | 直接命中可以零成本
L2 模糊通道 | rapidfuzz混合指标 | 拼写、命名等变体兜底
L3 关系扩展 | 外键(FK)一跳进行扩展 | 命中主表带上绑定表
L4 RAG | BM25语义召回 | 规则未命中时补召回语义近邻

10


================================================================================
## 第 11 页
================================================================================
增强：LocatorAgent三级定位

rapidfuzz混合指标：

rapidfuzz_score=WRatio*0.5+token_set_ratio*0.3+partical_ratio*0.2

[表格]
指标 | 作用
WRatio | 综合编辑距离、长度差、部分匹配，适合通用相似度
token_set_ratio | 忽略词序和重复词
partical_ratio | 长字段里包含短查询时仍能够命中

[表格]
指标 | 纯Python实现 | rapidfuzz C++算子
编辑距离计算 | 2781.54ms | 192.16ms
单次匹配平均 | 0.0093ms | 0.0006ms

10


================================================================================
## 第 12 页
================================================================================
增强：DecomposeAgent schema注入拆解

输入：候选表真实结构+多级表头+字段类型+外键FK+填表规则
输出：结构化json的intents

样例：
输入：Ability表格row1列名、row2类型、字段类型、外键、填表规则
输出：{action:add,table:Ability,fields:{id:100,name:”TEST”},produces:[<aid>]}

schema注入:模型只能从真实列名中进行选择，可以从源头解决幻觉列名的问题
few-shot：提示中附上正确ID/占位符的示例，禁止伪造占位符如<resolved_form_xxx>，同时超时降级逐表格重拆；
规则安全网：LLM为主，当LLM产生intent小于2条或者畸形时回退模板

[表格]
指标 | before | after
字段精准率 | 0.5185 | 0.6917
严格通过率 | 0.1667 | 0.3333

11


================================================================================
## 第 13 页
================================================================================
增强：produces/consumes依赖契约

跨表新增的时候，后续表格依赖钱买你新建行的ID，需要靠依赖契约将其确定下来，而不是靠模型记忆










样例：
新增活动activity_id=3001,新增奖励reward_id=30010,绑定activity_id=3001,新增邮件mail_id=5001,附件reward_id=3001

根据produces和consumes，后续step3的执行可以得到排序顺序，成功分配ID、回填外键

[表格]
术语 | 含义 | 作用
produces | 本意图产生的占位符 | 记录生成的ID
consumes | 本意图消费的占位符 | 记录使用的ID
占位符placeholder | 写表时临时标记 | 执行时替换为真实ID

[表格]
指标 | before | after
quest_npc引用一致 | 0.62 | 1.0
quest_npc耗时 | 134s | 107s

12


================================================================================
## 第 14 页
================================================================================
Step1 质量门禁

LLM结束产生intents以后ParseAgent继续schema-aware编译，进行再一次检验

[表格]
检查内容 | 修复方式
删除孤立add intent、同sheet 稀疏影子重复 | 删除
修字段不在schema、sheet或者name混用、目标表格大小写差异 | 修正到真实列
检查produces和consumes | 从已存在资源或新行解析出真实ID
自产自销、依赖环 | 阻断执行

[表格]
优化项 | before | after
type_mismatch | 2 | 1
失败trace归纳反模式 | 0 | 4类

ExcelAgent_And_Merge 技术答辩

14


================================================================================
## 第 15 页
================================================================================
增强:Step2 L0/L1/L2三层校验

三层校验

[表格]
层级 | 方法 | 处理内容
L0 规则闸 | 纯代码 | 类型、PK、FK、占位符等
L1 AI裁决 | 1次LLM | NL覆盖度、值合理性
L2 人工gate | 交互卡片 | 用户改字段/值后续跑

ExcelAgent_And_Merge 技术答辩

13


================================================================================
## 第 16 页
================================================================================
Step2：写盘前校验

Step2 是执行前的硬门禁：
- 字段是否存在，中文表头是否能映射到真实列。
- 值类型是否可转换，枚举值是否合法。
- FK 是否指向正确表和正确主键，placeholder 是否有 producer。
- Step1 质量报告如果有 hard error，阻断写盘并生成可交互修正。

15


================================================================================
## 第 17 页
================================================================================
Step2 交互修正

当前已有列名批量修正卡片，下一步应升级为“全字段可编辑表格”。
用户可以改列名、改值、删除字段或删除整条 intent，然后 POST /api/agent/reply 续跑。
后端应把用户编辑回写到原 intent，再重新跑 Step2，而不是只绕过错误。

16


================================================================================
## 第 18 页
================================================================================
Step3：确定性执行

Step3 不再调用模型，只做可解释写盘

执行动作：

[表格]
动作 | 技术
拓扑排序 | 按produces和consumes依赖执行先后
ID分配、占位符替换 | 新增行分配主键，替换后续placeholder
失败结构化归因 | 缺字段、PK冲突、类型错误等
可恢复边界 | 失败停于可恢复点，避免半写入污染

[表格]
测例 | before | after
复杂样例 | 204s | 112s

17


================================================================================
## 第 19 页
================================================================================
增强:Step4结果汇总与自学习闭环

Step4汇总最终成功或失败情况，同时还将失败的情况进行沉淀

[表格]
功能 | 说明
汇总结果 | 成功数+失败数+写入行+预览
失败归因 | 定位失败发生在哪个层
anti_patterns | 从失败trace归纳反模式
mini_regression | 失败样例进入小回归集

ExcelAgent_And_Merge 技术答辩

18


================================================================================
## 第 20 页
================================================================================
Skill 系统

将列名、如何定位行、哪些操作历史出错沉淀成可查询规则，Agent 与 Merge 共用同一份知识源。

[表格]
层级 | 目录 | 内容 | 生成方式
L0 人工/根目录 | skills/*.yaml | sheet别名、引导动词、列短形式 | 人工+自动混合
L1 自动派生 | L1_derived/ | 列别名、行定位规则、多sheet消歧、类型约束 | 扫描resources/*.xlsx自动生成
L2 运行时 | L2_runtime/ | 运行时学到的列别名(带hits/confidence)、跨表关系权重 | 从evidence promote写入
L3 反模式 | L3_anti_patterns/ | 失败模式规则：歧义列/类型约束/失败操作/语义模式 | 规则频次升级+AI归纳

19


================================================================================
## 第 21 页
================================================================================
Skill 系统：生成与审核机制

[表格]
判定 | 阈值
列别名候选转正 | 同一张表/同一个分页里，用户把某个词说成某列，七天内命中够 3 次、且系统对这次猜测的把握在 30%~75% 之间 → 才把这个别名收进知识库
反模式升级active | 某列被判定容易认错的情况，7天内出现≥3次 → 系统自动改成只许精确匹配，不许模糊猜
反模式升级pending_review | 某个操作失败，30天内出现≥2次 → 先标记"待人工审核"，下次遇到同类操作会弹窗要用户确认，但还不是最终生效规则
AI归纳反模式转正 | AI 自己总结出来的"这种情况容易出错"，因为没有人工标注的标准答案兜底，所以门槛更高——要多命中 3 次才能真正生效
冲突消解 | 同一个用户说法，被两种不同的"标准列名"抢着对应 → 系统不敢自己决定，先不收录，交给人工来判断
衰减 | 一条规则 60 天都没再被用到 → 自动移除；如果这条规则的可信程度掉到 30% 以下 → 先"休眠"

AI归纳的反模式先进pending_review，跑过mini回归(30样本,lift≥0.05)确认没把已有能力改坏才转正active，不通过则回滚+隔离区(quarantine)。

20


================================================================================
## 第 22 页
================================================================================
Skill 系统：效果数据

[表格]
数据点 | 指标 | before | after
skill简版30样例 | 定位成功率 | 0.933 | 1.000
skill简版30样例 | 精确命中 | 0 | 1.00
skill简版30样例 | 严格综合 | 0.704 | 0.959
skill简版30样例 | 平均耗时 | 533ms | 321ms
skill真写盘8样例 | 定位/覆盖/精准 | 0.472/0.417/0.497 | 0.639/0.583/0.590
Step4反模式沉淀(模拟回放) | 同类失败预警覆盖 | 0/8(0%) | 8/8(100%)
Step4反模式归纳(真实e2e) | 8条失败trace→4类反模式 | — | 命中PK冲突/seed缺失/conv_id定位/类型转换

21


================================================================================
## 第 23 页
================================================================================
三、Merge 合并方案

19


================================================================================
## 第 24 页
================================================================================
Merge

[表格]
Merge模式 |  | 过程
跨分支合并 | 分支合并 | 通过From分支与To分支比对合并到To分支
 | 合并回trunk | 分支合回trunk，内容集成在trunk分支
目录合并 |  | 将子目录的生产内容合并回trunk分支

Merge-base

From分支

To分支

Merge合并结果


================================================================================
## 第 25 页
================================================================================
Merge 总体流程

1. 选择From/To两个目录。
2. 通过SVN copyfrom定位 merge-base。
3. 提取 Excel 数据、表头、公式、样式和文件元信息。
4. 比较变更并识别冲突。
5. ID 重映射与引用更新。
6. AI 辅助冲突建议。
7. LibreOffice 重算、公式保护、安全写回。

两套Merge模式除了分支、base确定上有差异，核心diff逻辑一致。

20


================================================================================
## 第 26 页
================================================================================
模拟生产环境

SVN demo 目录模拟真实项目结构：trunk、branches配表目录。
重点不是复制 Git 分支，而是在 SVN 的历史信息里还原“共同祖先”。
这样 Merge 才能判断谁改了什么，而不是简单覆盖文件。

目前的SVN目录如下所示：
merge/svn/demo_svn/
├── repo/                      # svnadmin 仓库（file:// 直连，无需 svnserve）
└── wc/                        # 工作副本
    ├── trunk/                 # 74 张 + subdev_1/2/3 子目录
    │   ├── subdev_1/          # ability/const/item_drop/monster/reward（5 私有表）
    │   ├── subdev_2/          # const/fabao/guild/mail/tips（5 小表）
    │   └── subdev_3/          # ability/reward/map/interaction/world_buff（5 小表）
    └── branches/
        ├── dev1/              # 全量（跨分支 source/target）
        ├── dev2/              # 全量
        ├── dev3/              # 小表（删了大表，改 ability/reward/tips）
        └── dev4/              # 小表（删了大表，改 const/fabao/guild）

[表格]
情况 | copyfrom-path | 标记
正常切分支 | trunk@r1 | inferred=false
新建 | 路径本身 | inferred=true

21


================================================================================
## 第 27 页
================================================================================
SVN merge-base 定位

在选择分支/目录后，找到base分支与base版本的核心方法：copyfrom反查

1、From分支和To分支各自寻找自身的copyfrom；







2、沿着copyfrom链上溯，寻找完整的祖先链；
3、两条copyfrom链交叉寻找最近公共祖先(LCA)

[表格]
版本 | 方法 | 解决问题 | 性能提升
A | Python三重循环 | —— | 10w行*100列会卡死
B | 根据哈希建立row_idx_maps | O(n)线性扫描卡死 | 哈希定位实现O(n)->O(1)
C | 使用sparse稀疏化 | 全表建立哈希内存过大 | 内存从GB级别降至MB级别
D | numpy向量化 | 逐单元格python函数调用需要单一化，消耗高 | 纯数据大表提升10-50倍
E | 公式、批注在sheet回退逐单元格循环 | 解决公式、批注向量化出错的问题 | 保证性能同时保证正确性

[表格]
情况 | copyfrom-path | 标记
正常切分支 | trunk@r1 | inferred=false
新建 | 路径本身 | inferred=true

[表格]
优化 | before | after
SVN rev查询 | 每张表单独查一次版本，总计191s | 一次递归查询全目录，缩减为0.2s

22


================================================================================
## 第 28 页
================================================================================
性能优化：稀疏化与向量化

大表不能逐单元格 Python 循环。
系统把变更表示为 sparse diff，只处理真正变化的行列区域。
数值和字符串比较尽量向量化，减少 Python 层循环。
实测 10w 级行表比较可从分钟级降到秒级。

ExcelAgent_And_Merge 技术答辩

24


================================================================================
## 第 29 页
================================================================================
Compare 全流程

比较过程分为四层：
文件层：新增、删除、重命名、移动。
Sheet 层：新增 sheet、删除 sheet、表头变化。
行列层：主键匹配、行新增删除、字段变更。
语义层：公式、外键引用、ID 冲突和可合并性判断

技术:

优化：

[表格]
技术 | 解决问题 | 效果
哈希索引 | list.index线性扫复杂度O(n) | 行定位O(1)
sparse diff | 全表物化100列 | 只保留变化格
numpy向量化 | 解决逐格归一化的问题 | 批量进行比较
公式、批注回退 | 向量化无法理解公式、批注 | 保证了正确性

[表格]
指标 | before | after
大表compare | 平均48s | 平均6.4s
10万行全链 | 卡死 | 10.4s
提交数据量 | 46.4MB | 0.1MB

23


================================================================================
## 第 30 页
================================================================================
ID 重映射与引用完整性

同主键多分支新增时，会出现主键冲突，对于后来者分配新的ID
随后同步更新引用列，避免只改主表 ID 却留下悬空外键，保证引用一致性。


这一层和 Excel-Agent思路一致：先建立依赖契约，再执行替换，否则会出现引用链断裂。

ExcelAgent_And_Merge 技术答辩

25


================================================================================
## 第 31 页
================================================================================
公式缓存保护

Excel 文件内部公式和缓存值分离，直接写盘容易造成缓存丢失或公式失效。




系统通过使用zip扫描excel文件底层的XML字节流，查找<f>公式标签判断表格中是否存在公式
，定位 changed/lost 公式。
10w行的表格，openpyxl全量读取需要6.1s，使用zip扫描降到 0.05s，减少大文件处理开销。

<f>formula</f>   
<v>value</v>

<f>formula</f>

校验与修复全流程：
1、表中无公式则走最快速路径；
2、将保存后文件读回，并与保存前快照做比较，若保存前后出现缓存值消失，则为缓存丢失，标记为lost；若前后值不一致，则属于正常的缓存更新，标记为changed；
3、对于changed情况，无需修复；
4、对于lost情况，调用LibreOffice对文件进行重算，进行再度校验。

设置保护:
60s超时上限，避免单文件卡死全流程；
文件添加互斥锁，保护文件不被损坏；
重算结果先输出到临时目录，再替换掉源文件

26


================================================================================
## 第 32 页
================================================================================
公式缓存与fast-apply

公式单元格冲突解决：
对于公式单元格，系统会将其识别为公式列(diff_type=formula)
公式写法一致但缓存值不一致，视为不冲突，如果公式引用的输入值发生变化，会有标记formula_changed，调用内置求值器预览重算结果进行展示；
公式写法不一致则视为公式冲突，这时在冲突显示界面会显示单元格原有的公式，供用户比对选择。

选择写回
用户选择公式版本以后，系统回打上formula_resolved标记，在apply写回阶段根据标记将用户选择的公式文本写回单元格。

对于大数据量表格，fast_apply快路径+zip+XML的形式直接修改目标单元格，不触碰公式单元格，不会破坏公式缓存。
Merge进行到最后是合并结果写回xlsx,用openpyxl消耗时间太长

xlsx本质是zip包，用zipfile打开源文件，只读需要修改的sheet的XML,用lxml/ElementTree定位到具体单元格，最后将改完的XML放回新zip

[表格]
因子 | 作用
SVN rev/SVN date | 版本越新倾向于采纳
列名语义 | 判断值是否合理
单元格值 | 判断值是否异常
基准值 | 提供base值供参考

[表格]
 | before | after
apply处理 | 19.6s | 4.3s
序列化 | 4.9s | 0.3s

27


================================================================================
## 第 33 页
================================================================================
AI 辅助冲突处理

AI 只提供冲突建议，其完整的建议链路为：
点击AI建议前端发送请求->调用线程池进行运行->根据LLM输入拼成prompt发送给模型->LLM返回Json输出->防幻觉校验


AI建议 相关的因子:







输出json：建议保留来源、理由和置信度，最终由用户确认。

置信度结合规则一致性、字段风险、冲突复杂度和模型解释质量。

置信度相关的因子:
LLM自评因子llm_conf:LLM自行评价得出指标；
修订号领先度因子rev_conf:版本越领先取值越高；
值域合理性因子value_conf:取值越合理分支越高




AI建议并行操作使用的技术：
缓存优先：缓存键由表名、sheet名、列名和版本哈希组成，命中且未过期则返回；
批量LLM：将为缓存的单元格拼接进prompt，让LLM一次调用返回json，根据index返回位置；
回退并行：在批量操作失败的情况下用线程池并发逐格调用。

[表格]
因子 | 作用
SVN rev/SVN date | 版本越新倾向于采纳
列名语义 | 判断值是否合理
单元格值 | 判断值是否异常
基准值 | 提供base值供参考

28


================================================================================
## 第 34 页
================================================================================
大表格比对性能

Compare 和写回阶段按文件/Sheet 粒度并行。
CPU 密集比较使用 ProcessPool，I/O 和解析任务使用 ThreadPool。
worker 上限跟 CPU、文件数和内存占用绑定，避免并行越多越慢。
fast_apply 让无冲突、低风险文件快速落盘。

[表格]
优化项 | 受益
calamine Rust 读引擎 | 222次打开表格时长从17.5s优化到0.8s
svn info批量预填 | 192s->0.2s
ProcessPool/ThreadPool并行 | 对于8个表格时长从3.69s优化到1.84s
fast_apply快路径 | 25MB的sheet XML序列化从3.4s优化到0.98s
未变更表跳过 | 可以跳过占大多数的表格体

[表格]
优化项 | 受益
calamine Rust 读引擎 | 222次打开表格时长从17.5s优化到0.8s
svn info批量预填 | 192s->0.2s
ProcessPool/ThreadPool并行 | 对于8个表格时长从3.69s优化到1.84s
fast_apply快路径 | 25MB的sheet XML序列化从3.4s优化到0.98s
未变更表跳过 | 可以跳过占大多数的表格体

29


================================================================================
## 第 35 页
================================================================================
当前验证结论

最新代码合并后，核心回归通过 71 项；此前相关 agent 回归通过 109 项。
这次更新对智能化有正向价值：enum resolver、schema-aware parse 修复、locator 调整都在减少模型幻觉。
剩余风险集中在复杂链路：跨段代词、省略主语、枚举口语化、资源缺必填字段。

ExcelAgent_And_Merge 技术答辩

30


================================================================================
## 第 36 页
================================================================================
四、总结

Excel-Agent 的正确方向不是堆 prompt，而是“LLM 理解 + schema 编译 + 规则校验 + 确定执行”。
Merge 的正确方向是“可信 Base + 稀疏 diff + 引用保护 + 安全写回”。
后续重点：全字段交互修正、复杂链路上下文继承、经验沉淀、更多端到端回归和可观测指标。

ExcelAgent_And_Merge 技术答辩

31


================================================================================
## 第 37 页
================================================================================
模板使用说明

1、本模板为通用模板，仅适用于基础性制作需求，使用时请根据工作实际需求进行微调。
-  PPT 模板中的背景版式、辅助图形、文字内容、配图等，可进行相应替换、删减。
-  PPT 模板中的公司 Logo，仅适用于可代表公司层面的相关商务洽谈、合作、展示场景。非必要情况，请勿使用，或可替换为其它适用的 Logo。
2、如您在使用中对此模板有任何问题及建议，请添加 popo 群沟通反馈（ popo 群号：1587248）。本模板将基于使用者建议定期优化更新。
联系人
总裁办 薛婧婷 hzxuejingting@crop.netease.com
UEDC 杨魏 hzyangwei@corp.netease.com


================================================================================
## 第 38 页
================================================================================
2

标题内容简述部分说明通知

Template description


================================================================================
## 第 39 页
================================================================================
图表配色建议

The content of the title


================================================================================
## 第 40 页
================================================================================
图表内容-柱状图

The content of the title


================================================================================
## 第 41 页
================================================================================
图表内容-柱状图

The content of the title


================================================================================
## 第 42 页
================================================================================
图表内容-饼状图

The content of the title


================================================================================
## 第 43 页
================================================================================
THANK YOU
