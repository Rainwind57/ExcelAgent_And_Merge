# PPTX 现状梳理 + 可补充创新点（供后续加页用）

> **源文件**：`网易互娱通用PPT模板（含保密）_ExcelAgent答辩优化版.pptx`（2026-09-01 14:51 最新版，43 页，含 7 页模板/图表说明页，**实际内容页 1-36**）。
> **本文档两部分**：
> - **第一部分**：把现有 36 页内容页转成结构化 md（讲什么/表格/指标），方便你核对现状。
> - **第二部分**：核对项目代码与 `PERF_DATA_SUMMARY.md` 后，找出**已有真实 before/after 数据但还没进 PPT** 的创新点，标好证据路径，可以直接抄进 PPT。

---

# 第一部分：现有 PPTX 内容（逐页还原）

## 目录结构

| 页 | 内容 |
|---|---|
| 1-2 | 封面 / 目录 |
| 3-5 | 业务背景、任务目标 |
| 6-22 | 二、4-Step Excel-Agent（架构、Prompt、Step1-4、LocatorAgent、rapidfuzz、DecomposeAgent、produces/consumes、Skill 系统） |
| 23-35 | 三、Merge（架构、merge-base、Compare、ID重映射、公式缓存、AI建议、并行性能、验证结论） |
| 36 | 四、总结 |
| 37-43 | 模板使用说明 + 图表配色示例（非内容页，可删或留作备用版式） |

## 第 1-5 页｜封面与业务背景

- **P1 封面**：AI配表助手——自然语言驱动配表+三方合并Merge｜吴植贤｜2026年9月4日
- **P2 目录**：01业务背景与任务目标 / 02 Excel-Agent 4-Step / 03 Merge / 04 总结归纳
- **P3**：一句话点题——复杂 Excel 配表是跨表/跨目录/跨版本一致性工程
- **P4 业务背景**：规模复杂/表达复杂/协作复杂三维度 + 问题-后果表（漏行丢数据、跨分支编号冲突、公式缓存丢失、改表慢易错、AI误写不可逆）
- **P5 任务目标**：Excel-Agent（自然语言→结构化意图+多表配表）/ Merge（目录分支三方合并，保护公式/ID引用/冲突可解释）

## 第 6-22 页｜Excel-Agent 4-Step

- **P7 总体定位**：LangGraph 编排 4-Step（解析/校验/执行/汇总）
- **P8 Prompt 工程**：角色设定+真实schema注入+输出JSON+反例约束+few-shot+截断策略；指标：Prompt上下文 76.11MB→510字符/159token
- **P9 Step1**：分段+候选表定位+schema注入+LLM拆解+produces推断；指标：定位0.6667→0.8056(+20.8%)，覆盖度0.6111→0.75(+22.7%)
- **P10 LocatorAgent 三级定位**：L1规则/L2模糊(rapidfuzz)/L3关系扩展(FK)/L4 RAG(BM25语义召回)
- **P11 rapidfuzz混合指标**：WRatio×0.5+token_set×0.3+partial×0.2；编辑距离 2781.54ms→192.16ms
- **P12 DecomposeAgent**：schema注入拆解JSON，few-shot禁止伪造占位符，规则安全网兜底；指标：字段精准率0.5185→0.6917，严格通过率0.1667→0.3333
- **P13 produces/consumes依赖契约**：跨表ID回填；指标：quest_npc引用一致0.62→1.0，耗时134s→107s
- **P14 Step1质量门禁**：schema-aware编译二次校验；type_mismatch 2→1，失败trace归纳反模式0→4类
- **P15 Step2 三层校验**：L0规则闸/L1 AI裁决/L2人工gate
- **P16 Step2写盘前校验**：字段/类型/枚举/FK/占位符门禁
- **P17 Step2交互修正**：列名批量修正卡片（现状），计划升级全字段可编辑表格
- **P18 Step3确定性执行**：拓扑排序+ID分配+失败结构化归因；复杂样例204s→112s
- **P19 Step4自学习闭环**：汇总结果+失败归因+anti_patterns+mini_regression
- **P20-21 Skill系统**：L0人工/L1自动派生/L2运行时/L3反模式四层 + 转正/衰减/冲突消解阈值规则
- **P22 Skill系统效果数据**：简版30样例定位0.933→1.0，耗时533ms→321ms；真写盘8样例定位/覆盖/精准均提升；Step4反模式沉淀(模拟回放) 0/8→8/8

## 第 23-35 页｜Merge

- **P24-25 架构与流程**：跨分支合并/目录合并两模式，merge-base→Compare→ID重映射→AI建议→重算写回七步
- **P26 模拟生产环境**：demo_svn 目录结构（trunk 74表+3子目录，branches dev1-4）
- **P27 merge-base定位**：copyfrom反查+LCA；版本 A→E 优化链（三重循环卡死→哈希O(1)→sparse→numpy向量化10-50倍→公式/批注回退保正确性）；SVN rev查询 191s→0.2s
- **P28 稀疏化与向量化**：10w行比较分钟级→秒级
- **P29 Compare全流程**：文件/Sheet/行列/语义四层；大表compare 48s→6.4s，10万行全链卡死→10.4s，提交量46.4MB→0.1MB
- **P30 ID重映射与引用完整性**：主键冲突分配新ID+级联更新引用列
- **P31 公式缓存保护**：zip扫`<f>`标签判断公式，10w行 6.1s→0.05s；lost/changed判定+LibreOffice重算+60s超时+互斥锁+临时目录
- **P32 公式缓存与fast-apply**：formula_changed/formula_resolved标记，zip+lxml直改XML；apply处理19.6s→4.3s，序列化4.9s→0.3s
- **P33 AI辅助冲突处理**：AI只做建议不进核心策略；置信度=llm_conf+rev_conf+value_conf；缓存优先+批量LLM+回退并行
- **P34 大表格比对性能**：calamine 17.5s→0.8s，svn info批量192s→0.2s，ProcessPool/ThreadPool 3.69s→1.84s，fast_apply序列化3.4s→0.98s
- **P35 当前验证结论**：核心回归71项，agent回归109项

## 第 36 页｜总结

Excel-Agent="LLM理解+schema编译+规则校验+确定执行"；Merge="可信Base+稀疏diff+引用保护+安全写回"；后续重点：全字段交互修正、复杂链路上下文继承、经验沉淀、更多端到端回归。

---

# 第二部分：核对项目后发现的可补充内容

> 核对 `PERF_DATA_SUMMARY.md`、`bench/ppt_*.md`、`docs/OPTIMIZATION_LEDGER.md` 后，以下内容**有真实/模拟压测 before/after 数据，但当前 43 页 PPT 里完全没有**，是本轮（2026-09-01）刚做的代码优化，最适合作为"创新点"补充。

## 一、【强烈建议加页】本轮 5 项代码优化闭环（未提交改动，已模拟压测验证）

背景：这批改动围绕"对话树占位符闭环""命名风格漂移误报""聊天噪音""LLM成本控制""Step4口径修复"，都是**真实调用仓库函数**（部分子项因依赖过重简化为状态机模拟，已逐条标注），可以做成一页"本轮工程优化"或拆成 2 页。

| # | 创新点（一句话） | 模块 | 指标 | Before | After | 证据 |
|---|---|---|---|---:|---:|---|
| 1 | 占位符命名归一化，消除产物/消费引用命名漂移误报 | `validator_agent._norm_name` | FORWARD_REF_BROKEN 假阳性（6组应匹配样例） | 5/6 误报 | **2/6 误报** | `tools/simulate_validator_forwardref_norm.py` |
| 2 | 嵌套占位符递归扫描（dict/list 任意深度） | `step3_execute_subagent` | 8条混合样例漏检嵌套占位符 | 7 处漏检 | **0 处漏检** | `tools/simulate_placeholder_nested_backfill.py` |
| 2b | 循环依赖回填（forward-ref backfill），打乱执行顺序仍可全部回填 | 同上 | 5节点链式依赖，可执行节点数 | 1/5 | **5/5** | 同上 |
| 3 | 聊天区用户可见内容过滤+摘要+截断，降噪不降信息量 | `agent_service` chat_stream | 25条混合会话展示行数 / 噪音占比 | 24行 / 66.7% | **8行 / 0%** | `tools/simulate_agent_service_noise_filter.py` |
| 4a | 分组硬上限（chain_group+max_groups），控制LLM子调用成本 | `decompose_agent` | 12候选实际跑的分组数（=LLM子调用次数） | 3 | **2** | `tools/simulate_decompose_chain_cost.py` |
| 4b | dict列lint 修复合法嵌套字段误清空 | `decompose_agent._lint_split_intents` | 3个合法dict列场景误清空率 | 100% | **0%** | 同上 |
| 5 | Step4 `all_ok` 口径修复（不再用 s3.ok 单点代表整体） | `step4_conclude_subagent` | 8种(s1,s2,s3)组合中判定错误场景数 | 3/8 错误 | **0/8 错误** | `tools/simulate_step4_allok_drift.py` |

**讲法建议**：
> "占位符协议、命名匹配、聊天展示、LLM成本、结果口径"——5个看似独立的小修复，本质都是同一条主线：**让 4-Step 流水线的每一环都"说真话"**（不误报、不漏检、不多花钱、不假成功）。

**诚实边界（讲台上主动说更显专业）**：
- 表中 #2b 是"简化状态机模拟"而非直接调用生产 `_backfill_forward_refs`（该函数依赖 Agent 实例上下文，单独调用成本过高）。
- #4a 是按 git diff 复原的分组骨架模拟（真实分组逻辑嵌在需要真实 LLM 会话的方法内）。
- #1/#4b/#5 是**真实函数直接 import 调用**，无 mock。
- 这批改动**尚未合并进 43 页 PPT 提到的"回归 180 项"基线**，如果要用，建议标注日期"2026-09-01 模拟压测"，不与端到端准确率混用。

## 二、Skill 系统安全阀机制（PPT P21 已提转正阈值，建议补一句"转正安全阀"代码机制）

现状 P21 已经讲了阈值规则，但没提**代码层的双重保险**：
- `skill_updater.py::promote_with_guard`：候选 → 快照 → 写盘 → mini回归(30样本, lift≥0.05) → 通过才转 active，不通过**自动回滚 + 隔离区(quarantine) 30天**。
- AI 归纳的反模式比确定性规则门槛更高（多需命中 3 次），因为没有 ground-truth 兜底。

这是"自学习系统不失控"的关键证据，建议在 P21/P22 补一句或加一个小图（候选→快照→回归→转正/回滚流程图），比纯文字阈值表更有说服力。

## 三、以下是有清晰工程价值但**暂无强 before/after 数字**的候选创新点（可选，视时间决定加不加）

> 这些是 `docs/OPTIMIZATION_LEDGER.md` 里记录的近期架构改动，创新性强，但多数是 opt-in 默认关闭 / 阻塞在外部 `codemaker serve`，没有可靠的量化对比，讲的时候要老实说"设计完成，e2e 数据待补"。

| 创新点 | 一句话 | 数据现状 |
|---|---|---|
| Replan-on-failure（失败后自动重规划） | Step5 执行失败不再只是上报，而是聚合 failures+剩余子任务，触发 1 次 LLM 重规划后重跑（上限2轮） | 已落地+20个单测，e2e 阻塞于 R7（serve侧），无量化对比 |
| 编号账本跨分支查重（`claim_id`） | 多分支同时新建同一 ID 时，实时查重+建议下一个空闲号，替代"后提交覆盖" | 直接对应 PPT P4 痛点"跨分支编号冲突"，但目前是单测验证，无真实多分支压测数字 |
| BM25 语义召回（RAG 阶段一） | LocatorAgent 规则+模糊都没命中时，用 BM25 对表结构做语义召回兜底 | PPT P10 已经提到"L4 RAG"，但没有召回率/命中率对比数据，可加一组 A/B（有无BM25层的 locate_rate 对比）作为后续实测方向 |
| Schema 血缘联动（跨分支列同步预览） | 扫描多分支 Excel 列定义，标出 trunk 有列但其他分支缺的情况，为 Merge 提供列级血缘视图 | 无量化数据，属于"发现问题"能力而非"性能"能力 |
| pre_commit_hold 统一拦截通道 | 公式丢失/批注丢失/ID冲突三类风险，统一走同一个"暂停确认"事件+审计留痕，而不是各自静默处理 | 定性能力（可观测性/可追溯性），无 before/after 数字，可用"审计留痕数：0→N类事件"这种覆盖度指标代替性能指标 |

**建议**：如果 45 分钟答辩时间够，可以在"总结与展望"章节前加 1 页"工程闭环设计（规划中/已落地未验证）"，把这 4 项放进去，明确标"设计完成，量化数据待 serve 环境解封后补齐"——这本身也是一种严谨性的展示，比堆砌数字更让人信服。

## 四、顺带发现的小问题（与内容无关，建议顺手修）

现有 43 页 PPT 页脚编号有多处重复/跳号（如 P8="09"与P9="09"重复、P10="10"与P11="10"重复、P20="19"与P23="19"重复、P25="20"与P26="21"与P30/P31 均出现"20/21"重复），建议在最终定稿前统一用"插入幻灯片编号"字段而非手打文本，避免翻页时观众发现编号乱跳。

---

# 落地建议（如果要立刻加页）

1. **必加**：第二部分「一」5项本轮优化 → 建议作为 Excel-Agent 章节新增 1-2 页，题目可用《本轮工程优化：让每一环都"说真话"》。
2. **建议加**：第二部分「二」Skill 安全阀流程图 → 并入现有 P21，不用加新页。
3. **可选加**：第二部分「三」4项规划中创新点 → 若时间紧可只口头提及，不占页面；若时间够可加 1 页放在"总结"前。
4. **顺手修**：页码字段统一。

以上内容对应证据文件均已在表格中列出，可直接核对后誊入 PPT。
