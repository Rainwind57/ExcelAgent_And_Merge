# Excel-Agent × Merge 答辩 PPT 逐页脚本（完整版）

> **本文件用途**：可直接照着做 PPT 的逐页脚本，共 **40 页**，按"业务背景 → 系统架构 → Excel-Agent 内部 → Merge 内部 → 验证与总结"的顺序组织，前后页之间尽量做到"上一页留的问题，下一页回答"。
> **写法约定**：
> 1. 不单独开"术语解释"段落，术语在**第一次出现的句子里用括号就近解释**；
> 2. **只有英文/外来术语，或确实生僻的专业词才加括号注释**（比如 `BM25`、`schema`、`quarantine`），日常中文词（别名、账本、休眠等）靠上下文和例子理解，不重复加注；
> 3. 每页仍保留：一句话定位 →【配图占位】→ 例子 → 数据表（如有）。
> **代码基准**：2026-09-02 初核，2026-09-03 二次核对（已对照 `excel-agent-and-merge.pptx` 现有 32 页 + `PERF_DATA_SUMMARY.md` 单一事实源 + 真实代码函数/常量定位）。**同步收录今日（09-03）excel-agent 系统性重写落地项⑨-⑭，已各自独立成页并归入所属 Step 环节**（⑨→P11-6、⑫→P11-4、⑬→P13b、⑪→P14b、⑩⑭→P15b、Step1.5 契约校验层→P15c），与有 bench 数据的①-⑧ 严格区分。

---

# 〇、PPTX 现状对照（当前内容 vs 可补充创新点）

> **对照基准**：`excel-agent-and-merge.pptx` 共 **32 页**（2026-09-03 提取），本脚本规划 **40 页**。本节作为第二步"原地扩充 PPTX"的施工清单：A 表逐页对照现状 → B 列完全缺失页 → C 列内容口径纠正 → D 列施工优先级。
> **核对结论**：① 脚本全部指标与 `PERF_DATA_SUMMARY.md`（单一事实源）一致，零冲突；② 所有代码引用（`_run_verify_repair_loop`/`_compute_rollback_targets`/`_save_nl_checkpoint`/`_run_replan_phase`/`claim_id`/`_is_pk_missing`/`_pk_inferred_downgrade`/`_llm_judge_pk_required`/`_check_business_required_pre_add`/`_llm_judge_business_required`/`_auto_resolve_enum`/`_norm_name`/`get_primary_key_overlay`/`get_required_false_overlay`/`promote_with_guard`）经 grep 全部真实存在；③ 所有关键阈值常量（`PROMOTE_HITS=3`/`PROMOTE_WINDOW_DAYS=7`/`PROMOTE_MIN_CONF=0.3`/`AP_FAILED_OP_HITS=2`/`RELATIVE_LIFT_THRESHOLD=0.05`/`MINI_REGRESSION_SAMPLE=30`/`DECAY_REMOVE_DAYS=60`）与脚本描述逐字一致（注：`MINI_REGRESSION_SAMPLE=30` 是回归抽样常量，但生产默认 `CODEMAKER_SKIP_REGRESSION=1` 跳过同步回归，见 P24；`repair_recipes.yaml`/`L2_runtime/column_aliases.runtime.yaml` 当前为空骨架、数据未跑出，见 PERF §5 诚实边界）；④ 置信度公式 `0.60/0.25/0.15` 在 `agent_service.py:94-96`、step4 `all_ok` 口径修复在 `step4_conclude_subagent.py:144-189`（最终收敛到 `is_clean_success`）真实落地。**→ 第二步扩充 PPTX 时照搬本脚本数据即可，不另造数字。**

## A. 现有 32 页 vs 脚本 40 页 · 逐页对照

> 现状口径（按**表格单元格级**实读，非 `shape.text` 表层）：✅表格已齐 / △小补即可 / ⚠需改字 / ◻仅标题。
> **重要修正**：首轮若用 `shape.text` 读 TABLE 形状会显示空白，会误判"页很空"；实读单元格后，现有 32 页里绝大部分**已有完整表格**，真正缺口远小于初判。

| PPTX | 标题（实际） | 对应脚本页 | 现状 | 真实差距 |
|---|---|---|---|---|
| 1 | 封面 | P1 | ✅ | 无 |
| 2 | 目录 | P2 | ✅ | 无 |
| 3 | 一、业务背景（一句话） | P3 | ✅引子 | 章引子页，留一句即可；三维复杂性已落到 slide4 |
| 4 | 真实配表复杂性 | P3+P4 | ✅合并 | 已含三复杂度卡（规模/表达/协作）+ P4 五类问题后果表（编号/问题/后果）；信息密集，**不必再单开 P4** |
| 5 | 任务目标 | P5 | ✅ | 已有引擎目标表 |
| 6 | 二、4Step Excel-Agent（章封） | P7 | △ | 仅大字；已有空白副题框（sh1）可直接填副题 |
| 7 | Excel-Agent 总体定位 | P8 | ✅ | 已有 4-Step 职责表 |
| 8 | Prompt 工程 | P9 | ✅ | 已有组件表 + 优化表（76.11MB→510字符/159token） |
| 9 | Step1：全链路核心 | P10 | ✅ | 已有操作表 + 指标表（定位 0.6667→0.8056 等） |
| 10 | ⚠"LocatorAgent 三级定位" | P11 | ⚠ | 标题写"三级"，但表内**已含 L1-L4 四行（含 L4 RAG/BM25）**→改"四级"；内容齐 |
| 11 | ⚠"LocatorAgent 三级定位" | P12 | ⚠ | 标题沿用错误；实讲 rapidfuzz→改"rapidfuzz 混合评分"；三指标表 + 性能表（2781.54→192.16ms）已齐 |
| 12 | DecomposeAgent schema 注入 | P13 | ✅ | 已有精准率指标表（0.5185→0.6917） |
| 13 | produces/consumes 依赖契约 | P14 | ✅ | 已有术语表 + 指标表（引用一致 0.62→1.0）；正文"钱买你"为错字→改"前面" |
| 14 | Step1 质量门禁 | P15 | ✅ | 已有检查内容表 + 优化项表 |
| 15 | Step2 L0/L1/L2 三层校验 | P16+P17 | ✅合并 | 已合并层级表 + 交互修正文字描述，无需再单开 P17 |
| 16 | Step3 确定性执行 | P18 | ✅ | 已有动作表 + 204s→112s 指标 |
| 17 | Step4 结果汇总与自学习 | P21 | △ | 有功能表；可补 `all_ok` 口径修复一句 |
| 18 | 业务规则 | P22 | ✅ | 已有目录消费表 |
| 19 | Skill 系统 | P23 | ⚠ | 首句误抄成"业务规则"那句→换 P23 一句话定位；四层表已齐 |
| 20 | Skill 生成审核机制 | P24 | ✅ | 已有完整判定阈值表 |
| 21 | Skill 效果数据 | P25 | ✅ | 已有完整数据表（定位 0.933→1.000、反模式 8/8 等） |
| 22 | 优化 | P26+P27 | ⚠ | 表只有 ①②③④（命名归一/嵌套占位符/执行顺序/dict列lint）；**缺 ⑤⑥⑦⑧**（all_ok / claim_id / Replan / Step2 三处硬规则纠偏）= **最大内容缺口** |
| 23 | 三、Merge 合并方案（章封） | P28 | △ | 缺副题 |
| 24 | Merge（base/From/To/结果） | P29 | ✅ | 已有跨分支/目录两种模式表 |
| 25 | Merge 总体流程 | P30 | ✅ | 已有 7 步 + demo_svn 目录树 |
| 26 | SVN merge-base 定位 | P31 | ✅ | 已有 A→E 优化链表 + inferred 表 + SVN 指标表（191s→0.2s） |
| 27 | Compare 全流程 | P32 | ✅ | 已有四层 + 技术表 + 指标表（48→6.4s、10w→10.4s、46.4→0.1MB） |
| 28 | Merge（ID 重映射+公式缓存保护） | P33+P34 | ✅合并 | 已合并；含 lost/changed/保护逻辑 |
| 29 | 公式缓存与 fast-apply | P35 | △ | 有 apply 指标表（19.6→4.3s、4.9→0.3s）；sh2 另存一张因子表疑似与 slide30 重复→核对 |
| 30 | AI 辅助冲突处理 | P36 | ✅ | 已有建议链路 + 因子表 + 置信度公式 |
| 31 | 大表格比对性能 | P37 | △ | 表重复两份（sh2 与 sh8 相同）→去重一份；内容齐 |
| 32 | THANK YOU | P40 | ✅ | 无 |

> **小结**：32 页里 **24 页表格已齐**，真正要做的是：补 5 个缺失页 + 补 slide22 缺失的 4 项创新点 + 4 处字面纠正 + 2 处去重/核对。工程量远小于初判。

## B. 完全缺失页（需新增幻灯片，共 5 页）

| 脚本页 | 主题 | 补这个能讲清什么 | 优先级 |
|---|---|---|---|
| **P6** | 总体系统架构 | FastAPI:8000 业务后端 + CodeMaker Serve:8666 模型后端分层，模型挂了本地规则不塌 | 高 |
| **P19** | 写后自检与自修复 | `_run_verify_repair_loop` Level1 规则修复→Level2 ReAct 修复；**最能体现"不半写入脏数据"的工程严谨** | 高（重点创新） |
| **P20** | 数据安全：备份与回滚 | 批量事务/链式/checkpoint 三种回滚 + 中间态存档；**回应 slide4 问题 5"AI 误写不可逆"** | 高（重点创新） |
| **P38** | 验证与可靠性 | 回归 180 项 + 单测 323+ 零回归，收尾可信度 | 高（收尾必备） |
| **P39** | 总结与展望 | 双引擎一句话总结 + 后续重点诚实标注（Replan/编号账本/聊天降噪/长指令分块现状） | 高（收尾必备） |

> 注：初判的"P4 问题后果""P17 交互修正"经表读取后**不缺**——P4 已并入 slide4，P17 已并入 slide15。

## C. 内容口径纠正（改字，不新增页）

| PPTX | 现文字 | 应改为 | 依据 |
|---|---|---|---|
| 第 10 页标题 | "LocatorAgent **三**级定位" | "LocatorAgent **四**级定位" | 表内已含 L4 RAG/BM25（`CODEMAKER_RAG_MODE=bm25` 默认开），见 P11 代码位置 |
| 第 11 页标题 | "LocatorAgent **三**级定位" | "**rapidfuzz 混合评分**" | 本页讲 rapidfuzz，标题沿用了第 10 页串了 |
| 第 13 页正文 | "依赖**钱买你**新建行的 ID" | "依赖**前面**新建行的 ID" | 错字 |
| 第 19 页首句 | "将列名、如何定位行…共用同一份知识源"（与第 18 页重复） | 换成 P23 一句话："Skill 是经验沉淀层，从人工维护到自动学习分四层" | 首句误抄自业务规则页 |

## D. 第二步（原地扩充 PPTX）施工优先级

1. **新增 5 个缺失页（高价值，32→约 37）**：P6 系统架构、P19 写后自修复、P20 备份回滚、P38 验证可靠性、P39 总结展望。
2. **重点补 slide 22 缺失创新点**：在现有 ①②③④ 四行表后补 ⑤⑥⑦⑧（`all_ok` 口径修复 / `claim_id` 编号账本跨分支查重 / Replan-on-failure / Step2 三处硬规则纠偏）。
3. **纠正 4 处字面**：slide10、slide11 标题，slide13 错字，slide19 误抄首句。
4. **去重 / 核对**：slide31 重复表去一份；slide29 sh2 因子表与 slide30 核对是否误置。
5. **章封副题**：slide6、slide23 补副题（空白副题框已存在，直接填即可）。
6. **可选 polish**：slide17 补 `all_ok` 口径修复一句。

---

# 全文页面地图（共 40 页）

```
封面(1) → 目录(2)
第一章 业务背景与系统概览(3-6)
  业务背景：复杂性(3) → 问题与后果(4) → 任务目标(5) → 总体系统架构(6)
第二章 Excel-Agent：4-Step 配表引擎(7-27)
  章节封面(7) → 总体定位(8) → Prompt工程(9)
  Step1 解析(10-15c)：
    Step1解析总览(10) → 候选表定位总览(11) → 规则5级召回(11-1) → 列名信号补候选(11-2) → LLM复核收窄(11-3) → FK关系扩展(11-4) → 语义召回BM25(11-5) → LLM分段与预判⑨(11-6) → rapidfuzz混合评分(12)
    → DecomposeAgent拆解(13) → 拆解增强：灌值守卫+硬预算+⑬(13b)
    → produces/consumes依赖契约(14) → 契约兜底⑪(14b)
    → Step1质量门禁(15) → 质量增强⑩⑭(15b) → Step1.5契约校验层(15c)
  Step2 校验(16-17)：三层校验(16) → 交互修正(17)
  Step3 执行(18-20)：确定性执行(18) → 写后自检与自修复(19) → 数据安全：备份与回滚(20)
  Step4 汇总(21-22)：汇总与自学习(21) → 业务规则库(22)
  Skill 系统(23-25)：四层结构(23) → 生成审核机制(24) → 效果数据(25)
  本轮优化①②③(26) → 本轮优化④⑤⑥⑦⑧(27)
第三章 Merge：三方合并引擎(28-37)
  章节封面(28) → 三方合并模型(29) → 总体流程与模拟环境(30)
  → merge-base定位与优化链(31) → Compare四层比较(32) → ID重映射(33)
  → 公式缓存保护(34) → fast-apply写回(35) → AI辅助冲突处理(36) → 并行性能(37)
第四章 验证与总结(38-40)
  验证与可靠性(38) → 总结与展望(39) → 结束页(40)
```

---

# 第一章 · 业务背景与系统概览

## 第 1 页｜封面

- **标题**：AI 配表助手
- **副标题**：自然语言驱动配表 + 三方合并 Merge
- **信息**：吴植贤 ｜ 2026 年 9 月 4 日

【配图占位：项目 Logo / 网易互娱模板背景图】

---

## 第 2 页｜目录

| # | 章节 | 一句话 |
|---|---|---|
| 01 | 业务背景与系统概览 | 为什么做、系统长什么样 |
| 02 | Excel-Agent：4-Step 配表引擎 | 怎么用自然语言改表 |
| 03 | Merge：三方合并引擎 | 怎么合并多分支的表 |
| 04 | 验证与总结 | 靠不靠谱、下一步做什么 |

---

## 第 3 页｜业务背景：真实配表有多复杂

**一句话**：复杂 Excel 配表不是"改一个单元格"，而是跨表、跨目录、跨版本的一致性工程。

【配图占位：多张 Excel 表之间用箭头互相引用的关系图】

**例子**：策划要开一个活动"周年庆"，同时在奖励表里给这个活动配一条奖励——这一步就跨了 2 张表，还要保证奖励表引用的活动外键（**FK**，指向另一张表主键的列）对得上；如果同时有另一个策划在别的分支改同一张奖励表，两边改动还要能正确合并，不能互相覆盖。

**三维复杂性**：

| 维度 | 表现 |
|---|---|
| 规模复杂 | 60+ 张表，字段多、跨表外键密集（活动/奖励/技能/NPC/刷新点常跨多表） |
| 表达复杂 | 策划用自然语言描述需求，通常不会精确说出表名、列名、枚举值 |
| 协作复杂 | SVN 目录分支与外链并存，多人并行改表，人工合并容易漏行、丢公式 |

---

## 第 4 页｜业务背景：问题与后果

【配图占位：一张"问题→后果"的红色警示条形图】

| # | 问题 | 后果 |
|---|---|---|
| 1 | Merge 漏行丢数据 | 数据静默消失，事后难发现 |
| 2 | 跨分支编号冲突（多人新建同一编号） | 后提交覆盖先提交 → 线上 bug |
| 3 | 公式缓存丢失 | 表格里输出变成 0 或空 |
| 4 | 改表慢、易错 | 效率低、易漏外键 |
| 5 | AI 误写不可逆 | 写坏数据找不回 |

**例子**：两个策划分别在 dev1、dev2 分支给"物品表"新增一行，都用了下一个可用编号 `2001`——如果没有查重机制，谁后提交谁的数据就把对方覆盖掉，且没有任何报错提示（对应第 33 页 ID 重映射、第 27 页编号账本）。

> 问题 5"AI 误写不可逆"对应的解决方案在第 20 页"数据安全：备份与回滚"。

---

## 第 5 页｜任务目标

**一句话**：Excel-Agent 把自然语言变成结构化意图，一次说清多表联动；Merge 把 SVN 目录/分支的三方合并做到位，保护公式、ID 引用、冲突可解释。

【配图占位：左右两栏图标——左边"自然语言→表格"，右边"多分支→合并"】

| 引擎 | 目标 | 关键约束 |
|---|---|---|
| Excel-Agent | 输入自然语言 → 结构化意图（**intent**，拆解出的最小操作单元）+ 多表配表 | 存在校验、可执行、可跟踪的结构化意图 |
| Merge | 目标目录/分支模式实现 Excel 三方合并 | 保护公式、ID 引用、冲突可解释性 |

---

## 第 6 页｜总体系统架构

**一句话**：前端只对接一个业务后端，业务后端把"重逻辑判断"交给本地代码，把"语言理解"交给专门的模型服务，两者分工明确。

【配图占位：三层架构图——前端(Vue) → FastAPI 业务后端:8000 → CodeMaker Serve 模型后端:8666】

**例子**：用户在前端点"发送"，请求先到 **FastAPI**（一个 Python 后端 Web 框架）搭的业务后端（端口 8000）；业务后端里的 `AgentService` 负责调度 Excel-Agent 4-Step，`Diff/Merge Engine` 负责三方合并的比较和写回，两者都是本地代码（用 `openpyxl` 等库直接读写 Excel），只有真正需要"理解自然语言"的地方，才通过 HTTP 请求转发给专门的模型服务 **CodeMaker Serve**（端口 8666，创建会话/发消息/列模型等接口）。

```
前端 (Vue) ──HTTP──▶ FastAPI 业务后端 :8000
                        ├─ AgentService（Excel-Agent 4-Step 调度）
                        │     └─ 语言理解相关请求 ──HTTP──▶ CodeMaker Serve :8666（模型后端）
                        └─ Diff/Merge Engine（本地 openpyxl 操作，比较+写回）
```

> 这个分层的好处：模型服务挂了或变慢，本地的规则校验、确定性执行、Diff 比较都不受影响，只是"理解"这一步会降级或报错，不会把整个系统拖死。

---

# 第二章 · Excel-Agent：4-Step 配表引擎

## 第 7 页｜章节封面

- **大字**：二、Excel-Agent：4-Step 配表引擎
- **副题**：模型负责理解，规则负责约束，执行层保持确定性

【配图占位：章节分隔页统一底图】

---

## 第 8 页｜总体定位

**一句话**：用户不需要先知道表名、列名，Agent 负责"理解 → 定位 → 拆解 → 校验 → 执行 → 汇总"全流程，由**自研 Step Pipeline**（`server/agent/excel/core/pipeline/orchestrator.py`，按 `STEP_ORDER` 纯 Python 顺序串行的 Step 编排器，不是第三方 DAG 框架）编排成 **4 个主 Step + 1 个中间契约校验 Step1.5**。**LangGraph**（`server/agent/graph.py:63`）只做入口级 `classify → {qa|crud}` 路由，4-Step 本身不在 LangGraph 图里。

【配图占位：4 个主 Step（+ Step1.5）的流水线示意图，Step1→Step1.5→Step2→Step3→Step4，标注"按需调用模型 / 确定性写盘"】

**例子**：用户输入"新增灵兽'白虎'，品质神兽、成长率 1.5，再给它配一条进化路线"，Agent 自动识别涉及"灵兽表"和"进化表"两张表（进化表要引用白虎刚生成的 ID），不需要用户报出真实表名 `pet.xlsx`/`pet_evolve.xlsx`。

| Step | 职责 |
|---|---|
| Step1 解析 | 整句 LLM 路由分类 → LLM 分段（+指代消解）→ 候选表定位（规则召回 + LLM 复核收窄）→ schema 注入 → 每段模型拆解 → produces 推断 → 段级覆盖对账 |
| Step1.5 契约校验 | 编译 produces/consumes 引用图 + 语义计划 + 计划完整性审计（详见 P15c），脏计划在此拦截、不进 Step2 |
| Step2 校验 | 按字段/类型/依赖等业务规则做校验（含 Step1 前置拦截，详见 P16）|
| Step3 执行 | 按依赖顺序确定性写盘、ID 分配、引用回填 |
| Step4 汇总 | 归纳变更结果、失败原因、沉淀自学习经验 |

> 核心原则：Step1 产出的结果越准，后面 Step2/3/4 越省事——这也是后续大部分优化都扎堆在 Step1 的原因。

---

## 第 9 页｜Prompt 工程

**一句话**：不是把整份数据库都塞给模型，而是"只给这次任务需要的那一小块"，靠角色设定、真实表结构、输出格式、反例约束等组件把幻觉压到最低。

【配图占位：对比图——左边"76MB 全量表索引"堆成一座山，右边"510 字符聚焦表结构"一张小卡片】

**例子**：以前把 60+ 张表的全量索引（76.11MB）都发给模型，模型经常"编"出不存在的列名——这种模型编造出实际不存在的内容的现象叫**幻觉（hallucination）**；现在只定位到相关的 1-2 张表，注入约 510 字符（约 159 个 **token**，即模型处理文本的计量单位，大致等于半个到一个汉字）的真实**表结构（schema）**，模型只能从这些真实列名里选。

| 组件 | 作用 |
|---|---|
| 角色设定 | 限定"Excel 配表拆解专家"边界，不接无关任务 |
| 真实 schema 注入 | 表名/sheet/列名/类型/外键，防止编造字段 |
| 输出格式 JSON | 保证结果能被程序直接解析执行 |
| 反例约束 | 明确"不许输出不存在的列"，降低污染 |
| **few-shot** 示例（在提示词里给模型看几个正确答案示例） | 给跨表引用范例，提升结构稳定性 |
| 截断策略 | 只注入定位到的 schema，省 token、防超时 |

| 指标 | 优化前 | 优化后 |
|---|---|---|
| Prompt 上下文 | 全量表索引 76.11 MB（实为 `_table_index.json` 体积） | 单任务聚焦 schema，约 510 字符 / 159 token（**模拟估算**，非实测 bench；来源规划文档，写 PPT 当口径讲需降级标注）|
| 输出格式 | 自由文本，解析不稳定 | 固定 JSON，可执行可校验 |

---

## 第 10 页｜Step1 解析：分段 → 定位 → 拆解

**一句话**：把一句"人话"变成可执行的配表计划——**LLM 管判断（分段、选表、拆解），规则管兜底（召回、对账、审计）**。

【配图占位：一句话 → LLM 分段(+指代消解) → 每段独立 定位→拆解 → 段级覆盖对账 → Step1.5 契约校验 的流水线图】

**例子**："新增活动嘉年华，绑定奖励掉落金币100" → 拆成 ①活动表新增一行；②奖励表新增一条并挂在刚建的活动上（详见 P14）。

| # | 步骤 | 谁来做 |
|---|---|---|
| ① | 预判指令类型（新增/修改/删除/查询）与复杂程度 | LLM |
| ② | 切分任务 + 代词消解（"它"→实体名）+ 分散属性归拢 | LLM（切不动整句处理，不硬切） |
| ③ | 找表：规则召回候选 → LLM 复核"哪些真相关" | 规则 + LLM |
| ④ | 注入真实表结构（列名/类型/外键/填表规则） | 规则 |
| ⑤ | 逐段拆解成结构化指令（哪个表/加改删/填哪些字段） | LLM |
| ⑥ | 按外键+主键规则串"我用你刚生成的 ID"依赖链 | 规则（0 LLM） |
| ⑦ | 段级对账：每段≥1 条，产空重拆一次，仍空如实上报 | 规则 |
| ⑧ | 契约体检：引用成环？每条新增有主键？脏计划不进 Step2 | 规则 + 审计（P15c） |

> 为什么：分段/选表曾是纯规则（正则切句+关键词），策划口语多变必漏；现改为"LLM 判语义、规则管便宜稳定的活"。
> 代码位置（供答辩查证）：`server/agent/excel/parse_agent.py`、`server/agent/excel/core/pipeline/step1_parse_subagent.py`。今日重写⑨-⑭ 独立成页（P11-4/P11-6/P13b/P14b/P15b），均标"未跑 bench"。

| 指标 | before | after | 说明 |
|---|---|---|---|
| 定位功能 | 0.6667 | 0.8056 | **+20.8%** |
| 覆盖度 | 0.6111 | 0.7500 | **+22.7%** |
| 平均耗时 | 82,236ms | 98,637ms | 换更严的校验，耗时略增 |
| 响应 ok 率 | 0.8333 | 0.3333 | 主动拒错（宁可拒绝也不乱写） |

---

## 第 11 页｜候选表定位总览（规则召回 → LLM 复核收窄）

**一句话**：找表分两步——**规则先召回**（便宜可靠），**LLM 再收窄**（剔噪声表），用户全名/别名/错别字/只说列名都能找到对的表。五个通道分页详解：P11-1 ~ P11-5，分段见 P11-6。

【配图占位：候选表定位流程图——规则召回（表名/别名/列名）→ 列名信号补候选 → LLM 复核收窄 → FK 扩展 → 输出候选池】

**例子**："灵兽表"→规则命中；"灵兽表格"→模糊匹配；"给朱雀加技能"没提表名→FK 扩展到技能表；"名称/描述"通用列误带进 6 张表→LLM 复核剔掉；全不沾边→BM25 语义召回兜底。

| 通道 | 一句话 | 详见 |
|---|---|---|
| ① 规则 5 级召回 | 文件名→别名→模糊→sheet 名→列名语义，逐级命中，0 LLM | P11-1 |
| ② 列名信号补候选 | 指令里提取列名→反查哪张表→补进候选 | P11-2 |
| ③ LLM 复核收窄 | 候选>1 张判"真正涉及哪些表"，剔噪声表 | P11-3 |
| ④ FK 关系扩展 | 显式外键一跳 + 隐式外键推断 | P11-4 |
| ⑤ 语义召回 | BM25 最后兜底 | P11-5 |

---

## 第 11-1 页｜规则 5 级召回（0 LLM 的候选底座）

**一句话**：不调模型的规则先把候选表"捞回来"——按最省成本→最兜底逐级尝试，单表/直接命中场景在这一步就定了。

【配图占位：5 级漏斗图，标注每级命中样例】

| 级 | 命中方式 | 例子 |
|---|---|---|
| 1 exact_file | 表文件名精确命中 | `pet.xlsx` |
| 2 alias | 表别名/中文表头命中 | "灵兽表"→`pet` |
| 3 fuzzy_file | 文件名模糊匹配（rapidfuzz，见 P12） | "灵兽表格" |
| 4 sheet_name | 工作簿 sheet 名命中 | "Pet" |
| 5 column_semantic | 列名语义命中 | "成长率"→pet 列 |

**例子**：说"灵兽表"→ 第 2 级别名命中 `pet`，零 LLM 开销；说"灵兽表格"→ 第 3 级模糊兜住。

> 代码位置（供答辩查证）：`server/agent/excel/locator/table_locator.py`（5 级 + level6 余弦语义）。

---

## 第 11-2 页｜列名信号补候选（用户直接说出列名）

**一句话**：用户没提表名、只提了列名（"把法宝描述改成…"）时，靠"列名反查"把对应表补进候选池——修过"法宝表没进候选、正确操作被误删"的漏表问题。

【配图占位：指令"把法宝描述改…"→ 提取列名"法宝描述"→ 反查 fabao 表 → 补进候选】

| 规则 | 做法 |
|---|---|
| 专有列优先 | 命中"法宝描述"这类个别表才有的列才信；"名称/描述/编号"等通用列单独命中不算证据 |
| 歧义列抑制 | 某列同时对应 ≥3 张表（如 `model_id`）→ 跨表共享列，不凭它补表 |
| 强证据必留 | 指令原样出现判别列 → 高置信补进、不受数量上限约束 |
| 数量上限 | 最多补 4 张，按"专有列命中数+得分"排序，防候选池膨胀 |

**例子**："把法宝描述更新一下" → 提取到专有列"法宝描述" → 反查命中 `fabao` → 补进候选，拆解时能选到。

> 代码位置（供答辩查证）：`server/agent/excel/locator/column_extractor.py` + `locator_agent.py`（补候选逻辑）。

---

## 第 11-3 页｜LLM 复核收窄（"哪张表真正相关"的主判）

**一句话**：规则只负责召回，可能带回噪声表——候选 >1 张时让 LLM 判"这句指令真正涉及哪些表"，这是今日重写后**"LLM 当选表主判、规则只管召回"**的核心变化。

【配图占位：候选池（含 6 张噪声表）→ LLM 判定 → 收窄到 1 张真表】

| 情形 | 处理 |
|---|---|
| 触发条件 | 候选>1 张即触发（唯一入口，替代原来只在歧义/超上限/复杂时才复核） |
| 简单输入 | LLM 判定直接生效，剔掉不在"真正相关"清单里的表 |
| 复杂输入 | 不删表，只保护真表不被候选上限挤掉（过早收窄会误删外键链上表） |
| 兜底 | LLM 失败/关闭 → 退回置信度/规则硬判，不回归 |

**例子**："新增灵兽白虎，品质神兽" → 规则召回 `pet`，列名信号带进 6 张含"名称/描述"的表 → LLM 复核判"只有 pet 真相关" → 候选收窄到 1 张，拆解又快又准。

> 代码位置（供答辩查证）：`server/agent/excel/subagent/locator_agent.py`（`_llm_judge_candidate_relevance`）。

---

## 第 11-4 页｜FK 关系扩展（命中主表自动带上关联表）

**一句话**：命中主表后，把它的"外键关联表"也带进候选池——解决"只提主表、不提子表"的跨表需求。

【配图占位：pet 命中 → FK 一跳扩展 → 技能相关表进候选】

| 方式 | 做法 |
|---|---|
| 显式 FK 一跳 | 按表结构里声明/推导的外键，从命中表扩展一跳 |
| 隐式 FK 推断（今日重写⑫） | 按列名/数据模式推断声明之外的外键，用推断边再扩展（未跑 bench） |

**例子**："给朱雀加技能"没提技能表 → 命中 `pet` → 显式+推断 FK 扩展 → 技能相关表进候选 → 拆解时"技能"落在正确表上。

> 代码位置（供答辩查证）：`server/agent/excel/subagent/locator_agent.py`（`_infer_fk_edges`/`_expand_by_inferred_fk`）。现状：⑫ 已落地，未跑 bench。

---

## 第 11-5 页｜语义召回（BM25 兜底）

**一句话**：规则、列名、外键全都不中时，用 BM25 召回语义近邻，保证"怎么问都有候选"。

【配图占位："给萌宠换个造型" → BM25 词频相关性 → 召回 pet】

| 项 | 值 |
|---|---|
| 算法 | Okapi BM25（词频统计相关性，非大模型） |
| 参数 | `k1=1.5, b=0.75` |
| 分词 | jieba 中文分词 |
| 开关 | `CODEMAKER_RAG_MODE=bm25`（默认开） |

**例子**："给萌宠换个造型" → 规则/列名都没命中 → BM25 按词频相关性召回 `pet`（萌宠/宠物词族）。

> 代码位置（供答辩查证）：`server/agent/excel/rag_searcher.py`。是真实实现，不是设计稿。

---

## 第 11-6 页｜LLM 分段与预判（⑨：路由分类 + 切任务）

**一句话**：动手拆解前，先让 LLM 把整句"读一遍"——**预判**这是增/改/删/查哪种操作、是简单还是复杂跨表任务，再**把一句话切成一串独立任务**并处理好代词和散落的属性；这两件事以前靠正则猜、靠正则切，现在交 LLM 主判，是今日系统性重写的核心（⑨，未跑 bench）。

【配图占位：整句 → LLM 预判(action/是否复杂) → LLM 切段(+代词消解) → 每段独立 定位→拆解】

**为什么从规则换成 LLM**：策划口语千变万化。正则靠"分隔符+动作词白名单"切句，同义词漏判（"放/摆/搁"）、代词指代瞎猜、多段话被切碎成残句——复杂输入几乎必漏。LLM 读整句做判断，比正则准得多；而且**切不动就整句处理，绝不硬切**，宁可让模型多读点，也不切碎。

| 环节 | 做法 | 说明 |
|---|---|---|
| ⑨-a LLM 预判 | LLM 读整句，回答两个问题：① 什么操作（新增/修改/删除/查询）？② 简单一句还是复杂跨表任务？ | 结果传给后面的定位环节，帮它判断"要用力到什么程度"；预判失败退回规则判定，不影响后续 |
| ⑨-b LLM 分段 | 一次 LLM 调用同时完成三件事：① 按动作/实体切出有序的任务段；② 代词消解（"它""这个"→明确实体名）；③ 远距离事实回填（同一实体的属性分散在句尾，归拢回它所属的那段） | 只切文本段、不输出表名/字段——避免"硬路由"误导后面的定位 |

**例子**（同一句话，规则 vs LLM 的差别）：

```
原句：新增活动，然后给它绑一个奖励，奖励给金币100
规则切分：× 切不出——"给它"的"它"指谁？"奖励给金币100"没有动作词，切不动
LLM 切分：✓ 切成 2 段并消解指代
  段1：新增活动（动作=新增，实体=活动）
  段2：新增奖励，绑定刚建的活动，奖励金币100（动作=新增，实体=奖励，指代"它"→活动，属性"金币100"归拢到本段）
```

两段随后各自独立走"定位→拆解"，段2 拆解时会带上"消费刚建活动的 ID"（见 P14 依赖契约）。

> 代码位置（供答辩查证）：`server/agent/excel/subagent/locator_agent.py`（`_llm_classify_route`）、`server/agent/excel/parse_agent.py`（`_ai_plan_segments`/`_parse_whole`）。现状：2026-09-03 落地，已接入主流程·未跑 bench。

---

## 第 12 页｜rapidfuzz 混合评分

**一句话**：模糊匹配不是简单编辑距离，而是多种相似度加权组合，兼顾"整体像不像"和"局部有没有命中"。注意此处公式用于**列名匹配**（`locator/column_matcher.py:218`）；表定位的 L2 模糊走另一套 `FuzzyMatcher`（子串 0.5 + 编辑距离 0.35 + 字符重叠 0.15，`locator/fuzzy_matcher.py:126-147`）。

【配图占位：两个字符串对比，标出三种打分方式各自的判断角度——此为列名匹配场景】

**例子**："灵兽表" vs "灵兽资料表"：整体编辑距离（把一个字符串改成另一个字符串最少要改几个字符）一般，但词组重叠高、"灵兽"这个短查询完全被包含，混合评分能正确判断两者应该匹配。用的是 **rapidfuzz**（一个用 C++ 实现的高性能模糊字符串匹配库，比纯 Python 实现快十倍以上）：

```
rapidfuzz_score = WRatio × 0.5 + token_set_ratio × 0.3 + partial_ratio × 0.2
```

| 指标 | 作用 |
|---|---|
| `WRatio` | 综合编辑距离、长度差、部分匹配，适合通用相似度 |
| `token_set_ratio` | 忽略词序和重复词 |
| `partial_ratio` | 长字段里包含短查询时仍能命中 |

| 指标 | 纯 Python 实现 | rapidfuzz（C++ 算子） |
|---|---|---|
| 编辑距离计算 | 2781.54ms | **192.16ms** |
| 单次匹配平均耗时 | 0.0093ms | **0.0006ms** |

> 下表性能数字为**模拟压测**（来源 `bench/ppt_rapidfuzz_metrics.md`），非端到端 bench；公式与三指标仅作用于**列名匹配**。

---

## 第 13 页｜DecomposeAgent：schema 注入拆解

**一句话**：把候选表的真实结构（列名、类型、外键、填表规则）喂给模型，让它只能从真实列名里选字段，杜绝"编"列名。

【配图占位：输入(自然语言+真实schema) → DecomposeAgent → 输出(结构化 JSON) 的黑箱图】

**例子**：

```
输入：Ability 表结构（列名/类型/外键/填表规则）+ 用户描述
输出：{action: add, table: Ability, fields: {id: 100, name: "TEST"}, produces: [<aid>]}
```

- **schema 注入**：模型只能从真实列名中选，从源头堵住幻觉列名。
- **few-shot 防伪造**：示例里给出正确的**占位符（placeholder，真实 ID 生成前用的临时标记符号，如 `<aid>`）**写法，明确禁止伪造占位符（如把不存在的 `<resolved_form_xxx>` 编出来），超时自动降级为逐表重拆。
- **规则安全网**：模型产生的 intent 数量过少或格式畸形时，回退模板兜底。

| 指标 | before | after |
|---|---|---|
| 字段精准率 | 0.5185 | **0.6917** |
| 严格通过率 | 0.1667 | **0.3333** |

---

## 第 13b 页｜Step1 拆解增强：防脏值 + 控成本 + 稳性能（⑬）

**一句话**：拆解环节三道加固——**别让口语脏值落盘、别让重试烧光 token、别让一次超时拖垮任务链**。

【配图占位：输入 → 防脏值闸门 → 拆解（受调用预算约束）→ 输出；标注"会话复用 / 超时保护"】

| 加固 | 做法 | 例子 |
|---|---|---|
| 防脏值闸门 | 按列真实类型清洗：数字列填中文→清空；枚举列中文标签→转数字码；布尔列归一 | "攻击力中等"→ 该列是数字列 → 清掉，不留脏值 |
| 调用预算 | 单次拆解 LLM 调用数封顶（默认 5），产空重试但到预算停手如实上报 | 拆不出最多重试 5 次，不无限烧钱 |
| 表结构只拼一次 | 同批多段只拼一次真实表结构、复用结果 | 拆 5 段不再拼 5 遍表结构 |
| 会话复用 | 主流程复用同一模型会话，工作线程各用各的 | 不每段新建会话，省网络往返 |
| 防连环超时 | 默认不熔断——超时也要每段完整跑完，仅显式配置才开熔断 | 2 表任务链不被一次超时降级成残缺 |

> 代码位置（供答辩查证）：`server/agent/excel/parse_agent.py`（防脏值）、`server/agent/excel/subagent/decompose_agent.py`（预算/复用/超时）。现状：均已落地，未跑 bench。

---

## 第 14 页｜produces/consumes 依赖契约

**一句话**：新增行要跨表引用别处新生成的 ID 时，不能靠模型"记住"，要靠显式的依赖契约来确定顺序和回填。

【配图占位：三步链式示意图——活动(产出ID) → 奖励(消费ID+产出新ID) → 邮件(消费新ID)】

**例子**：新增活动 `activity_id=3001` → 新增奖励 `reward_id=30010` 并绑定 `activity_id=3001` → 新增邮件 `mail_id=5001` 附件绑定 `reward_id=30010`。三步谁先谁后、ID 怎么传递，全靠 `produces`（本意图产出的占位符）和 `consumes`（本意图消费的占位符）记录，Step3 执行时按此顺序回填真实 ID。

| 指标（quest_npc 样例） | before | after |
|---|---|---|
| 引用一致 | 0.62 | **1.0** |
| 耗时 | 134s | **107s** |

---

## 第 14b 页｜Step1 契约兜底：占位符回填强化 + 自引用循环消解（⑪）

**一句话**：P14 管"正常声明"的依赖链，本页管"**声明不全、链断了、绕成环**"的兜底——靠真实数据和结构把断链补上、把环解开。

【配图占位：长链断裂示意 → 补救：指令已写 ID 直接接上 / 缺外键补上 / 同文件跨 sheet 回填 / 环解开】

**例子**："建活动→活动产 ID→奖励引用 ID→奖励产新 ID→邮件再引用"长链上任何一环没声明引用，后面全断——以前报错重来，现在自动补救：

| 断链情况 | 补救 | 例子 |
|---|---|---|
| 指令里其实写了 ID 只是没声明引用 | 用写明的编号直接接上 | "新增活动3001，再建奖励绑定活动3001"→ 直接接上 |
| 引用别的表的字段忘了填 | 按外键补上缺失字段 | 奖励表缺"活动ID"列 → 自动补 |
| 同文件跨 sheet 引用断了 | 同文件范围内自动回填 | 表A产ID，同文件表B引用 → 自动接上 |
| 新增没保证产出主键 | 强制补齐主键 | 新增奖励行没填奖励ID → 补上 |
| 同表重复相同编号 | 去重保留一份 | 两条都写"编号=1001" → 去重 |
| 互相引用成环（A→B→A） | 解开环，必要时清自引用字段 | 选项1→选项2、选项2→选项1 → 解开 |

> 契约（P14）+ 兜底（本页）合起来：长链任务不"一步断、步步断"。
> 代码位置（供答辩查证）：`server/agent/excel/parse_agent.py`（回填强化）、`server/agent/excel/core/pipeline/step1_parse_subagent.py`（消环）。现状：2026-09-03 落地，未跑 bench。

---

## 第 15 页｜Step1 质量门禁

**一句话**：模型拆解完不算完，还要用真实 schema 再"编译"检查一次，把明显不合理的产物过滤/修正。

【配图占位：一个"体检"图标，Step1 输出经过质量门禁筛查后再进入 Step2】

**例子**：模型输出的字段用了"物功"，但真实列名是"物理攻击"——质量门禁会把它修正映射到真实列，而不是让它作为一个假字段直接写盘。

| 检查内容 | 修复方式 |
|---|---|
| 空壳新增（拆出来了但没填任何字段）/ 孤立新增 / 同一 sheet 里稀疏重复 | 直接删除 |
| 字段不在表结构里 / sheet·表名混用 / 大小写差异 | 修正到真实列 |
| 产出的编号重复 / 占位符格式不合法 / 引用了没人生产的编号 | 删占位符或绑回真实来源 |
| 自己引用自己 / 互相引用绕成环（含对话选项自引用循环） | 解开循环，必要时清掉自引用字段 |
| 把外键当主键用 / 外键关系不闭环 | 拦截或补回来源 |
| 某段任务完全没拆出来 / 拆了一部分 | 标记并纳入整体成功判定（详见 P21） |

> 代码位置：`server/agent/excel/core/pipeline/step1_parse_subagent.py`（`_step1_quality_report`约 701-833、`_build_step1_audit`约 836）。检查项是真实 issue 类型枚举，非设计稿。

| 优化项 | before | after |
|---|---|---|
| type_mismatch（类型不匹配） | 2 | **1** |
| 失败案例归纳出的规律类别数 | 0 | **4 类** |

> 质量门禁的"今日增强"独立成页：⑩ 引用图+语义计划+审计（P15b）、⑭ ReAct 表级回读自检（P15b）、Step1.5 契约校验层（P15c）。

---

## 第 15b 页｜Step1 质量增强：引用图审计（⑩）+ ReAct 表级回读自检（⑭）

**一句话**：两条系统级质量增强——**⑩ 把拆解结果当"合同"整体审一遍**（引用成环？主键齐？），**⑭ 拆完后让模型对照外键图"回读复查"**（漏表？多表？）。

【配图占位：左"拆解结果→编译引用图→审计→拦截"；右"产出表清单+外键图→LLM 回读→漏表补拆/多余移除"】

| 增强 | 做法 | 例子 | 与旧对比 |
|---|---|---|---|
| ⑩ 引用图审计（未跑 bench） | 把"所有指令+引用关系"编译成引用图整体检查：引用完整？依赖成环？每条新增有主键？ | "新增活动+新增奖励（引用活动ID）"，奖励忘了声明引用 → 审出"悬空引用"直接拦下 | 以前拖到写盘失败才发现，现在进 Step2 前就点出"根因在 Step1" |
| ⑭ 回读自检（未跑 bench） | 拆完后把"产出表清单+外键图"丢回 LLM 复查：候选池有"该拆没拆"的表？拆出的有多余的表？→ 漏补拆、多余移除 | 要 3 个怪物刷新点只拆了 2 个 → 对照外键图发现漏拆，触发补拆 | P19 是写盘后修复（事后），⑭ 是写盘前复查（事前） |

> 代码位置（供答辩查证）：`server/agent/excel/core/pipeline/step1_parse_subagent.py`（⑩）、`server/agent/excel/subagent/decompose_agent.py`/`parse_agent.py`（⑭）。现状：均已落地，未跑 bench（⑭ 多一次模型调用，线上走"快速修复优先"）。

---

## 第 15c 页｜Step1.5 契约校验层（引用图 / 语义计划 / 计划完整性审计）

**一句话**：Step1 拆解完的指令不能直接写盘——先过**契约体检**：把指令间的引用关系当契约编译检查，脏计划在进 Step2 前拦下。

【配图占位：Step1 产出 → 契约编译（引用关系图/语义计划/完整性审计）→ 通过进 Step2 / 不通过回 Step1 修复】

**例子**："新增活动并绑定一个奖励" → 检查：① 奖励引用的编号是否真有人产出？② 依赖是否成环（A 等 B、B 等 A）？③ 每条新增是否产出主键？任一不满足就拦下提示"先修 Step1 引用关系"，不带断链进写盘。

| 检查 | 内容 | 失败处理 |
|---|---|---|
| 引用关系图 | "产出→消费"是否完整、有无悬空/成环 | 悬空/成环 → 拦截 |
| 语义计划 | 指令能否编译成完整计划结构 | 编译不过 → 硬性拦截 |
| 计划完整性 | 每条新增是否产出主键、外键是否闭环 | 不完整 → 硬性拦截 |
| 与 Step2 联动 | 检查结果在 Step2 校验前统一生效 | 脏计划不进校验/写盘 |

> 定位：与 P15/P15b"同源拦截、两道关卡"——P15 修复补齐、Step1.5 转硬 error、P16 再兜漏网，层层收紧。
> 代码位置（供答辩查证）：`server/agent/excel/core/pipeline/contracts.py:36,41-43`（`STEP1_5_CONTRACT`）、`step1_parse_subagent.py`（审计）、`step2_validate_subagent.py`（前置拦截）。现状：2026-09-03 接入主流程，未跑 bench。

---

## 第 16 页｜Step2 三层校验

**一句话**：写盘前设三道关卡，从"纯代码检查"到"模型语义判断"再到"人工确认"，层层收紧，能在便宜的层解决就不往贵的层传。

【配图占位：三层关卡示意图，L0(代码闸)→L1(模型裁决)→L2(人工确认)】

**例子**：用户输入的字段里枚举值写了"史诗"，但该列合法枚举是"传说/稀有/普通"——L0 规则闸直接拦截（零成本）；如果字段值语义上"看起来合理但覆盖不全"，交给 L1 让模型判断一次；如果问题需要用户拍板（比如列名歧义），弹出交互卡片走 L2（详见第 17 页）。

| 层级 | 方法 | 处理内容 |
|---|---|---|
| L0 规则闸 | 纯代码 | 类型、主键、外键、占位符等硬校验 |
| L1 AI 裁决 | 1 次模型调用 | 自然语言覆盖度、值合理性判断 |
| L2 人工 gate | 交互卡片 | 用户改字段/值后续跑三层校验 |

> L0 里"主键必填""业务必填豁免"这两条不是铁板一块——第 27 页⑧号创新点讲了它们怎么从"猜错就死拦"变成"猜错能自动/半自动纠正"。
> 另：三道关卡**之前**还有一道**前置拦截**（即 Step1.5 契约校验层，详见 P15c）：把 Step1 的结构性/质量/语义编译/计划完整性问题统一转硬错误，脏计划不进校验与写盘——与 P15c 是"同源拦截、两道关卡"，层层收紧。

---

## 第 17 页｜Step2 交互修正（现状与计划）

**一句话**：校验不通过时不是直接报错了事，而是给用户一张可以直接改的卡片。

【配图占位：交互卡片截图占位——列名批量修正卡片的 UI 截图】

**例子**：Step2 发现"物功"这一列名系统里没有，会弹出卡片提示"你是不是想写'物理攻击'？"，用户确认或改成别的列名，提交后系统把编辑写回原 intent 再重新跑一次三层校验，而不是简单跳过这个错误。

| 阶段 | 能力 |
|---|---|
| 现状 | 已有"列名批量修正卡片"，可改列名/值、删字段、删整条 intent |
| 计划 | 升级为"全字段可编辑表格"，覆盖更多字段类型的修正场景 |

---

## 第 18 页｜Step3：确定性执行

**一句话**：Step3 不再调用模型，只做"看得懂、可解释"的确定性写盘。

【配图占位：拓扑排序执行顺序图——活动→奖励→邮件，按箭头先后写盘】

**例子**：延续第 14 页的例子，Step3 按**拓扑排序**（按"谁必须先于谁"的依赖关系排出一个不矛盾的执行顺序）先建活动、再建奖励（用刚分配的活动 ID）、最后建邮件（用刚分配的奖励 ID），任何一步失败都停在可恢复点，不会半写入脏数据。这里的"确定性"是指同样的输入一定得到同样的输出，不依赖模型的"随机发挥"，方便排查问题。

| 动作 | 技术 |
|---|---|
| 拓扑排序 | 按 produces/consumes 依赖确定执行先后 |
| ID 分配、占位符替换 | 新增行分配主键，替换后续 placeholder |
| 失败结构化归因 | 缺字段、主键冲突、类型错误等分类记录 |
| 可恢复边界 | 失败停于可恢复点，避免半写入污染 |
| 写前悬空占位符拦截 | 上游占位符未解析（`upstream_placeholder_unresolved`，`step3_execute_subagent.py:322-352`）→ 写前拦掉，避免孤儿行 |
| 全子任务失败 | 整批没一条成功 → 升级为 hard error（`step3_execute_subagent.py:635-648`），不当软失败 |
| preflight 预演 | 写盘前先跑预演报告（`step3_execute_subagent.py:233-255`），提前暴露依赖缺失 |

| 指标（复杂级联样例） | before | after |
|---|---|---|
| 运行耗时 | 204s | **112s（-45%）** |

---

## 第 19 页｜写后自检与自修复

**一句话**：单个字段/单行写完不代表就对了，Step3 内部还有一个"写完立刻自检、自检不过就自己修"的小循环，不用等到最后 Step4 汇总才发现问题。

【配图占位：写入→自检→（失败）分类→修复→重写→再自检 的循环示意图】

**例子**：Agent 给"任务表"新增一行并写入了一个 NPC 引用 ID，写完后自检发现这个 ID 在 NPC 表里根本不存在（引用断裂）——系统不会直接报失败，而是先按问题类型（分类）匹配一套修复策略（Level 1：规则修复，比如换成最相似的真实 ID），改完立刻重写、重新自检；如果规则修复解决不了，才升级到 Level 2，用 **ReAct**（Reasoning + Acting，一种让模型交替"推理"和"调用工具"来解决问题的智能体范式）调用模型和技能工具做更灵活的修复。整个循环最多重试若干轮，首次就成功的情况直接跳过，不额外耗时。

| 环节 | 说明 |
|---|---|
| 快路径 | 首次写入即成功且自检通过 → 直接返回，不额外调用模型 |
| Level 1 修复 | 按错误类型匹配规则化修复策略，重写后再自检 |
| Level 2 修复 | 规则修复失败时，升级为 ReAct（模型 + 技能工具）灵活修复 |
| 防重复分配 | 记录本轮已分配/已写的 ID，避免重复轮次撞出同一个错误 ID |

> 代码位置：`server/agent/excel/core/agent.py:_run_verify_repair_loop`（`server/agent/excel/repair/` 目录下的分类器和修复手册）。这一层修的是"单次写入"级别的问题；第 27 页的 Replan-on-failure 修的是"一批任务失败后要不要换个执行计划"，两者不重复。

> **补充：真实实现结构（按代码核对，P19 讲深一点也不超纲）**——本页上面的"Level 1 / Level 2"是简化口径，真实代码是**三层递进 + recipe 快路径**：
> - **repair/ 五模块**：`error_classifier.py`（按 error_signature 分类错误类型）、`repair_playbook.py`（修复手册：分类→选 RepairAction，含"LLM 细分修复产 fix_payload"档）、`cascade_planner.py`（级联影响规划）、`repair_context.py`/`verify_repair_loop.py`（上下文与自检循环）。
> - **三层递进**：① recipe 快路径 → ② 规则 playbook（含 LLM 细分 fix_payload）→ ③ Level 2 ReAct。哪层成功就在哪层返回，互为兜底。
> - **recipe 快路径**（第 24 页 Skill 系统的延伸，文档原 P19 没讲）：repair 前先查 committed `repair_recipes.yaml` 的 active recipe（`lookup_repair_recipe`），命中直接 apply 已验证 fix（`_try_recipe_fast_path`，跳过 playbook/LLM）；repair 成功后捕获（`_capture_repair_recipe`），同一 `error_signature`（type+table+sheet+col）累积到 `RECIPE_PROMOTE_HITS=3` 次就晋升为 committed active（`skill_updater.promote_repair_recipes`），下次同签名走快路径——与列别名转正（`PROMOTE_HITS=3`）同档安全阀。
> - **Level 2 ReAct 实现**（`_run_react_repair`）：手写文本协议的 ReAct 循环——LLM 输出 fenced JSON `tool_call` → 调 `skill_executor` → 结果回灌 prompt → 循环直至产出修正 intent；`skill_tool_call_limit=4`（单次修复最多 4 次 tool 调用），`enable_skill_tools_recovery` 开关控制。
> - **skill tools 自主探查**：`make_skill_tools(self.skill_executor)` 把技能执行器注册成 LangChain `@tool`，让 ReAct 循环里的 LLM 自主决定调用哪些 skill 去探查修复（capability 名 `skill-executor-tools`），不是硬编码"调哪个工具"。
> 现状：上述结构均已落地（2026-09-03 核对 `agent.py:8937/9063/9440/9456`、`repair/` 目录、`skill_updater.py:1368/1379`、`tools.make_skill_tools`）；recipe 快路径/晋升与 Level 2 ReAct **未单独跑 bench**，写进 PPT 时按"已落地机制"讲，不附臆造数字。
> 代码位置补充：`server/agent/excel/core/agent.py`（`_run_react_repair:8937`/`_run_verify_repair_loop:9063`/`_capture_repair_recipe:9440`/`_try_recipe_fast_path:9456`）、`server/agent/excel/repair/`（五模块）、`server/agent/excel/core/skill_updater.py`（`lookup_repair_recipe:1368`/`promote_repair_recipes:1379`）、`server/agent/excel/tools.py`（`make_skill_tools`）、`server/agent/excel/core/llm_context.py`（`skill-executor-tools` capability）。

---

## 第 20 页｜数据安全：备份与回滚

**一句话**：回应第 4 页"AI 误写不可逆"的问题——每次真正写盘前都先备份，出问题能退回去。

【配图占位：写盘前备份→写入→（失败）回滚还原 的示意图】

**例子**：Agent 给"道具表"写入一行时，会先把这个文件备份一份；如果这行写入触发了硬失败（比如后续检查发现主键冲突），系统会用备份文件把这次写入撤销掉，而不是留下一个改了一半的文件。如果这次失败的操作后面还有别的操作依赖它产出的 ID，系统还会顺带把那些"直接依赖它"的后续操作也一起回滚（不会牵连完全无关的操作），如果开启了"批量事务"模式，则整批已提交的操作都会一起回滚，保证要么全部生效、要么全部撤销。

| 回滚模式 | 触发条件 | 回滚范围 |
|---|---|---|
| 批量事务回滚 | 开启批量事务模式 | 回滚整批前面已提交的操作 |
| 链式回滚 | 有依赖关系 | 只回滚直接依赖这次失败结果的操作 |
| 不回滚 | 无依赖、非事务模式 | 只处理当前这一次失败 |

> 另外还有一套可选的"中间态存档"（checkpoint）机制：在 Step1 解析完、Step2 校验完之后可以把当时的结果存一份档，如果任务中途中断，下次能从存档接着跑，不用把前面模型拆解的步骤重新跑一遍（默认关闭，按需开启）。
> 代码位置：`server/agent/excel/core/agent.py`（`_compute_rollback_targets`、`_rollback_write`、`_save_nl_checkpoint`/`_load_nl_checkpoint`）。

---

## 第 21 页｜Step4：结果汇总与自学习闭环

**一句话**：Step4 把这次配表的成功/失败情况汇总成报告，还把失败模式沉淀下来，反哺 Skill 系统。

【配图占位：汇总报告样式截图占位——成功N条/失败N条/预览】

**例子**：一次任务里 Step1 有一段话完全没解析出 intent（漏了），但 Step3 执行的那几个 intent 都成功了——旧逻辑判断"任务是否整体成功"（代码里这个总开关叫 **`all_ok`**）时只看 Step3，会误判为全部成功；新逻辑还会检查 Step1 有没有漏解析、有没有段落没处理完，任何一处有问题都不算 `all_ok`，并在报告里明确指出"有 1 段未解析"，避免"其实没做完却报成功"的假象。

| 功能 | 说明 |
|---|---|
| 汇总结果 | 成功数 + 失败数 + 写入行 + 预览 |
| 失败归因 | 定位失败发生在哪个层 |
| 反模式沉淀（anti_pattern，从失败案例总结出的易踩坑规律） | 从失败案例归纳规律，供 Skill 系统复用 |
| 小回归验证 | 失败样例进入小规模测试集验证 |

> 代码位置：`server/agent/excel/core/pipeline/step4_conclude_subagent.py:144-189`（`all_ok` 最终收敛到 `is_clean_success`），测试见 `test_generic_pk_and_step4_fixes.py` §7。这项 `all_ok` 口径修复**已在代码主链路落地**。
> **补充（按代码核对）**：① **反模式归纳异步化**——Step4 触发反模式归纳走 daemon 线程不阻塞主响应（`step4_conclude_subagent.py:236-281`），慢因由 `StepTrace` 定位卡在哪个 Step（`:301-327`）；② **用户字段修正沉淀**——Step4 收集本次用户在交互卡片改过的字段映射（`_collect_field_corrections`，`:29-79`），喂给 Skill 系统作列别名运行时候选（`skill_updater.ingest_field_corrections:342`），把"人改一次"变"系统学一次"。现状：均已落地，未跑 bench。

---

## 第 22 页｜业务规则库：填表规则与校验规则

**一句话**：把"列名怎么定位、行怎么找、历史踩过的坑"沉淀成按表管理的规则文档，Agent 和 Merge 共用同一份知识源。

【配图占位：目录树截图占位——填表规则目录、校验规则目录】

**例子**：奖励表的规则文档里写明新增行时哪些字段必填、怎么对应活动表外键；这份文档同时被 Step1 的解析提示词和 Step2 的校验流程消费，不用在两处各写一套逻辑。

| 目录 | 内容 | 消费阶段 |
|---|---|---|
| `rules/fill` | 业务规定的填表经验 | 注入 Step1 解析提示词 |
| `rules/validate` | 业务规定的校验规则 | 注入 Step1 schema 列标注（`primary_key` overlay / `value_constraints` 标 `[PK]/[FK→…]/[范围]`，`decompose_agent.py:2550/2558`）+ 合并进 Step2 校验流程 |

---

## 第 23 页｜Skill 系统：四层结构

**一句话**：Skill 系统是"经验沉淀层"，从人工维护到自动学习分四层，层级越高越靠"系统自己攒经验"。

【配图占位：四层金字塔图，人工维护层在底座，自动学习层在顶端】

**例子**：一开始"灵兽表"的别名要人工写进配置文件（第 0 层）；系统扫描 Excel 文件自动发现更多列别名（第 1 层）；运行时用户纠正了几次列名映射，系统自己学会了新的别名（第 2 层）；某个操作反复失败，系统总结出一条"这种情况容易出错"的规律（第 3 层）。

| 层级 | 内容 | 生成方式 |
|---|---|---|
| 第 0 层：人工维护 | sheet 别名、引导动词、列短形式 | 人工 + 自动混合 |
| 第 1 层：自动派生 | 列别名、行定位规则、多 sheet 消歧、类型约束 | 扫描 Excel 文件自动生成 |
| 第 2 层：运行时学习 | 学到的列别名（含命中次数和置信度）、跨表关系权重 | 从实际运行证据晋升写入 |
| 第 3 层：反模式 | 歧义列、类型约束、失败操作、语义模式等失败规律 | 规则频次升级 + 模型归纳 |

---

## 第 24 页｜Skill 系统：生成与审核机制

**一句话**：Skill 不是学到就直接用，要过"命中次数 + 置信度区间 + 小规模回归验证"三道关，避免学坏。

【配图占位：候选→快照→回归→转正/回滚 流程图】

**例子**：某个列别名在 7 天内被命中 3 次以上、且系统对这次猜测的把握在 30%~75% 之间（太自信的不用学，太没把握的不敢学）——才会进入转正候选；转正前还要跑 30 个样本的**小回归**（`mini regression`，在小规模测试集上做新旧对比），确认没把已有能力改坏（提升量，代码里叫 **`lift`**，要求 `lift ≥ 0.05`）才真正生效，不通过就自动回滚并放进**隔离区**（`quarantine`，暂停使用观察一段时间的候选存放区）30 天。

| 判定 | 阈值 |
|---|---|
| 列别名候选转正 | 7 天内命中 ≥3 次，且把握在 30%~75% 之间 |
| 反模式升级为正式生效 | 某列易认错，7 天内出现 ≥3 次 → 自动改成只许精确匹配 |
| 反模式升级为待审核 | 操作失败 30 天内出现 ≥2 次 → 先标"待人工审核"，下次遇到弹窗确认 |
| 模型归纳反模式转正 | 无人工标注兜底，需多命中 3 次才生效 |
| 冲突消解 | 同一说法被两个不同标准列名抢着对应时，交人工判断 |
| 衰减清理 | 60 天无引用自动移除，可信度低于 30% 进入休眠 |
| 转正安全阀 | 候选 → 快照 → 写盘 → 30 样本小回归（`lift≥0.05`）→ 通过转正 / 不通过回滚+隔离 30 天；**生产默认 `CODEMAKER_SKIP_REGRESSION=1` 跳过同步回归**（提速），回归移到 eval/定时 |

> 代码位置：`server/agent/excel/core/skill_updater.py`（`PROMOTE_HITS=3`、`PROMOTE_WINDOW_DAYS=7`、`PROMOTE_MIN_CONF=0.3`、`PROMOTE_MAX_CONF=0.75`、`AP_FAILED_OP_HITS=2/30天`、`DECAY_REMOVE_DAYS=60`、`RELATIVE_LIFT_THRESHOLD=0.05`、`MINI_REGRESSION_SAMPLE=30`）——与本页描述的阈值完全一致。另：`ALIAS_COMMIT_HITS=5`/`ALIAS_COMMIT_MIN_CONF=0.6`（runtime 别名晋升 committed `column_short_form.yaml`）、`AP_FREQUENT_HITS=3`（repair 频次信号→反模式晋升）、`RECIPE_PROMOTE_HITS=3`（repair recipe 晋升，见 P19）同源安全阀设计，均同档"频次+置信+小回归"三道关。

---

## 第 25 页｜Skill 系统：效果数据

【配图占位：柱状图/折线图占位——定位成功率 before/after 对比】

| 数据点 | 指标 | before | after |
|---|---|---|---|
| skill 简版 30 样例 | 定位成功率 | 0.933 | **1.000** |
| skill 简版 30 样例 | 精确命中 | 0 | **1.00** |
| skill 简版 30 样例 | 严格综合 | 0.704 | **0.959** |
| skill 简版 30 样例 | 平均耗时 | 533ms | **321ms（减半）** |
| skill 真写盘 8 样例 | 定位/覆盖/精准 | 0.472/0.417/0.497 | **0.639/0.583/0.590** |
| Step4 反模式沉淀 | 同类失败预警覆盖 | 0/8（0%） | **8/8（100%）** |
| Step4 反模式归纳（真实 e2e） | 8 条失败案例 → 4 类反模式 | — | 命中主键冲突/种子数据缺失/会话定位/类型转换 |

> 证据来源区分：前 5 行 skill 数据见 `PERF_DATA_SUMMARY.md` §1.6/1.7；**最后两行"反模式沉淀 0/8→8/8、8 条→4 类反模式"不在 PERF 性能维度**，来自 Step4 自学习 A/B（`bench/ppt_step4_self_learning_ab.md`）+ 端到 e2e 归纳，写 PPT 当"机制验证"讲、非性能 bench。顶部〇 "脚本全部指标与 PERF 一致" 是指**性能数字**，对这两行不适用。

---

## 第 26 页｜本轮代码优化①②③：让每一环都"说真话"

**一句话**：三个看似独立的小修复，本质都是同一条主线——不误报、不漏检、不写坏合法数据。

【配图占位：三个问题图标横排，each标注 before→after 数字】

**例子**：系统生成的占位符一会儿叫 `newConvId`，一会儿叫 `<new_conv_id>`，本质是同一个东西——没有归一化处理之前，系统会误判成"这个占位符没有被正确生产"（假阳性报错）；用代码里的归一化函数 **`_norm_name`**（把不同写法的同一名称统一转换成同一种格式再比较）处理后，能正确识别出两者是同一个。

| 问题 | 方案 | 数据 |
|---|---|---|
| 命名写法不一致（`newConvId` vs `<new_conv_id>`） | `_norm_name` 归一化后再比较 | 误报率从 5/6 降至 2/6 |
| 占位符藏在嵌套字典/列表结构里漏检 | 递归扫描任意深度的嵌套结构 | 漏检数 7 → 0 |
| 执行顺序错乱导致循环依赖占位符补不齐 | 一轮执行完再补跑一次回填 | 可执行节点 1/5 → 5/5 |

> 代码位置：`server/agent/excel/subagent/validator_agent.py`（`_norm_name`）、`server/agent/excel/core/pipeline/step3_execute_subagent.py`（嵌套占位符扫描+回填）。均为真实函数调用，已在生产代码路径上，不是模拟。

---

## 第 27 页｜本轮代码优化④⑤⑥⑦⑧：已落地但 PPT 里还没讲过的创新点

**一句话**：这五项都是**真实写进代码主链路**的能力，比堆旧数据更能体现工程严谨性。

【配图占位：五个功能图标+简短说明卡片】

- **④ dict 列 lint**（**lint** 原意是代码静态检查，这里指对列值做结构合法性检查）：某些列本身就应该存字典/JSON 格式的值（比如"效果参数"列存 `{atk: 10, def: 5}`），以前的检查逻辑会把这种合法结构误判成"模型编造的假字段"直接清空。
  例子：策划让 Agent 给技能表填"效果参数：攻击+10，防御+5"，Agent 正确生成了 `{"atk": 10, "def": 5}`，旧逻辑因为它是个嵌套结构而误清空，新逻辑能区分"合法 dict 列"和"真的幻觉字段"。
  数据：误清空率 100% → **0%**。

- **⑤ Step4 `all_ok` 口径修复**（详见第 21 页）
  数据：8 种阶段组合测试中，误判场景从 3/8 降到 **0/8**。

- **⑥ 编号账本跨分支查重（`claim_id`）**：`claim_id` 是代码里"认领一个 ID"的函数名——新建一行前先去"账本"里登记这个 ID，如果发现别的分支已经登记过同一个号，会立刻提示冲突并建议下一个空闲号，而不是等提交时才发现两边用了同一个号（回应第 4 页的问题 2）。
  例子：dev1 分支想新建 ID=2001 的道具，`claim_id` 一查发现 dev3 分支已经用过 2001，于是提示"ID 2001 跨分支冲突，建议使用 2002"。
  现状：单目录场景默认生效，跨分支场景需配置环境变量指定要一起查重的分支路径。
  代码位置：调用点 `server/agent/excel/core/agent.py:2438`（`claim = v.claim_id(...)`)，`claim_id` 方法定义在 `server/engine/id_scope.py:451`，跨分支额外目录由 `CODEMAKER_ID_SCOPE_BRANCHES=path1;path2` 传入。

- **⑦ Replan-on-failure（失败后自动重规划，环境变量控制的开关名）**：以前执行失败就直接上报"失败了"；现在会把这次失败的原因和还没执行的任务打包，再请求一次模型帮忙"重新规划一下怎么补救"，最多重试 2 轮，避免无限循环。
  例子：Step3 执行到"新增奖励"这一步失败（因为它引用的活动行还没建出来），系统会把这个失败信息和剩下的任务交给模型，模型给出新计划"先补建活动行，再重试奖励行"，系统按新计划重跑。
  现状：功能已实现并接入主流程。⚠**默认状态在代码中自相矛盾**：`replan_agent.py:226 replan_enabled()` 默认 `"1"`（开），但同文件 `:7` docstring 写"默认开=1"、`:40` docstring 写"默认关=0"，`agent.py:9656` 注释又写"默认关"——写 PPT 前需按 `replan_enabled()` 真实默认值确认表述，不可照"默认关闭"讲。
  代码位置：`server/agent/excel/subagent/replan_agent.py`（`replan_enabled`）、`server/agent/excel/core/agent.py:_run_replan_phase`（:9606）。

- **⑧ Step2 硬规则纠偏：显式覆盖 + 经验证据自动降级 + 可选 LLM 二次判断（已扩展到 3 处，含 1 个真实覆盖盲区修复）**：Step2 里有两条规则原来是纯关键词/命名猜测，猜错了没有任何纠正入口，只能硬拦。这次先修主键必填，再把同一套思路扩展到业务必填豁免；另外核对"枚举歧义"这条时，发现它本来就有 LLM 但存在一个真实的覆盖盲区，一并修掉：

  **主键必填**：以前列名里带"id"/"编号"字样就当主键，猜错了（比如"关联ID""备注编号"这类本来可以留空的辅助列）也照样硬拦。排查还顺带挖出一个**隐藏 bug**：判断"是否已显式声明主键"这条分支里读取的字段 tip 从来就没有过，导致这条分支从建好起就没生效过，100% 全靠命名子串硬判——属于"代码里写着两条路径，实际只有一条在跑"的典型隐性缺陷。
  修复后三层纠偏：① 规则文件里显式声明 `primary_key: []`（该 sheet 无主键）或 `required: false`（该列可留空），立即生效、优先级最高；② 无显式声明时先看现有数据，该列本来就有空值就自动降级不阻断，零模型调用；③ 实在判断不了（全新表、无历史数据）时，可选开一个环境变量让模型二次判断，默认关闭，判不出来则保持硬拦截。

  **业务必填豁免**：判断"用户是否明确要求填某一列"（比如活动名称、发送人、奖励）原来全靠 4 条硬编码关键词/正则豁免（全空列/否定式/同族已填/反规范化镜像列），覆盖不到的表达方式就会被误判"漏填"，直接硬报错并把整条新增操作标记跳过。
  新增第 5 条豁免：前 4 条都没命中时，可选开一个环境变量，让模型结合用户原始指令二次判断"这一列是不是真的被要求填了"，判"可选"才豁免；判不出来/模型不可达/没开启开关，一律维持原来的硬拦截——不会因为加了 LLM 就变得敢"悄悄放行"。

  两条规则用的是**同一套安全阀设计**：显式配置 > 数据/规则确定性证据 > 可选 LLM 兜底 > 保守硬拦截，LLM 永远只在前面几层都判断不了的边界情况才出场，且只会让"过度硬拦"变宽松，绝不会让"真该拦的"变松。
  例子（主键）：某张新表"关联ID"被误判成必填主键，写盘被硬拦截；开发者一看现有数据本来就有空的"关联ID"，或直接在规则文件补一行 `required: false`，问题立刻解决。
  例子（业务必填）：用户说"开一个活动叫'九霄论剑'，发送人相关的以后再补"，"发送人"命中了硬编码关键词但用户其实没打算现在填——开启开关后模型能正确判定"可选"，不再因为这一列没填就把整条新增操作拦下来。
  数据：主键纠偏新增 5 项回归测试、业务必填豁免新增 5 项回归测试，全部通过；全量测试套件（1500+ 项）跑完无新增回归。
  代码位置：`server/agent/excel/subagent/validator_agent.py`（`_is_pk_missing`/`_pk_inferred_downgrade`/`_llm_judge_pk_required`/`_check_business_required_pre_add`/`_llm_judge_business_required`）、`server/agent/excel/core/rules_loader.py`（`get_primary_key_overlay`/`get_required_false_overlay`）；测试见 `test_pk_infer_downgrade_and_overrides.py`、`test_business_required_llm_judge.py`。

  **枚举歧义（排查后发现是"覆盖盲区"而不是"没有 LLM"）**：中文标签→枚举数字码的推断（`_auto_resolve_enum`）本来就是 LLM 实现，且默认开启，一开始以为这条不用补。但真去代码里核对判断顺序后，发现了一个真实的覆盖盲区：原代码是"先判断有没有交互回调（前端会话），没有就直接跳过"，这一判断排在调用 LLM 自动推断**之前**——导致所有没有前端会话的场景（批量执行、CI 跑批）永远走不到这段自动推断，中文标签落进枚举列既不会被自动纠正，又因为这类问题本身不算硬阻断，会被原样写盘。现在把自动推断的调用顺序提到"判断有没有回调"之前，让批量场景也能吃到同一次自动纠正，不依赖有没有前端会话，且完全不影响原来有会话时的行为。
  数据：该修复是顺序调整，不改变既有函数逻辑，风险低；具体行为不变的前提（LLM 不可达/无 session 时优雅返回，回落原逻辑）保持不动。
  代码位置：`server/agent/excel/subagent/validator_agent.py`（`_auto_resolve_enum` 调用点顺序调整，函数本身未改）。

> 对照第 11 页：L4 语义召回（BM25）同样属于"已经是真实实现"的能力，很多人会误以为只是设计稿，实际代码里默认开启。

---

> **今日（2026-09-03）excel-agent 系统性重写⑨-⑭ 已各自独立成页、归入所属 Step 环节**：⑨ LLM 分段与预判→P11-6、⑫ 隐式 FK 边推断→P11-4、⑬ DecomposeAgent 性能稳定性→P13b、⑪ Placeholder 回填强化→P14b、⑩ 引用图+语义计划+审计→P15b、⑭ ReAct 表级回读自检→P15b；另 Step1.5 契约校验层独立成页 P15c。全部为未提交代码改动（`svn status` 标 M、逐文件确认函数存在），已接入主流程但**未跑 bench**——①-⑧ 是"点状修复有 09-01 模拟压测数字"，⑨-⑭ 是"系统性重写机制已落地尚未测"，写进 PPT 时务必分开标注，不可把⑨-⑭ 当成有数据的成果讲。

---

# 第三章 · Merge：三方合并引擎

## 第 28 页｜章节封面

- **大字**：三、Merge：三方合并引擎
- **副题**：可信基准 + 稀疏比较 + 引用保护 + 安全写回

【配图占位：章节分隔页统一底图】

---

## 第 29 页｜三方合并模型

**一句话**：Merge 支持两种模式——跨分支合并（From 分支 → To 分支）和目录合并（子目录生产内容合回主干），核心比较逻辑一致，只是分支/基准版本的确定方式不同。

【配图占位：基准版本、From 分支、To 分支的三角关系图（经典三方合并模型图）】

**例子**：三方合并（**3-way merge**）不是只比较"改之前"和"改之后"两个版本，而是同时参考一个"共同祖先版本"（基准版本），这样才能分清楚"是谁改的、改了什么"，而不是把所有差异都当冲突。

| 模式 | 过程 |
|---|---|
| 跨分支合并 | From 分支与 To 分支比对，合并到 To 分支 |
| 目录合并 | 子目录合回主干，内容集成在主干分支 |

---

## 第 30 页｜Merge 总体流程与模拟环境

**一句话**：合并分七步走，从找基准版本到最终安全写回；为了验证正确性，项目里搭了一套模拟生产环境的 SVN 目录结构。

【配图占位：七步流程横向时间轴图】

| 步骤 | 内容 |
|---|---|
| 1 | 选择 From/To 两个目录 |
| 2 | 通过 SVN 的"复制自"记录定位基准版本（详见第 31 页） |
| 3 | 提取 Excel 数据、表头、公式、样式和文件元信息 |
| 4 | 比较变更并识别冲突（详见第 32 页） |
| 5 | ID 重映射与引用更新（详见第 33 页） |
| 6 | AI 辅助冲突建议（详见第 36 页） |
| 7 | 重算校验、公式保护、安全写回（详见第 34-35 页） |

```
merge/svn/demo_svn/
├── repo/  (svnadmin 仓库)
└── wc/
    ├── trunk/ (74 张表 + subdev_1/2/3)
    └── branches/ (dev1-4)
```

---

## 第 31 页｜merge-base 定位与性能优化链

**一句话**：找"合并基准版本"要靠 SVN 的"复制自"（**copyfrom**，SVN 里记录一个分支是从哪个版本切出来的属性）关系反查最近公共祖先，这条链路本身经历了 5 轮性能优化。

【配图占位：A→B→C→D→E 优化路线图，每步标注一个技术关键词】

**例子**：正常切分支能直接确定基准版本关系；如果是新建路径而非切分支产生的，标记为"推断得出"，提示这条基准关系置信度略低。查找基准版本本质是找两个分支历史里第一个共同出现的版本节点，这在图论里叫**最近公共祖先**（**LCA**，Lowest Common Ancestor）。

| 版本 | 方法 | 解决问题 | 性能提升 |
|---|---|---|---|
| A | Python 三重循环 | 10w 行×100 列会卡死 | — |
| B | 按哈希建立行索引映射 | 线性扫描卡死 | O(n)→O(1) |
| C | **稀疏化**（`sparse`，只记录有变化的格子，不物化整表） | 全表建哈希内存过大 | 内存 GB→MB |
| D | **numpy 向量化**（把逐格循环处理改成批量运算） | 逐单元格 Python 调用消耗高 | 大表提升 10-50 倍 |
| E | 公式/批注回退逐单元格循环 | 向量化处理公式/批注会出错 | 保证性能同时保证正确性 |

| 优化项 | before | after |
|---|---|---|
| SVN 版本号查询 | 每张表单查一次，共 191s | 一次递归查询全目录，**0.2s** |

---

## 第 32 页｜Compare 全流程：四层比较

**一句话**：比较分四层——文件层、Sheet 层、行列层、语义层，逐层从"文件有没有变"细化到"公式/外键会不会冲突"。

【配图占位：四层漏斗/分层图，从粗到细】

| 层 | 比较内容 |
|---|---|
| 文件层 | 新增、删除、重命名、移动 |
| Sheet 层 | 新增/删除 sheet、表头变化 |
| 行列层 | 主键匹配、行新增删除、字段变更 |
| 语义层 | 公式、外键引用、ID 冲突和可合并性判断 |

| 技术 | 解决问题 | 效果 |
|---|---|---|
| 哈希索引 | 逐行线性查找慢 | 行定位常数时间 |
| 稀疏比较 | 全表物化上百列 | 只保留变化格 |
| numpy 向量化 | 逐格归一化慢 | 批量比较 |
| 公式/批注回退 | 向量化理解不了公式/批注 | 保证正确性 |

| 指标 | before | after |
|---|---|---|
| 大表 compare（平均） | 48s | **6.4s** |
| 10 万行全链 | 卡死 | **10.4s** |
| 提交数据量 | 46.4MB | **0.1MB** |

---

## 第 33 页｜ID 重映射与引用完整性

**一句话**：多分支同时新增同一主键会冲突，系统给后来者分配新 ID，随后同步更新引用列，避免只改主表 ID 却留下悬空外键。

【配图占位：ID 冲突→分配新ID→级联更新引用列 三步示意图】

**例子**：dev1 和 dev2 分支都新建了 ID=2001 的道具行，合并时系统发现冲突，把 dev2 的那一行改成新分配的 ID=2005，并同步把所有引用了 2001（属于 dev2 那行）的地方都改成 2005，不会出现"改了主表 ID，但引用它的邮件表还指着旧 ID"的情况。

> 这一层思路和 Excel-Agent 的 produces/consumes（第 14 页）一致：先建立依赖契约，再执行替换，否则会出现引用链断裂。

---

## 第 34 页｜公式缓存保护

**一句话**：Excel 文件里公式和缓存值是分开存的，直接写盘容易只留缓存值丢公式；这一页讲怎么检测和修复。

【配图占位：公式标签和缓存值标签两个 XML 片段的对比截图】

**例子**：Excel 保存公式单元格时，除了公式本身，还会存一个"上次算出来的值"（公式缓存）方便打开时不用重新算。如果写盘时不小心只写了值、丢了公式，打开表格公式就会变成 0 或空——这种情况标记为 `lost`（真的出问题，需要修复）；如果只是缓存的值变了但公式还在，属于正常的重算更新，标记为 `changed`（不需要修复）。

**处理逻辑**：
1. 无公式的表走最快路径（扫描文件底层压缩包判断有没有公式标签，不用整表解析）；
2. 保存前后做快照比较：缓存值消失标记 `lost`，只是数值变化标记 `changed`；
3. `lost` 的情况调用 LibreOffice 重算校验；
4. 设置 60 秒超时上限（避免单文件卡死全流程）+ 文件互斥锁 + 重算结果先写临时目录再替换源文件。

| 指标 | before | after |
|---|---|---|
| 公式检测（10 万行） | 完整解析 ~6s | 压缩包扫描标签 ~0.05s（**约 120 倍**） |

---

## 第 35 页｜fast-apply 写回

**一句话**：公式单元格冲突单独处理；大表写回不走完整表格解析，而是直接改文件底层字节，快很多。

【配图占位：冲突界面截图占位——显示单元格原有公式供用户比对选择】

**例子**：两个分支都改了同一个公式单元格，写法一样但缓存值不一样——不算冲突（正常重算差异）；如果引用的输入值变了，标记为 `formula_changed`（代码里表示"公式引用值变了，需要提醒"的字段名）并用内置求值器预览重算结果；写法本身不一样才算真冲突，弹出界面让用户对比选择，选完打上 `formula_resolved`（表示"用户已选定写回版本"的字段名）标记，写回阶段按标记写回。

**fast-apply**（一种绕开整表读写、直接修改文件内部数据的快速写回方式）：xlsx 文件本质是一个压缩包，直接定位到要改的那个单元格改完塞回去，不用把整个表格完整读出来再整体写回。

| 指标 | before | after |
|---|---|---|
| apply 处理 | 19.6s | **4.3s** |
| 序列化 | 4.9s | **0.3s** |

---

## 第 36 页｜AI 辅助冲突处理

**一句话**：AI 只提供冲突建议，不参与核心合并策略，最终由用户确认。

【配图占位：AI 建议卡片截图占位——展示来源、理由、置信度】

**例子**：AI 给冲突一个"我有多确定该选这个值"的置信度分数，不是只看模型自己怎么说，还要结合"版本新不新""这个值本身合不合理"综合打分，避免模型一个人说了算。

**建议链路**：点击 AI 建议 → 前端请求 → 后台线程池运行 → 拼接提示词发给模型 → 模型返回结构化结果 → 防幻觉校验 → 输出建议（含来源、理由、置信度）。

**置信度公式**（`server/services/agent_service.py:94-96`，三个因子对应代码里的变量名 `llm_conf`/`rev_conf`/`value_conf`）：

```
final = 0.60 × llm_conf + 0.25 × rev_conf + 0.15 × value_conf
```

| 因子 | 作用 |
|---|---|
| `llm_conf`（模型自评置信度） | 语义判断主信号 |
| `rev_conf`（修订号领先度） | 版本越新越倾向采纳 |
| `value_conf`（值域合理性） | 判断取值是否异常 |

> 某个因子没有信号时用模型自评置信度兜底，保证"没有规则信息时不稀释模型自评"。

**并行技术**：缓存优先（缓存键由表名+sheet名+列名+版本组成）→ 批量调用（未缓存的单元格一次性拼进提示词）→ 批量失败时回退线程池逐格并发。

---

## 第 37 页｜大表格比对并行性能

**一句话**：Compare 和写回按文件/Sheet 粒度并行，计算密集用多进程池，等待密集（读文件/网络）用多线程池。

【配图占位：多进程池 vs 多线程池 分工示意图】

**例子**：**ProcessPool**（多进程池，适合"算得很凶"的任务，比如大量数值比较）和 **ThreadPool**（多线程池，适合"等待很多"的任务，比如读文件）分别处理不同性质的子任务；读取 Excel 用 **calamine**（一个用 Rust 语言写的高速 Excel 读取引擎，比常规 Python 方案快得多）替代原来的读取方式。

| 优化项 | 受益 |
|---|---|
| calamine 高速读取引擎 | 222 次打开表格：17.5s → **0.8s** |
| SVN 信息批量预填 | 192s → **0.2s** |
| 多进程/多线程池并行 | 8 个表格：3.69s → **1.84s** |
| fast-apply 快路径 | 25MB sheet 数据序列化：3.4s → **0.98s** |
| 未变更表跳过 | 可跳过占大多数的表格体 |

---

# 第四章 · 验证与总结

## 第 38 页｜验证与可靠性

**一句话**：功能好不好用，最终要靠回归测试和单元测试数量说话。

【配图占位：回归通过率仪表盘图标】

| 指标 | 数值 |
|---|---|
| 回归通过 | Agent 109 项 + Merge 核心 71 项 = **180 项** |
| 单元测试 | 145 → 204 → **323+ 项零回归** |

> 证据：`docs/archive/优化全过程.md`

---

## 第 39 页｜总结与展望

**一句话总结**：
- Excel-Agent = **理解（模型）+ 编译校验（真实 schema）+ 规则约束 + 确定性执行 + 写后自检 + 自学习**
- Merge = **可信基准版本 + 稀疏比较 + 引用保护 + 安全写回 + AI 辅助建议**

【配图占位：两条主线汇总成一张总览图】

**后续重点**（诚实标注现状）：

| 方向 | 现状 |
|---|---|
| 全字段可编辑交互修正 | 设计完成，逐步替换现有列名批量修正卡片 |
| Replan-on-failure | 已实现；默认状态代码自相矛盾（`replan_enabled()` 默认 `"1"` 但多处 docstring 矛盾），写 PPT 前按真实默认值确认（见 P27⑦）|
| 编号账本跨分支查重全面推广 | 单目录已生效，跨分支需要显式配置分支列表 |
| 聊天区降噪 | 仅完成模拟压测验证，尚未合入生产代码 |
| Step1 二次核对接入主流程 | `ai_verify_intents` 已实现且在旧 6-Step 路径生效，尚未接入现有 4-Step 主链路，评估中 |
| Step1 长指令实体级分块 | 已定位到"复合长指令单次拆解漏项"是当前最大失分项，分块方案设计中，尚未实现 |
| 更多端到端回归 | 持续补充复杂跨表/跨分支场景 |

---

## 第 40 页｜结束页

**THANK YOU**

【配图占位：结束页统一底图 + 联系方式】

---

# 附录一：术语速查表（仅收录英文/外来术语与生僻专业词，按首次出现页码排序）

| 术语 | 页码 | 一句话解释 |
|---|---|---|
| FK（外键） | P3 | 一张表里指向另一张表主键的列 |
| intent（意图） | P5 | 自然语言拆解出的最小操作单元 |
| FastAPI / CodeMaker Serve | P6 | 业务后端框架 / 独立的模型服务后端 |
| LangGraph | P8 | `graph.py` 入口 `classify→{qa|crud}` 路由框架（4-Step 本身用自研 Step Pipeline 编排，非 LangGraph 图） |
| hallucination（幻觉） | P9 | 模型编造出不存在的字段/表名 |
| schema | P9 | 一张表的列名/类型/外键说明书 |
| token | P9 | 模型处理文本的计量单位 |
| few-shot | P9 | 在提示词里给模型看示例学格式 |
| BM25 | P11 | 基于词频统计的文本相关性打分算法（非大模型） |
| rapidfuzz / WRatio / token_set_ratio / partial_ratio | P12 | 高性能模糊字符串匹配库及其三种相似度指标 |
| placeholder（占位符） | P13 | 真实 ID 生成前的临时标记符号 |
| 拓扑排序 | P18 | 按依赖关系排出不矛盾的执行顺序 |
| ReAct | P19 | 让模型交替"推理"和"调用工具"的智能体范式 |
| checkpoint（中间态存档） | P20 | 任务中途的进度快照，可从存档续跑 |
| all_ok | P21 | 代码里判断"任务是否整体成功"的字段名 |
| anti_pattern（反模式） | P21 | 从失败案例总结出的易踩坑规律 |
| lint | P27 | 对代码/数据做静态结构检查 |
| quarantine（隔离区） | P24 | 未通过审核的候选规则暂停使用的观察区 |
| lift（提升量） | P24 | 开启新规则后通过率的提升幅度 |
| mini regression（小回归） | P24 | 转正前的小样本新旧对比测试 |
| `_norm_name` | P26 | 代码里做命名归一化比较的函数名 |
| `claim_id` | P27 | 代码里"认领 ID"、跨分支查重的函数名 |
| Replan-on-failure | P27 | 失败后聚合信息触发一次重规划的开关名 |
| 3-way merge（三方合并） | P29 | 参考共同祖先版本判断差异归属，而非只比较两版本 |
| copyfrom | P31 | SVN 记录分支是从哪个版本切出来的属性 |
| LCA（最近公共祖先） | P31 | 两分支历史回溯的第一个共同版本节点 |
| sparse（稀疏化） | P31 | 只记录有变化的格子，节省内存 |
| numpy 向量化 | P31 | 批量运算代替逐格循环，提速明显 |
| `formula_changed` / `formula_resolved` | P35 | 代码里标记公式状态的字段名 |
| fast-apply | P35 | 直接改文件内部字节，绕开整表读写 |
| `llm_conf` / `rev_conf` / `value_conf` | P36 | AI 建议置信度公式的三个因子变量名 |
| ProcessPool / ThreadPool | P37 | 多进程/多线程池，分别适合算力密集/等待密集任务 |
| calamine | P37 | Rust 实现的高速 Excel 读取引擎 |

---

# 附录二：数据来源速查

| 数据 | 路径 |
|---|---|
| 单一事实源（全部性能数据） | `PERF_DATA_SUMMARY.md` |
| Excel-Agent 优化全过程 | `docs/archive/优化全过程.md` |
| Merge 性能报告 | `merge/scripts/benchmark_report.md` |
| 大表压测 | `server/tests/reports/bench_perf_tables_latest.{md,json}` |
| Merge A/B 复跑 | `server/tests/reports/bench_merge_before_after_latest.json` |
| 本轮优化模拟压测报告 | `bench/ppt_validator_forwardref_norm.md` / `ppt_placeholder_nested_backfill.md` / `ppt_agent_service_noise_filter.md` / `ppt_decompose_chain_and_dict_lint.md` / `ppt_step4_allok_drift.md` |
| Skill 系统阈值常量 | `server/agent/excel/core/skill_updater.py` |
| 语义召回实现 | `server/agent/excel/rag_searcher.py` + `server/agent/excel/locator/table_locator.py` |
| Replan-on-failure 实现 | `server/agent/excel/subagent/replan_agent.py` |
| 主键硬规则纠偏实现 | `server/agent/excel/subagent/validator_agent.py`（`_is_pk_missing`/`_pk_inferred_downgrade`/`_llm_judge_pk_required`）+ `server/agent/excel/core/rules_loader.py`（`get_primary_key_overlay`/`get_required_false_overlay`） |
| 主键硬规则纠偏测试 | `server/tests/test_pk_infer_downgrade_and_overrides.py` |
| 业务必填豁免 LLM 二次判断实现 | `server/agent/excel/subagent/validator_agent.py`（`_check_business_required_pre_add`/`_llm_judge_business_required`） |
| 业务必填豁免 LLM 二次判断测试 | `server/tests/test_business_required_llm_judge.py` |
| 枚举歧义非交互覆盖盲区修复 | `server/agent/excel/subagent/validator_agent.py`（`_auto_resolve_enum` 调用点顺序调整） |
| 枚举歧义非交互覆盖盲区测试 | `server/tests/test_enum_autofix_noninteractive.py` |
| 写后自检与自修复实现 | `server/agent/excel/core/agent.py:_run_verify_repair_loop` + `server/agent/excel/repair/` |
| 备份/回滚/存档实现 | `server/agent/excel/core/agent.py`（`_compute_rollback_targets`/`_rollback_write`/`_save_nl_checkpoint`） |
| AI 冲突置信度公式 | `server/services/agent_service.py:91-167` |
| 系统架构说明 | `server/agent/excel/README_CODEMAKER_API.md` |
