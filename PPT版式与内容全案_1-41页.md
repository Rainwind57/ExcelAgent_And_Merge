# PPT 版式与内容全案（1-41 页，含布局线框）

> 在 `PPT逐页脚本_整合重排版.md`（内容排序）基础上，叠加 `PPT答辩数据充分性审查与问答预案.md` 的 4 条🔴必改项（补数据口径页、补N标注、补耗时/ok率代价、补Merge局限页），并给**每一页**标出具体版式区域划分。可直接照此在 PPT 里摆放文本框/表格/图形。
> 全篇统一沿用现有 PPT 红黑白风格：标题黑色/网易红强调关键词，指标框用红底白字或红字数字卡片。
> 页数：**44 页内容页**（P19b/P25b/P36b 为插入页，未整体重排编号——定稿时把它们插入到对应位置后顺次下移即可，这里保留 b 后缀是为了不牵动前面已核对过的页号引用）。
> **2026-09-01 追加**：新增 P25b/P36b「核心代码实现展示」，对应答辩评分「Part 3 核心部分——核心功能具体实现+必要代码展示」硬要求，此前全篇只有JSON示例/目录树，零真实源码，是本轮补的最大缺口。
> **2026-09-01 再追加**：新增 P19b「业务规则接口」（紧邻Skill章节前，方便与P20直接对比）——此前全篇只字未提"非工程师如何手动补充业务规则"这条真实存在的机制（`rules/fill|validate/*.md`），是配表这类强业务场景的重要能力点。

---

## 版式模板库（先定义，下面每页引用编号）

**模板 A｜封面/结束页**
```
┌───────────────────────────────┐
│                                 │
│        超大标题（居中）           │
│        副标题（居中，小字）        │
│        作者｜日期（居中，底部）      │
└───────────────────────────────┘
```

**模板 F｜章节封面**
```
┌───────────────────────────────┐
│                                 │
│      章节大字（居中，网易红数字装饰） │
│      一句话副题（居中，灰）          │
│                                 │
└───────────────────────────────┘
```

**模板 C｜标准内容页（单栏）**
```
┌───────────────────────────────┐
│ 标题                        P.N │
│ 一句话定位（斜体/灰底）              │
├───────────────────────────────┤
│ 主体：要点列表 或 单张全宽表格         │
├───────────────────────────────┤
│ ▓指标框▓ before→after(N=x)      │
└───────────────────────────────┘
```

**模板 D｜左文右表（双栏）**
```
┌───────────────────────────────┐
│ 标题                        P.N │
├────────────┬──────────────────┤
│ 左35%       │ 右65%             │
│ 要点/结论    │ 表格/技术细节        │
│            │                  │
├────────────┴──────────────────┤
│ ▓指标框▓ before→after(N=x)      │
└───────────────────────────────┘
```

**模板 E｜流程图页**
```
┌───────────────────────────────┐
│ 标题                        P.N │
├───────────────────────────────┤
│ [①]→[②]→[③]→[④]  横向流程框+箭头 │
├───────────────────────────────┤
│ 补充说明表格/要点                  │
└───────────────────────────────┘
```

**模板 H｜问题-方案-数据卡片页**（用于"本轮工程优化"类页面：先讲遇到什么问题，再讲怎么修，最后才是数据，避免直接甩指标显得突兀）
```
┌───────────────────────────────┐
│ 标题                        P.N │
├───────────────────────────────┤
│ 卡片1 ❗问题场景 → 🔧方案 → 📊before→after(N) │
│ 卡片2 ❗问题场景 → 🔧方案 → 📊before→after(N) │
│ 卡片3 ❗问题场景 → 🔧方案 → 📊before→after(N) │
├───────────────────────────────┤
│ 诚实标注：哪些真调用/哪些模拟           │
└───────────────────────────────┘
```

**模板 H'｜核心代码展示页**（专用于"核心代码实现展示"页，是模板H的变体：卡片内容从"问题-方案-数据"换成"代码块-技术难点-数据引用"）
```
┌───────────────────────────────┐
│ 标题                        P.N │
├───────────────────────────────┤
│ 代码卡片1：```code```(8-15行)     │
│           →技术难点一句话          │
│           →数据详见Pxx(不重复摆数字) │
│ 代码卡片2/3：同上结构               │
└───────────────────────────────┘
```
> 每段代码必须真实可查（file:line），不写伪代码/示意代码；答辩时可直接现场打开仓库对照，这是最强的"技术含量"背书。

**模板 G｜汇总看板页**
```
┌───────────────────────────────┐
│ 标题                        P.N │
├───────────────────────────────┤
│ 大汇总表格（6-9行，全宽，斑马纹）        │
├───────────────────────────────┤
│ 一句话收束（不重复表格数字）             │
└───────────────────────────────┘
```

---

# 第一章 · 封面与业务背景（P1-4）

## P1｜封面　版式：A

- 主标题：**AI 配表助手**
- 副标题：——自然语言驱动配表 + 三方合并 Merge
- 底部：吴植贤｜2026年9月4日

## P2｜目录　版式：C（无指标框）

- 标题：目录
- 主体（四行要点，非表格，用大编号01-04竖排）：
  - 01 业务背景与任务目标
  - 02 Excel-Agent：4-Step 多 Agent 配表流水线
  - 03 Merge：面向 SVN 目录分支的三方合并
  - 04 验证、展望与总结

## P3｜业务背景：真实配表的复杂性　版式：D

- 一句话定位：复杂 Excel 配表不是单表编辑问题，而是跨表、跨目录、跨版本的一致性工程。
- 左栏（三维度要点卡片）：
  - 规模复杂：表多字段多、跨表引用密集
  - 表达复杂：自然语言不会精确写表名/列名/枚举/外键
  - 协作复杂：SVN目录分支+外链并存，人工合并易漏行丢公式
- 右栏（问题→后果表，5行，只保留一张不重复）：

| 编号 | 存在问题 | 后果 |
|---|---|---|
| 1 | Merge过程漏行丢数据 | 数据静默消失 |
| 2 | 跨分支编号冲突 | 后提交覆盖导致线上bug |
| 3 | 公式缓存丢失 | 输出0或空 |
| 4 | 改表慢、易错 | 效率低、易漏外键 |
| 5 | AI误写不可逆 | 写坏数据找不回 |

- 指标框：本页无量化指标（背景定性页），底部改放一句"以下每个技术点，都能追溯到解决这5条中的哪一条"。

## P4｜任务目标　版式：C

- 主体表格：

| 引擎 | 目标 | 关键约束 |
|---|---|---|
| Excel-Agent | 自然语言→结构化意图+多表配表 | 存在校验、可执行、可跟踪 |
| Merge | 目录分支模式三方合并 | 保护公式、ID引用、冲突可解释 |

- 指标框：无数字，放一句★核心主张："模型负责理解，规则负责约束，执行层保持确定性"。

---

# 第二章 · Excel-Agent 4-Step（P5-25）

## P5｜章节封面　版式：F

- 大字：二、4-Step Excel-Agent
- 副题：模型负责理解，规则负责约束，执行层保持确定性

## P6｜总体架构与定位　版式：E

- 流程图（横向4方框）：`①解析 → ②校验 → ③执行 → ④汇总`
- 补充表格：

| Step | 职责 |
|---|---|
| 1、解析 | 分段→定位→schema注入拆解→语义计划编译 |
| 2、校验 | 字段/类型/依赖等规则校验 |
| 3、执行 | 依赖顺序确定性写盘、ID分配、引用回填 |
| 4、汇总 | 变更归纳、错误总结、自学习反模式 |

- 底部一句话：LangGraph 编排 TableAgent 4-Step，Step1 的 JSON 越准，后面越省事。

## P7｜★NEW｜数据口径说明　版式：C

> 🔴必改项：防"测试环境/单次测量"追问，插在开始讲数字之前。

- 一句话定位：后面所有 before/after 数字遵循同一套口径，不跨样本集横比。
- 主体（要点列表）：
  - 同批次、同硬件、同数据集对比，不同样例集不横向比较
  - "大表"统一基准：3000行×150列（并行测试）/ 10万行（全链压测）/ 25MB sheet（fast_apply测试）
  - 标注类型：**真实e2e**（HTTP实测/回归集跑出）／**多样本聚合**（30/8样例）／**单次算法级压测**（rapidfuzz/编辑距离）／**模拟压测**（本轮5项优化，已在对应页标注）
  - 含"设计值/目标"的条目会明确标注，不与实测数字混排
- 指标框：无，本页是方法论页。

## P8｜Prompt 工程：约束输出协议　版式：D

- 左栏（组件表）：

| 组件 | 作用 |
|---|---|
| 角色设定 | 限定任务边界 |
| 真实schema注入 | 防止幻觉字段 |
| 输出格式JSON | 可执行可校验 |
| 反例约束 | 降低污染 |
| few-shot示例 | 提升结构稳定性 |
| 截断策略 | 降低token/防超时 |

- 右栏（优化对比表）：

| 优化点 | before | after |
|---|---:|---:|
| Prompt上下文 | 全量表索引76.11MB | 单任务schema 510字符/159token |
| 输出格式 | 自由文本，解析不稳 | 固定JSON |

- 指标框：Prompt体积压缩比 76.11MB→159token（单次静态测算，非样本聚合）

## P9｜Step1 解析：全链路核心　版式：D

> 🔴必改项：补耗时/ok率代价数据，不能只放正向数字。

- 左栏（技术要点）：分段→候选表定位→schema注入→LLM拆解→produces推断
- 右栏（技术说明表）：

| 操作 | 技术说明 |
|---|---|
| 分段 | 规则分段 |
| 候选表定位 | 规则+检索+关系扩展 |
| schema注入 | 注入真实表头、类型和FK |
| LLM拆解 | DecomposeAgent输出结构化intents |
| produces推断 | FK图+主键规则 |

- 指标框（**两行，正向+代价并列**）：

| 指标 | before | after | 说明 |
|---|---:|---:|---|
| 定位功能(N=8) | 0.6667 | 0.8056 | +20.8% |
| 覆盖度(N=8) | 0.6111 | 0.7500 | +22.7% |
| 平均耗时(N=8) | 82236ms | 98637ms | 代价：更严格校验多花时间 |
| 响应ok率(N=8) | 0.8333 | 0.3333 | 代价：主动拒错，非硬凑答案 |

## P10｜增强：LocatorAgent 三级定位　版式：D

- 左栏一句话：用户不会准确说出真实表名，系统要把口语映射到真实表。
- 右栏（四通道表）：

| 通道 | 技术 | 作用 |
|---|---|---|
| L1 规则通道 | 表名/别名/关键词 | 零成本直接命中 |
| L2 模糊通道 | rapidfuzz混合指标 | 拼写/命名变体兜底 |
| L3 关系扩展 | FK一跳扩展 | 命中主表带绑定表 |
| L4 RAG | BM25语义召回 | 规则未命中兜底 |

- 指标框：无（定位机制说明页，性能数据在下一页）

## P11｜增强：rapidfuzz 混合指标算子性能　版式：C

- 公式（居中大字）：`rapidfuzz_score = WRatio×0.5 + token_set_ratio×0.3 + partial_ratio×0.2`
- 主体表格：

| 指标 | 作用 |
|---|---|
| WRatio | 综合编辑距离/长度差/部分匹配 |
| token_set_ratio | 忽略词序和重复词 |
| partial_ratio | 长字段含短查询仍命中 |

- 指标框：

| 指标 | 纯Python | rapidfuzz C++ |
|---|---:|---:|
| 编辑距离计算(单批) | 2781.54ms | 192.16ms |
| 单次匹配平均 | 0.0093ms | 0.0006ms |

（标注：算法级压测，非样本聚合，输入为表名/列名字符串集合）

## P12｜增强：DecomposeAgent schema 注入拆解　版式：D

- 左栏（输入输出样例，代码块）：
```
输入：Ability表格 row1列名/row2类型/字段类型/外键/填表规则
输出：{action:add, table:Ability, fields:{id:100,name:"TEST"}, produces:[<aid>]}
```
- 右栏（要点+指标框）：
  - schema注入从源头解决幻觉列名
  - few-shot禁止伪造占位符，超时降级逐表格重拆
  - 规则安全网：intent<2条或畸形→回退模板

| 指标(N=8) | before | after |
|---|---:|---:|
| 字段精准率 | 0.5185 | 0.6917 |
| 严格通过率 | 0.1667 | 0.3333 |

## P13｜增强：produces/consumes 依赖契约　版式：D

- 左栏（样例）：新增活动3001→奖励30010绑定3001→邮件5001附件30010
- 右栏（术语表）：

| 术语 | 含义 |
|---|---|
| produces | 本意图产生的占位符 |
| consumes | 本意图消费的占位符 |
| placeholder | 写表时临时标记，执行时替换 |

- 指标框（标注：quest_npc 单链场景，非样本聚合，R7→R8c迭代）：

| 指标 | before | after |
|---|---:|---:|
| quest_npc引用一致 | 0.62 | 1.00 |
| quest_npc耗时 | 134s | 107s |

## P14｜Step1 质量门禁：schema-aware 二次编译　版式：C

- 主体表格：

| 检查内容 | 修复方式 |
|---|---|
| 孤立add intent/稀疏影子重复 | 删除 |
| 字段不在schema/大小写差异 | 修正到真实列 |
| produces/consumes核对 | 解析真实ID |
| 自产自销/依赖环 | 阻断执行 |

- 指标框（标注：单批回归集计数，非比率）：

| 优化项 | before | after |
|---|---:|---:|
| type_mismatch | 2 | 1 |
| 失败trace归纳反模式 | 0 | 4类 |

## P15｜增强：Step2 L0/L1/L2 三层校验　版式：E

- 流程图（三个纵向/横向卡片）：`L0规则闸(纯代码) → L1 AI裁决(1次LLM) → L2人工gate(交互卡片)`
- 补充表格：

| 层级 | 处理内容 |
|---|---|
| L0 规则闸 | 类型、PK、FK、占位符 |
| L1 AI裁决 | NL覆盖度、值合理性 |
| L2 人工gate | 用户改字段/值后续跑 |

- 指标框：无数据（机制说明页，⚠️审查发现的空白点，若答辩前能补一组"三层校验前后误报率"数字，优先补在这页）

## P16｜Step2：写盘前硬门禁　版式：C

- 主体（要点列表）：
  - 字段是否存在，中文表头能否映射真实列
  - 值类型可转换，枚举值合法
  - FK指向正确表和主键，placeholder有producer
  - hard error阻断写盘并生成可交互修正
- 指标框：无（沿用L0规则闸，无独立数字）

## P17｜Step2 交互修正：从报错到可恢复　版式：C

- 主体（要点列表）：
  - 现状：列名批量修正卡片；计划：全字段可编辑表格
  - 用户改列名/值/删字段/删intent → POST `/api/agent/reply` 续跑
  - 后端回写用户编辑到原intent，重新跑Step2
- 指标框：无（讲设计现状+规划，不编数字）

## P18｜Step3：确定性执行　版式：D

- 左栏一句话：Step3 不再调用模型，只做可解释写盘。
- 右栏（动作表）：

| 动作 | 技术 |
|---|---|
| 拓扑排序 | 按produces/consumes依赖排序 |
| ID分配、占位符替换 | 新增行分配主键 |
| 失败结构化归因 | 缺字段/PK冲突/类型错误 |
| 可恢复边界 | 失败停于可恢复点 |

- 指标框（标注：R4 case106单用例，非样本聚合）：

| 测例 | before | after |
|---|---:|---:|
| 复杂样例106 | 204s | 112s |

## P19｜增强：Step4 结果汇总与自学习闭环　版式：D

- 左栏一句话：Step4 汇总最终结果，同时把失败情况沉淀下来。
- 右栏（功能表）：

| 功能 | 说明 |
|---|---|
| 汇总结果 | 成功数+失败数+写入行+预览 |
| 失败归因 | 定位失败发生在哪个层 |
| anti_patterns | 从失败trace归纳反模式 |
| mini_regression | 失败样例进小回归集 |

- 指标框：无数字（数字在Skill效果数据页与本轮优化页出现），底部一句：Step4产出的反模式是Skill系统的原料 →

## P19b｜★NEW｜业务规则接口：给"人"填的强约束　版式：D

> 紧邻Skill章节放置：下一页起的Skill四层体系是"系统自己攒的经验"，本页是"人定的红线"，两者都是配表知识源，先看人工规则，再看系统自学习，顺序上更好对比。

- 一句话定位：有些强约束AI永远猜不出来（比如"新增道具ID=当前段最大值+1"），必须由懂业务的人手写，系统要留一个**专门给人填、AI不会覆盖**的位置。
- 左栏（结构表）：

| 目录 | 内容 | 编写者 | 消费阶段 |
|---|---|---|---|
| `rules/fill/<表名>.md`+`_global.md` | 自然语言强约束（ID分段/枚举归一化/默认值） | 配置人员手打 | 注入Step1解析prompt |
| `rules/validate/<表名>.md` | md内嵌yaml（type/required/enum/min/max/unique） | 配置人员/工程师 | 合并进Step2校验管线 |

- 右栏（真实示例，摘自`rules/fill/item.md`）：
```markdown
- 新增道具时 item_id 取当前段最大ID+1，禁止硬编码。
- quality 品质列只能填1-5：1凡品/2良品/3上品/4珍品/5绝品。
```
- 指标框：无量化数字（机制说明页），改放一句收束：优先级 `rules`(用户手打) > `skills/L1_derived`(自动派生)，冲突以rules为准；按候选表stem精准加载对应文件，不撑爆prompt。
- 诚实局限：改动需**重启进程**才生效（无热更新）；纯文本无格式校验，yaml手误会静默失效；当前一表一文件，规则多了没有跨表检索/UI。MVP阶段选成本最低的纯文本方案，规模变大后可加轻量校验工具/表单，不改变底层注入机制。

## P20｜Skill 系统①：四层知识结构　版式：G

- 主体大表：

| 层级 | 目录 | 内容 | 生成方式 |
|---|---|---|---|
| L0 人工/根目录 | skills/*.yaml | sheet别名/引导动词/列短形式 | 人工+自动混合 |
| L1 自动派生 | L1_derived/ | 列别名/行定位规则/多sheet消歧/类型约束 | 扫描resources自动生成 |
| L2 运行时 | L2_runtime/ | 学到的列别名(hits/confidence)/跨表关系权重 | evidence promote写入 |
| L3 反模式 | L3_anti_patterns/ | 歧义列/类型约束/失败操作/语义模式 | 规则频次升级+AI归纳 |

- ⚠️辨析（呼应上一页「业务规则接口」，防止被问"这两个是不是重复设计"）：

| | rules/（上一页） | skills/（本页） |
|---|---|---|
| 内容性质 | 业务强约束（ID生成/枚举含义/校验范围） | 检索匹配知识（别名/定位规则/关系权重） |
| 生成方式 | 100%人工手打，永不自动写入 | L0人工，L1-L3可自动派生/学习/转正 |
| 优先级 | 最高，与AI自学习结果冲突时以它为准 | 低于rules，可被新证据迭代覆盖 |
| 一句话 | "AI永远猜不出的规则" | "AI能学会、也能学错的经验" |

- 收束句：Agent与Merge共用同一份知识源；rules是"人定的红线"，skills是"系统自己攒的经验"，两者不冲突、不重复。

## P21｜Skill 系统②：生成审核机制 + 转正安全阀　版式：D

- 左栏（阈值表）：

| 判定 | 阈值 |
|---|---|
| 列别名候选转正 | 7天内命中≥3次，把握30%~75% |
| 反模式升级active | 7天内≥3次 |
| 反模式pending_review | 30天内≥2次 |
| AI归纳反模式转正 | 需多命中3次（无ground-truth兜底） |
| 冲突消解 | 不自己决定，交人工 |
| 衰减 | 60天未用移除，可信度<30%休眠 |

- 右栏（★NEW安全阀小流程图）：`候选 → 快照 → 写盘 → mini回归(30样本,lift≥0.05) → 转正active / 回滚+隔离区(30天)`
- 指标框：无数字（机制页），底部一句：不是"学了就用"，是"学了先试跑，回归不掉分才转正"。

## P22｜Skill 系统③：效果数据　版式：G

- 主体大表（含N标注）：

| 数据点(N) | 指标 | before | after |
|---|---|---:|---:|
| skill简版(N=30) | 定位成功率 | 0.933 | 1.000 |
| skill简版(N=30) | 精确命中 | 0 | 1.00 |
| skill简版(N=30) | 严格综合 | 0.704 | 0.959 |
| skill简版(N=30) | 平均耗时 | 533ms | 321ms |
| skill真写盘(N=8) | 定位/覆盖/精准 | 0.472/0.417/0.497 | 0.639/0.583/0.590 |
| Step4反模式(模拟回放,N=8) | 同类失败预警覆盖 | 0/8 | 8/8 |
| Step4反模式(真实e2e,N=8) | 失败trace→反模式 | — | 4类 |

## P23｜★NEW｜本轮工程优化①：占位符闭环 + 命名漂移修复　版式：H（问题-方案-数据表格）

| # | ❗问题 | 🔧方案 | 📊数据(N) |
|---|---|---|---:|
| 1 | 命名写法不一致（`newConvId` vs `<new_conv_id>`），精确字符串比较认不出同一ID，误报FORWARD_REF_BROKEN | `_norm_name`归一化后再比较 | 误报率(N=6) **5/6→2/6** |
| 2 | 占位符藏在嵌套结构里（如`{"target":{"data":{"npc_id":"<npc_1>"}}}`），旧扫描只认顶层，漏检 | 递归扫描dict/list任意深度 | 漏检数(N=8) **7→0** |
| 3 | 执行顺序一乱，consumer找不到还没跑出来的producer，直接判失败 | 一轮执行完后再跑一轮backfill补齐 | 可执行节点数(5节点反序压测) **1/5→5/5** |

**底部诚实标注**：#1、#2 真实函数调用；#3 backfill为简化状态机模拟（真实逻辑依赖Agent写盘上下文，单独调用成本高）。

## P24｜★NEW｜本轮工程优化②：聊天降噪 + 成本控制 + Step4口径修复　版式：H（问题-方案-数据表格）

| # | ❗问题 | 🔧方案 | 📊数据(N) |
|---|---|---|---:|
| 1 | 内部thinking日志原样推给用户，有用信息被淹没 | 加可见性过滤+摘要，只留结果性内容 | 展示行数(N=25会话) **24→8行**，噪音占比 **66.7%→0%** |
| 2 | 链式分组数随候选表线性涨，每组都调一次LLM | 加分组硬上限，超额交规则兜底 | 分组数=LLM子调用次数(N=12候选) **3→2** |
| 3 | 合法dict列（如`target_data`）被当幻觉字段误清空 | 区分"合法dict列"与"真幻觉列"，只清后者 | 误清空率(N=3) **100%→0%** |
| 4 | `all_ok=s3.ok`只看Step3，Step1/2真实问题被静默吞掉 | 改成s1/s2/s3三步综合判定 | 判定错误场景数(N=8种布尔组合) **3/8→0/8** |

**底部诚实标注**：#3、#4 真实函数调用；#1 为AST提取真实函数体加载执行；#2 为按git diff复原的分组骨架模拟（真实逻辑需真实LLM会话，单独调用成本高）。

## P25｜Excel-Agent 章节指标汇总　版式：G

| 指标(N) | before | after | 提升 |
|---|---:|---:|---:|
| 一次配表LLM调用 | 23+次 | 3-5次 | -80% |
| 定位功能(N=8) | 0.6667 | 0.8056 | +20.8% |
| 字段精准率(N=8) | 0.5185 | 0.6917 | +33.4% |
| quest_npc引用一致 | 0.62 | 1.00 | 达标 |
| Skill简版定位(N=30) | 0.933 | 1.000 | +7pts |
| 占位符假阳性(N=6) | 5/6 | 2/6 | -50% |
| 嵌套占位符漏检(N=8) | 7 | 0 | 归零 |
| 聊天噪音占比(N=25) | 66.7% | 0% | 归零 |
- 收束句：从Step1到本轮优化，主线是"少问模型、先校验后执行、失败可恢复、每一环不说假话"。

## P25b｜★NEW｜核心代码实现展示①：Excel-Agent关键算法　版式：H'（代码卡片×3）

> ⚠️**为什么加这页**：答辩评分要求「核心部分——最能体现技术含量的工作详解，如核心功能的具体实现、必要的代码展示」。前面 P6-P25 全是架构图/表格/指标，**没有一行真实代码**，扣分风险最高的就是这一条。这页把前面讲过的3个技术点还原成**可运行的真实源码片段**（非伪代码，全部标了file:line，可当场打开仓库对照）。
> 版式H'：每张代码卡片 = 代码块（8-15行，语法高亮）+ **一句话技术难点** + **对应哪页的数据**（不重复摆数字，只引用页码）。

**代码卡片1｜rapidfuzz 三算法加权评分（对应新页11）**
```python
# server/agent/excel/locator/column_matcher.py:210-221
if _HAS_RAPIDFUZZ:
    for i, h in enumerate(self.headers):
        if not h:
            continue
        h_name = _clean_header(h)
        wr = _rfz.WRatio(key, h_name)
        ts = _rfz.token_set_ratio(key, h_name)
        pr = _rfz.partial_ratio(key, h_name)
        combined = (wr * 0.5 + ts * 0.3 + pr * 0.2) / 100.0
        if combined > best_score:
            best_score, best_idx = combined, i
    source = "rapidfuzz"
```
- 技术难点：单一相似度算法各有偏科（WRatio怕长句、token_set怕词序、partial怕子串），加权组合是针对配表口语化输入实测调出来的经验权重，不是默认参数。
- 数据详见新页11：编辑距离计算 2781.54ms→192.16ms。

**代码卡片2｜占位符命名归一化（对应新页23卡片1）**
```python
# server/agent/excel/subagent/validator_agent.py:33-44
def _norm_name(s: str) -> str:
    """归一标签名：去尖括号/空白/下划线后缀/大小写，
    供 produces 标签与 consumes 占位符匹配。"""
    if not s:
        return ""
    s = str(s).strip()
    if s.startswith("<") and s.endswith(">"):
        s = s[1:-1].strip()
    s = s.split(":")[0]
    return re.sub(r"\s+", "", s).lower()
```
- 技术难点：函数本身短，但要解决的是"LLM每次命名风格都不一样"这个软性问题——不是加强校验，是放宽匹配到语义等价，思路上和"严格校验"是反直觉的取舍。
- 数据详见新页23：FORWARD_REF_BROKEN假阳性 5/6→2/6（N=6）。

**代码卡片3｜LocatorAgent L3通道离线消融开关（本轮新加，对应"分通道贡献度"追问）**
```python
# server/agent/excel/subagent/locator_agent.py（本次新加3行）
# §ablation 开关：CODEMAKER_LOCATOR_FK_OFF=1 时直接跳过 L3 关系扩展，
# 用于分通道贡献度压测对照（不改变默认行为，仅供离线评测使用）。
if os.environ.get("CODEMAKER_LOCATOR_FK_OFF") == "1":
    return []
rg = self._get_relation_graph()
```
- 技术难点：不是功能代码，是**可复现验证**能力——给4个定位通道各开一道能单独关闭的口子，才能量化出"这一层到底值多少分"（真实压测见 `bench/ppt_locator_channel_ablation.md`：关L3表级recall -4.9pts）。
- 讲法：这3行代码就是"怎么证明LocatorAgent四层通道真的都有用"这个问题的答案。

---

# 第三章 · Merge 三方合并（P26-37）

## P26｜章节封面　版式：F

大字：三、Merge 合并方案

## P27｜Merge 架构与总体流程　版式：E

- 流程图（横向7步）：`①选目录→②定位merge-base→③提取数据→④比较冲突→⑤ID重映射→⑥AI建议→⑦重算写回`
- 补充表格：

| Merge模式 | 过程 |
|---|---|
| 跨分支合并(分支) | From与To比对合并到To |
| 跨分支合并(回trunk) | 分支合回trunk |
| 目录合并 | 子目录内容合并回trunk |

## P28｜模拟生产环境：demo_svn 目录结构　版式：D

- 左栏一句话：重点是还原"共同祖先"，不是复制Git分支。
- 右栏（目录树代码块+标记表）：
```
merge/svn/demo_svn/
├── repo/  (svnadmin仓库)
└── wc/
    ├── trunk/ (74张+subdev_1/2/3)
    └── branches/ (dev1-4)
```

| 情况 | 标记 |
|---|---|
| 正常切分支 | inferred=false |
| 新建 | inferred=true |

## P29｜SVN merge-base 定位　版式：E

- 流程图（3步）：`①各自找copyfrom → ②上溯祖先链 → ③交叉找LCA`
- 优化链表（A→E）：

| 版本 | 方法 | 性能提升 |
|---|---|---|
| A | 三重循环 | 10w行×100列会卡死 |
| B | 哈希row_idx_maps | O(n)→O(1) |
| C | sparse稀疏化 | 内存GB→MB |
| D | numpy向量化 | 提升10-50倍 |
| E | 公式/批注回退逐格 | 保正确性 |

- 指标框（单文件/单目录实测）：SVN rev查询 191s→0.2s

## P30｜Compare 全流程：四层比较 + 稀疏化向量化　版式：D

- 左栏（四层）：文件层/Sheet层/行列层/语义层
- 右栏（技术表）：

| 技术 | 效果 |
|---|---|
| 哈希索引 | 行定位O(1) |
| sparse diff | 只保留变化格 |
| numpy向量化 | 批量比较 |
| 公式/批注回退 | 保证正确性 |

- 指标框（标注：基准表3000行×150列/大表定义见P7口径页）：

| 指标 | before | after |
|---|---:|---:|
| 大表compare | 48s | 6.4s |
| 10万行全链 | 卡死 | 10.4s |
| 提交数据量 | 46.4MB | 0.1MB |

## P31｜ID 重映射与引用完整性　版式：C

- 主体（要点）：
  - 同主键多分支新增冲突时，为后来者分配新ID
  - 同步更新引用列，避免悬空外键
  - 与Excel-Agent思路一致：先建契约再替换
- 指标框：无独立数字（⚠️审查发现空白点，属于机制页，可考虑用ID冲突数/引用断裂数做补充，暂无数据不编）

## P32｜公式缓存保护　版式：D

- 左栏（4步流程）：`①无公式走快速路径 → ②保存前后快照比对 → ③changed不修复 → ④lost调LibreOffice重算`
- 右栏（保护设置）：
  - 60s超时上限
  - 文件互斥锁
  - 重算先输出临时目录再替换源文件
- 指标框（10w行单文件实测）：公式检测 6.1s(openpyxl)→0.05s(zip扫描)

## P33｜公式缓存与 fast-apply　版式：D

- 左栏（冲突解决）：
  - 缓存值不一致但公式一致→不冲突，标formula_changed
  - 公式写法不一致→冲突，界面比对选择
  - 用户选择后标formula_resolved，apply阶段写回
- 右栏（fast_apply原理）：zip+lxml直改XML，不触碰公式单元格
- 指标框（big_data单表实测）：

| 指标 | before | after |
|---|---:|---:|
| apply处理 | 19.6s | 4.3s |
| 序列化 | 4.9s | 0.3s |

## P34｜AI 辅助冲突处理　版式：E

- 流程图（链路）：`点击AI建议→线程池运行→拼prompt→LLM返回JSON→防幻觉校验`
- 补充表格（参考因子）：

| 因子 | 作用 |
|---|---|
| SVN rev/date | 版本越新倾向采纳 |
| 列名语义 | 判断值是否合理 |
| 单元格值 | 判断是否异常 |
| 基准值 | 提供base参考 |

- 指标框（★补充：原稿缺失，已从PERF_DATA_SUMMARY补回）：

| 指标 | before | after |
|---|---:|---:|
| 冲突建议调用 | N次网络往返/每格15-45s | 1次批量LLM(约30-60s) |

## P35｜大表格并行性能优化　版式：G

| 优化项(N) | before | after |
|---|---:|---:|
| calamine读引擎(222次) | 17.5s | 0.8s |
| svn info批量预填 | 191s | 0.2s |
| ProcessPool/ThreadPool(8表) | 3.69s | 1.84s |
| fast_apply序列化(25MB) | 3.4s | 0.98s |
| 未变更表跳过 | 全74表 | 跳过大多数(依赖快照,见P37局限) |

## P36｜Merge 章节指标汇总　版式：G

| 环节 | before | after | 提升 |
|---|---:|---:|---:|
| SVN rev查询 | 191s | 0.2s | ~950× |
| 大表compare | 48s | 6.4s | 7.5× |
| apply处理 | 19.6s | 4.3s | 3.8× |
| 提交数据量 | 46.4MB | 0.1MB | -99.8% |
| 公式检测(10w行) | 6.1s | 0.05s | ~120× |
| calamine(222次) | 17.5s | 0.8s | 21.9× |
| 10万行全链 | 卡死 | 10.4s | 从不可用到可用 |
- 收束句：可信Base+稀疏diff+引用保护+安全写回，AI只做建议不做决策。

## P36b｜★NEW｜核心代码实现展示②：Merge关键算法　版式：H'（代码卡片×3）

> 同P25b理由：这是Merge侧的"必要代码展示"，覆盖前面讲的三个最有技术含量的优化点——向量化比较、公式缓存zip扫描、fast_apply XML直改。全部真实源码，标file:line。

**代码卡片1｜numpy向量化矩阵diff（对应新页30，10-50倍提速核心）**
```python
# server/engine/compare.py:312-337（节选，_compare_sheet_vectorized）
def _pk(r):
    return str(r[0]).strip() if r and r[0] is not None else ''
base_pk_map = {_pk(r): r for r in base_rows}
...
n_rows = len(all_pks)
pk_to_idx = {pk: i for i, pk in enumerate(all_pks)}
# 构建 numpy 矩阵：object dtype（兼容混合类型 None/int/str/float）
# 真向量化：预计算 _semantic_key 矩阵，后续判等改 numpy != 广播（C层tuple比较），
# 替换三层 Python 循环（每格重算两个key+函数调用开销）
matrices: Dict[str, np.ndarray] = {}
for fname in all_files:
    mat = np.full((n_rows, n_cols), None, dtype=object)
```
- 技术难点：Excel单元格是混合类型（数字/文本/公式结果/None），不能直接用numpy数值向量化；这里的做法是先按PK对齐建行索引映射，再构造`dtype=object`矩阵把"判等"降级成C层tuple广播比较，绕开Python逐格循环，同时保留了混合类型兼容性。
- 数据详见新页29/30：大表compare 48s→6.4s，10万行全链 卡死→10.4s。

**代码卡片2｜公式缓存zip字节扫描（对应新页32，120倍提速核心）**
```python
# server/engine/parser.py:122-139（完整函数，_has_formula_part）
def _has_formula_part(file_path: str) -> bool:
    """zip 字节快扫探测 xlsx 是否含任何公式单元格（<f> 标签）。
    10w 行文件 ~0.05s vs openpyxl 全量 load ~6s，公式标签不可能出现在
    转义后的文本值里（文本 `&lt;f`），无假阳性。扫描失败保守返回 True。"""
    try:
        import zipfile
        with zipfile.ZipFile(file_path) as zf:
            for name in zf.namelist():
                if name.startswith("xl/worksheets/") and name.endswith(".xml"):
                    data = zf.read(name)
                    if b"<f>" in data or b"<f " in data or b"<f/>" in data:
                        return True
            return False
    except Exception:
        return True
```
- 技术难点：xlsx本质是zip包，不用openpyxl把整张表反序列化成对象模型，直接在zip里按字节找`<f>`标签——关键是想清楚"公式标签不会出现在转义后的文本值里"这个前提，才敢做字节级判断而不怕假阳性；扫描失败时保守返回True（宁可多走一次完整读取，也不能漏判公式导致缓存损坏）。
- 数据详见新页31/32：公式检测 6.1s(openpyxl)→0.05s(zip扫描)。

**代码卡片3｜fast_apply XML直改（对应新页33，避免大表全量重写）**
```python
# server/engine/fast_apply.py:434-450（节选，fast_apply_xml）
with zipfile.ZipFile(src) as zf:
    sheet_map = _sheet_map(zf)
    shared = _load_shared_strings(zf)
    new_entries: Dict[str, bytes] = {}
    for sheet_name, key in edits.items():
        xml_path = sheet_map.get(sheet_name)
        new_xml = _edit_xml(zf.read(xml_path), key, shared)
        new_entries[xml_path] = new_xml
with zipfile.ZipFile(src) as zf, zipfile.ZipFile(dest, "w", zipfile.ZIP_DEFLATED) as out:
    for item in zf.infolist():
        data = new_entries.get(item.filename, zf.read(item.filename))
        out.writestr(item, data)
```
- 技术难点：只重写"真正被改动的sheet对应的那段XML"，其余zip entry原样`zf.read(item.filename)`直接搬运，不经过openpyxl的对象化/序列化往返——这是"改哪块拆哪块"而不是"整包重建"的思路，代价是不能碰公式单元格（外层`_eligible`已做前置排除）。
- 数据详见新页33：apply处理 19.6s→4.3s，序列化 4.9s→0.3s。

## P37｜★NEW｜Merge 已知局限　版式：C

> 🔴必改项：与P38 Agent局限页对称，防"听起来太完美"追问。

| 局限 | 说明 |
|---|---|
| 大表压测准确率仅50% | 10k行表set操作语义层偏差，已暴露待优化 |
| ProcessPool真并行未在生产开启 | worker需完全隔离才可启，当前回退ThreadPool，独立样例验证2.0x |
| 未变更表跳过命中率依赖快照 | 历史快照64/74跳过；09-01复跑rev全不同未触发(1.06x)，不可跨快照横比 |

---

# 第四章 · 验证、展望与总结（P38-41）

## P38｜Excel-Agent 当前验证结论　版式：C

- 主体（要点）：
  - 核心回归通过71项；此前相关agent回归通过109项
  - 正向价值：enum resolver、schema-aware parse修复、locator调整都在减少幻觉
  - 剩余风险：跨段代词、省略主语、枚举口语化、资源缺必填字段

## P39｜（可选）展望：规划中的创新点　版式：G

> 时间紧可跳过，仅口头提及；标注"设计完成，量化数据待补"。

| 创新点 | 数据现状 |
|---|---|
| Replan-on-failure | 已落地+20单测，e2e阻塞于外部serve环境 |
| 编号账本跨分支查重 | 对应P3痛点#2，单测验证，无真实多分支压测数字 |
| BM25语义召回(RAG) | 已并入P10"L4 RAG"，暂无召回率A/B数据 |
| Schema血缘联动 | 定性能力(发现问题)，非性能类指标 |

## P40｜总结　版式：C

- 主体（要点）：
  - Excel-Agent："LLM理解+schema编译+规则校验+确定执行"
  - Merge："可信Base+稀疏diff+引用保护+安全写回"
  - 本轮验证方法论：用可复现的模拟压测代替"看起来应该没问题"
  - 后续重点：全字段交互修正、复杂链路上下文继承、经验沉淀、更多端到端回归

## P41｜结束页　版式：A

**THANK YOU**

---

# 页数与🔴必改项落点速查

| 🔴必改项 | 落点页 |
|---|---|
| 补数据口径说明 | P7（新增） |
| 补Step1耗时/ok率代价 | P9（指标框改两行为四行） |
| 全篇百分比补N标注 | 已在P9/P12/P13/P20/P22/P23/P24/P25等所有指标框标出 |
| 补Merge已知局限对称页 | P37（新增） |
| 补"核心功能具体实现+必要代码展示"（答辩评分Part3硬要求） | P25b（Agent侧3段真代码）+ P36b（Merge侧3段真代码）（新增） |

原第37-43页模板说明/图表配色示例页（非内容页）仍可保留在文件末尾作备用版式，不占正文页码。
