# Excel-Agent × Merge 答辩 PPT 撰写文档

> **用途**：供撰写《Excel-Agent 4-Step + 三方合并 答辩》PPT 使用。受众为**技术 / 策划 / HR 三方混听**，故每页均给出"讲给谁"的口径。
> **素材来源**：桌面历史迭代版本（`project`/`ll`/`jj`/`test`/`pre`/`curr`/`demo`/`111`）+ 当前版 `Excel-Agent-And-Merge` + 桌面《性能优化指标整合文档.md》《优化全过程.md》。
> **写作原则**：两大引擎（**4-Step Agent 改表** 与 **Merge 合并**）各按 **①时间线铆钉 ②技术逻辑 ③创新点 ④难点 ⑤性能优化（改动 + 前后指标对比）** 五段组织；所有性能提升必须"有改动、有 before/after 数字、标注证据集"。
> **铁律 1**：不同样例集不可横比，所有"提升%"均为**同集 before→after**。
> **铁律 2（专业性）**：技术逻辑页保留真实术语与算法名（LCA 反查 / 语义归一 / 向量化 / 稀疏化 / 拓扑排序 / 占位符代换），不稀释。
> **铁律 3（普适性）**：每章末尾必给"非技术类比"一行——用生活比喻让策划/HR 听懂同一件事，两套口径并行不悖。

---

## 〇、总览页（PPT 第 1-2 页）

### 一页讲清：这是什么

- **一句话**：一套 **"双引擎"配表工程系统**——引擎一用 AI 把自然语言变成表格改动（4-Step Agent），引擎二把多人分支上的表格改动安全合并回主干（Merge）。
- **为什么要一起讲**：这两个引擎共享同一批性能武器（零 LLM 化、缓存、向量化、并行、事前预防），同一套质量理念（先校验后动手、失败可回滚）。分开看是两条线，合起来是"**改表 + 合表**"的完整闭环——这正是项目名 `Excel-Agent-And-Merge` 的含义。

### 一页讲清：核心数字（贴给全场看）

**Agent 侧**

| 指标 | 早期基线 | 终态（当前版） | 变化 |
|---|---|---|---|
| 一次配表 LLM 调用 | 6 步线性 23+ 次 | 4-Step **3-5 次** | **-80%** |
| 定位 10 万行表 | 全表扫描 6.8s | 索引 0.58s | **11.7×** |
| 定位准确率（S-A 8 条） | 66.7% | 80.6% | +13.9pts |
| 字段精准率 | 51.9% | 69.2% | +17.3pts |
| quest_npc 引用一致 | 6% | **100%** | 达标 |
| skill 定位成功率（30 样例） | 93.3% | **100%** | +6.7pts |
| skill 平均耗时 | 533ms | 321ms | 减半 |
| 复杂级联用例 106 耗时 | 204s | 112s | **-45%** |
| 单测 | 145 | 323+ | 零回归 |

**Merge 侧**

| 指标 | 早期基线 | 终态 | 变化 |
|---|---|---|---|
| 大表 compare（全表） | ~48s | ~6.4s | **7.5×** |
| apply（big_data 单表） | ~19.6s | ~4.3s | **3.8×** |
| 提交数据量 | 46.4MB | 0.1MB | **-99.8%** |
| subdir_compare | 21.8s | 9.3s | **2.33×** |
| branch_compare（74 表） | 99.5s | 89.5s | 1.11× |
| 公式快扫（10 万行） | ~6s | ~0.05s | **~120×** |
| 假冲突率 | 0.5 | 0 | **归零** |
| 假删除误报 | 69 | 0 | **归零** |
| 未变更表跳过 | 全表重比 | 86% 表跳过 | 只算有变化的 |
| ProcessPool 并行（8 表） | 3.69s | 1.84s | **2.0×** |

> ⚠️ PPT 口径提示：Agent 的 `<90s` 为**架构设计目标**，复杂跨表实测受外部 `codemaker serve` 阻塞（143.8k token/156s，非本 agent 代码可修）。**技术场讲实测（单表 29.9s/33.5s/9.2s OK），策划/HR 场讲趋势与目标，勿混。** Merge 的 7.5×/2.33×/120× 均为**已落地实测**，可放心全场讲。
> 证据：`docs/archive/优化全过程.md` + `merge/scripts/benchmark_report.md` + `server/tests/reports/` + `bench/bench_iter_results.md`

---

## 一、时间线总铆钉页（PPT 第 3-4 页）

> 把"历史迭代版本文件夹"钉到时间轴上，让 HR 一眼看懂演进脉络。两条引擎线并行标注。

| 时间 | 桌面文件夹 | Agent 形态 | Merge 形态 | 标志性事件 | 关键指标（锚） |
|---|---|---|---|---|---|
| 07-23 | `ll` | 单 agent（扁平 agent.py） | — | 全功能分类基线 | 准确率 89.47%，级联删除 **0%** |
| 07-24/28 | `project` | 单 agent | **快照版**（`_meta.json` 反查） | 大表 CRUD + 索引 + merge 基线 | index_locate **11.7x**、merge 自动合并率 **0%** |
| 07-30 | `test` | 单 agent + intent_scheduler | 快照版压测 | 真实压测 | 并发写 p50=9.38s、merge 3000 行约 18s |
| 08-03 | `jj` | 单 agent + operation_orchestrator | **引入真 SVN merge-base 反查** | 跨分支合并上真 SVN | skill on 早期**负收益**（locate -0.024） |
| 08-10/11 | `pre`/`curr`/`demo`/`111` | **单 agent 内 6 步线性**（core 分层） | **SVN 版优化 R3/R6** | R1-R9 全量优化 + merge 路由/引擎提速 | Agent 定位 +20.8%、merge subdir **2.33x**、假冲突 0.5→0 |
| 08-17/18 | `testtest` → 并入当前版 | **4 子智能体（V2）并行** | SVN 版 + AI 智能合并 | 4-Step 重构 + merge 联通 | LLM 23+→3-5 次（-80%） |
| 08-17+ | `Excel-Agent-And-Merge` | 4-Step V2 终态 | 双引擎合流 | 当前版 | Agent 6/6 样例通过；Merge compare **7.5×** |

**演进主线一句话**：Agent 从"扁平单 agent → 单 agent 内 6 步线性 → 4 个职责隔离的子智能体"，LLM 从 23+ 次压到 3-5 次；Merge 从"文件夹快照模拟 → 真 SVN LCA 反查 → 向量化 + AI 智能建议"，大表比对提速 7.5×。**两条线最终在一套系统里合流。**

---

## 二、Step 1 · Parse（解析）

> 对应 PPT 第 5-7 页。讲给**技术**：schema 驱动 + 零 LLM 主路径；讲给**策划**：你的自然语言指令怎么被拆成机器能懂的步骤；讲给 **HR**：大模型不再"拍脑袋",而是对着真实表结构干活。
> **非技术类比**：像拆解"帮我办三件事"的指令——先把三件事分开，再给每件事查字典（表结构），最后写成一步步的作业单。字典是真实存在的，AI 不能自己编词。

### 2.1 时间线铆钉

| 时间 | 版本 | 本 Step 形态 | 关键变化 |
|---|---|---|---|
| 07-28 | `project` | 全表扫描定位 | 引入索引，index_locate **11.7x** |
| 08-10 | `pre` R1 | 规则分类 + 值语义门 | 定位功能 **+20.8%**、覆盖 **+22.7%**、精准 **+33.4%**（S-A 集） |
| 08-13 | `pre` R8b | produces 推断层（关系图驱动） | quest_npc 引用一致 0.94 |
| 08-17 | 4-Step V2 | **ParseAgent 独立**：规则分段(0 LLM) → 粗路由(0 LLM) → schema-driven LLM 拆分 | 段级并发 + deadline 60s |

### 2.2 技术逻辑

输入 `user_text`（自然语言）→ 输出 `intents`（结构化指令数组）：

1. **`split_multi_intent` 规则分段**（0 LLM）：一句指令含多个意图时按规则切段，每段独立处理 → 段小、prompt 短、单段 LLM 更可靠。
2. **`LocatorAgent.locate` 粗路由**（0 LLM 主路径）：先按 stem 匹配候选表，按外键扩表；只有歧义时才 1 次 LLM 裁决。
3. **`DecomposeAgent.decompose`（LLM 拆分）**：把真实表结构（schema）注入 prompt，让 LLM 产出 JSON 数组 `SplitIntent`，每元素含 `{table, sheet, action, fields, produces, consumes}`；**列名必须是真实存在的列**（杜绝幻觉列）。
4. **`produces_inference`（0 LLM）**：关系图驱动推断每段"产出/消费"哪些主键，供 Step3 占位符代换。
5. **段级覆盖率对账**：每段 ≥1 意图，0 条段重跑（便宜），仍空则报 StepError（soft）。

### 2.3 创新点

- **多意图分段**：一段一次 LLM，小 prompt 小上下文 → 快 + 稳 + 便宜。
- **schema-driven（表结构驱动）**：LLM 只从真实列名里选，从源头掐死"幻觉列名"（历史上 `column_not_found` 是早期最大失败根因）。
- **零 LLM 主路径**：分段、粗路由、produces 推断全部规则化，LLM 只做"拆分"这一件最需要语义理解的事。
- **produces/consumes 标注**：为 Step3 的跨表占位符代换提前建立数据契约。

### 2.4 难点与解法

| 难点 | 解法 |
|---|---|
| 长指令多意图切分边界模糊 | 规则分段 + 段级对账兜底，空段重跑 |
| 多段反复拉取表头 I/O 开销大 | `(stem,sheet)→(headers)` 内存缓存 + 懒加载（省 60-70% I/O） |
| 跨表 produces/consumes 链在 12+ 表时断裂 | `force_single` 单 prompt 全候选一起看 + 按 FK 链分组（每组 ≤6 表） |
| 单段超时拖垮整体 | 全局 deadline 60s，超时冻结降级到 0 LLM 模板拆分 |

### 2.5 性能优化（改动 + 前后指标）

| 改动 | before | after | 指标 |
|---|---|---|---|
| **schema 内存缓存** `(stem,sheet)` → headers | 每段重复拉表头 I/O | 缓存命中 | **省 60-70% I/O** |
| **O1 单 prompt 主路径**（候选 ≤3 时 N→1 合并） | 每候选表一次 LLM（N 次） | 1 次 | **token 砍 50-70%** |
| **段级并发** `CODEMAKER_PARSE_SEGMENT_CONCURRENCY=1` | 顺序分段 | 并发 | 并行墙钟下降（早期并行=2 全 FAIL → 串行=1 全 OK，见下警示） |
| **索引定位**（07-28 起） | 全表扫描 6.8s | 索引 0.58s | **11.7x**（L-0728） |
| **R1 值语义门 + 级联** | 定位 0.667 | 定位 0.806 | **+20.8%**（S-A 集） |

> ⚠️ 并行警示（技术场必讲）：早期 **parallel=2/3 几乎全 FAIL**，当前 **parallel=1 全 OK**。结论是**串行稳定性显著优于并行**——因为 LLM 服务端（codemaker serve）是共享阻塞资源。这是踩坑后的真话，比"我们上并行所以快"更可信。
> 证据：`bench/bench_iter_results.md`（6/6 OK）+ `bench_myrun.md`（早期全 FAIL）

---

## 三、Step 2 · Validate（校验）

> 对应 PPT 第 8-9 页。讲给**技术**：零 LLM 规则闸是省钱核心；讲给**策划**：写错的字段在动手前就被拦下；讲给 **HR**：质量门禁前置，避免改错返工。
> **非技术类比**：填表前先有"检查员"照着规则清单逐项核对——列名存在吗、类型对吗、必填了吗、重复了吗、值在允许范围吗。**大部分问题在动手前就发现，不用等做完了返工。**

### 3.1 时间线铆钉

| 时间 | 版本 | 本 Step 形态 | 关键变化 |
|---|---|---|---|
| 07-23 | `ll` | 无独立校验，运行时才暴露 | 级联删除 0% 暴露校验缺失 |
| 08-03 | `jj` | skill 早期接入 | 负收益（locate -0.024）→ 证明需要系统性重构 |
| 08-10 | `pre` R1 | 值语义门（#2）+ SET PK 级联（#1） | 精准 +33.4%、严格通过 +100%（S-A） |
| 08-17 | 4-Step V2 | **ValidateAgent 独立**：6 项字段校验零 LLM + FK 拓扑层 | 硬/软错误分级，可恢复问题不拖垮流水线 |

### 3.2 技术逻辑

输入 Step1 的 `intents` → 输出修正后的 `validated` intents：

1. **`_structural_errors` cheap guard（0 LLM）**：先查 action/table/fields 结构是否合法，不合法直接返回。
2. **字段层 6 项并行校验（零 LLM）**：①列存在 ②类型 coerce ③必填 ④唯一 ⑤枚举白名单 ⑥范围分布。
3. **FK 拓扑层**：跨表外键一致性（`#19` opt-in LLM 前向引用裁决）。
4. **硬/软错误分级**：`unique_violation / type_mismatch / col_not_found` + 主键列缺失的 `MISSING_REQUIRED` 为 hard（`_HARD_ISSUE_TYPES`，validator_agent.py:240）；`enum_invalid` 等**非阻断**（保留 warning，可交互 ask，不硬跳过）→ 可恢复问题不拖垮整条流水线。
5. **ask_user 交互反问**：需要人工确认时挂起问用户，O3 后降级为纯展示非阻断。

### 3.3 创新点

- **L0/L1/L2 分层校验漏斗**：L0 规则闸（零 LLM，覆盖 80% 关切）→ L1 AI 判官（1 次 LLM）→ L2 人工 gate。**大部分错误在 L0 就被拦截，LLM 只在最难的语义裁决上场**。
- **硬/软错误分级**：不因一条可恢复问题全盘失败（早期行为），改为"记 warning 继续 + 交互纠正"。
- **零 LLM 校验路径**：6 项字段规则全部代码化，替代早期反复用 LLM 校验。

### 3.4 难点与解法

| 难点 | 解法 |
|---|---|
| 字段类型自动推断误判 | coerce 失败归入 soft，交互确认 |
| 跨表 FK 校验依赖外部表 | FK 拓扑层 opt-in LLM 前向引用裁决 |
| 交互反问打断自动化流程 | O3 后降级为"展示非阻断"，保流畅 |
| 早期校验缺失导致级联删除全挂 | 级联链路专项修复（07-23 0% → 后续全对） |

### 3.5 性能优化（改动 + 前后指标）

| 改动 | before | after | 指标 |
|---|---|---|---|
| **6 项字段校验代码化（零 LLM）** | 校验靠 LLM 往返 | L0 规则闸直接判 | **80% 错误零 LLM 拦截**（L0 覆盖） |
| **R1 值语义门（#2）** | 不合规值写入 | 写前拦截 | 精准 0.519→0.692（**+33.4%**，S-A）；ok 率下降是"拒绝错数据"的预期代价 |
| **R1 SET PK 级联（#1）** | 级联改引用行不联动 | 联动更新 | 严格通过 0.167→0.333（**+100%**，S-A） |
| **R2 类型校验加固** | type_mismatch 2 例 | 1 例 | **-1 例**（S-A，确定性证据链） |

---

## 四、Step 3 · Execute（执行）

> 对应 PPT 第 10-11 页。本 Step 是**全流程最硬核的优化点**：坚决零 LLM。讲给技术：拓扑排序 + 占位符代换；讲给策划：跨表联动按正确顺序执行；讲给 HR：执行环节不再"烧钱问大模型"。
> **非技术类比**：像流水线装配——"先造零件 A，再造要用到 A 的零件 B"。系统知道每件事的先后依赖，自动排好顺序，绝不乱序导致"引用了还没造出来的东西"。

### 4.1 时间线铆钉

| 时间 | 版本 | 本 Step 形态 | 关键变化 |
|---|---|---|---|
| 07-28 | `project` | 顺序执行 + 每步 LLM | merge 自动合并率 0%（手动 818） |
| 08-11 | `pre` R4 | 跨表模板 + 值容错 + verify 降级 | 用例 106 耗时 204→112s（**-45%**，S-B） |
| 08-11 | `pre` R6 | 引擎层向量化 + 语义归一 | compare O(n²)→向量化，假冲突 0.5→0 |
| 08-17 | 4-Step V2 | **ExecuteAgent 独立：拓扑排序 + 占位符代换，零 LLM** | LLM 往返 5→2-3 次（D4） |

### 4.2 技术逻辑

输入 Step2 的 `validated` intents → 输出 `results` + `failures`：

1. **拓扑排序**：复用 `OperationOrchestrator._topo_order`，按 produces/consumes 依赖链给子任务排序（**producer 先于 consumer**），保证跨表写入顺序正确。
2. **占位符字面代换**：`_resolve_placeholders` 把 consumes 里的 `<label>` 替换为 producer 实际产出的真实 PK（显式 PK 字面代换，R8c）。
3. **孤儿行防护（P0）**：`_find_unresolved_placeholders` 执行前扫描悬空非 `<auto>` 占位符，跳过写入 → 不生成缺外键的残缺行。
4. **原子 API 派发**：add 用 R23 二段提交、modify 用 R22 批量原子，写失败可回滚。
5. **零 LLM 硬约束（D4）**：`no_llm=True` 经 thread-local（`execute_no_llm`）短路一切越界 LLM 路径，try/finally 还原。

### 4.3 创新点

- **Step3 坚决零 LLM（D4 决策）**：执行是确定性工程，不该依赖概率模型。这是 4-Step 相比 6 步线性最大的架构决定。
- **thread-local 短路机制**：早期用 `os.environ["CODEMAKER_EXECUTE_NO_LLM"]` 进程级突变会污染并发请求并触发 P19；改为参数透传 + thread-local（try/finally）——**隔离、不污染、可并发**。
- **拓扑 + 占位符**：把"跨表依赖"从 LLM 记忆里搬到确定性算法里。

### 4.4 难点与解法

| 难点 | 解法 |
|---|---|
| 跨表占位符链解析（6 表） | 拓扑排序保证 producer 先执行 + 字面代换 |
| 并发请求共享进程状态被污染（P19） | os.environ → thread-local 隔离 |
| 孤儿行（producer 失败导致 consumer 缺外键） | 执行前悬空占位符扫描 + 跳过写入 |
| 写入失败留半成品 | 二段提交 / 批量原子 + 回滚 |

### 4.5 性能优化（改动 + 前后指标）

| 改动 | before | after | 指标 |
|---|---|---|---|
| **Step3 零 LLM（D4）** | 执行每步问 LLM | 纯代码派发 | 成功路径 **LLM 往返 5→2-3 次**（与 Step1/4 合并口径） |
| **LLM 调用三合一合并**（`ai_confirm_table + ai_plan_operation + ai_validate_plan` → 1 次） | 3 次往返 | 1 次 | 全链 LLM 5→2-3 次（openspec D4） |
| **引擎向量化**（R6，compare O(n²)→numpy） | list.index() O(n²) 10w 行卡死 | 矩阵级向量化 | 10w 行 compare+resolve+apply ≈10.4s |
| **R4 值容错 + 跨表模板** | 用例 106 覆盖 0.16 | 峰值 0.39 | **+143%**（S-B） |
| **R4 编号保护 + 跨表模板** | 用例 106 耗时 204s | 112s | **-45%**（S-B） |
| **R6 未变更表跳过** | 全表重比 | rev==base 早返回 | demo_svn **1.10x**（64/74 跳过） |

> 实测锚点（4-Step 单表单表子集，`bench_v1.json`）：新增灵兽朱雀 **29.9s** OK、新增瞭望塔（复合） **33.5s** OK、QA **9.2s** OK。→ 给策划/HR 展示"单表/复合操作秒级~半分钟完成"。

---

## 五、Step 4 · Conclude（汇总 + 自学习）

> 对应 PPT 第 12 页。讲给**技术**：反模式自学习闭环；讲给**策划**：每次失败都会被记住，下次同类问题更少；讲给 **HR**：系统会自我进化，不是一次性交付。
> **非技术类比**：像老师批改作业后记错题本——每次失败都被记下来，总结出"这类题容易错在哪"，下次还没做就提前提醒。**错得越来越少。**

### 5.1 时间线铆钉

| 时间 | 版本 | 本 Step 形态 | 关键变化 |
|---|---|---|---|
| 07-23 | `ll` | 失败信息黑洞，无结构化 | 失败不可复盘 |
| 08-10 | `pre` R2 | skill AI 归纳（#6） | 从 8 条失败 trace 归纳 4 反模式 |
| 08-17 | 4-Step V2 | **ConcludeAgent 独立**：#40 结构化失败清单 + `induce_anti_patterns` 门控自学习 | 失败可复盘、可沉淀、防回归 |

### 5.2 技术逻辑

输入 Step3 的 `subtasks` + `failures` → 输出 `summary`：

1. **无 LLM 模板汇总**：成功路径走模板拼接（"完成 X 个子任务"），不烧 LLM。
2. **结构化失败清单（#40）**：failures 汇总成面向用户的结构化清单（含 step_id / 错误类型 / 根因 / 建议），**不再黑洞吞**。
3. **反模式自学习**：failures 非空 + skill 开启 → `induce_anti_patterns`（1 次 LLM，失败降级）→ `_collect_failed_traces` + `_induce_anti_patterns_via` → 写入 `anti_patterns.yaml`。
4. **门控提升**：新反模式先入 `pending_review`，经 `promote_with_guard` + mini_regression 门控后才 `active` → 防错误模式污染。

### 5.3 创新点

- **失败清单结构化**：从"失败被吞掉"到"每条失败可定位、可复盘、可给修复建议"（对标 #40）。
- **反模式自学习闭环**：系统从自己的失败中沉淀规则，下次同类错误在 Parse/Validate 阶段就被拦截——**错误越跑越少**。
- **门控 promote**：pending → active 需过 mini_regression，杜绝"学坏"。

### 5.4 难点与解法

| 难点 | 解法 |
|---|---|
| LLM 归纳反模式质量不稳 | 失败降级为模板汇总 + 门控再审查 |
| 学坏的反模式污染规则库 | pending_review → mini_regression → active 三段门控 |
| 失败信息太散不可复盘 | #40 结构化清单（step_id + 根因 + 建议） |

### 5.5 性能优化（改动 + 前后指标）

| 改动 | before | after | 指标 |
|---|---|---|---|
| **无 LLM 模板汇总** | 成功路径也问 LLM 总结 | 模板拼接 | 成功路径 **0 次 LLM** 收尾 |
| **反模式归纳**（R2 #6 → 4-Step） | 失败即丢 | 沉淀进 anti_patterns.yaml | 8 条失败 trace → **4 条反模式** |
| **门控 promote** | skill 早期负收益 | mini_regression 把关 | 确定性集定位成功率恒 **1.0000**（S-G 6 轮零回归） |

---

## 六、Merge（三方合并引擎）

> 对应 PPT 第 13-17 页。**新增专章**——项目名 `And-Merge` 的另一半，与 4-Step Agent 构成"改表 + 合表"闭环。
> 讲给**技术**：LCA 反查 / 语义归一 / 向量化 / 稀疏化 / fast_apply；讲给**策划**：多个人改同一张表的改动安全合到一起；讲给 **HR**：从"人工核对几百个格子"到"机器自动判断、AI 给建议、人只做最终拍板"。
> **非技术类比**：多人同时改同一本书，merge 就是"自动找出每页哪里被谁改过、哪里改冲突了、怎么合才不丢内容"。**比人工一页页核对快 7 倍，且不会漏。**

### 6.1 时间线铆钉

| 时间 | 版本 | Merge 形态 | 关键变化 |
|---|---|---|---|
| 07-24/28 | `project` | **快照版**（文件夹 + `_meta.json` 反查） | 全链基线：自动合并率 **0%**（818 行全人工） |
| 07-30 | `test` | 快照版压测 | 3000 行 0.1 密度约 18s，0 error（正确但小表偏慢） |
| 08-03 | `jj` | **真 SVN**（copyfrom 反查 merge-base） | 引入 `svn log --stop-on-copy` + 最近公共祖先 |
| 08-11 | `pre` R3 | 路由层优化 | subdir **2.33x**、svn info 批量 ~950×、假删除 69→0 |
| 08-11 | `pre` R6 | 引擎层优化 | compare 向量化 + 语义归一（假冲突 0.5→0）+ 未变更跳过 1.10x |
| 08-17 | 当前版 | **双引擎合流 + AI 智能建议** | AI 预取/批量建议、compare **7.5x**、apply **3.8x** |

### 6.2 技术逻辑

流程主线：**compare（比对）→ resolve（解决）→ apply（写回）**，加一层 AI 建议联动：

**① compare 阶段**：
1. **merge-base 反查（SVN 核心算法）**：`_resolve_branch_point`（`svn log -v --stop-on-copy --xml` 取 copyfrom）→ `_copyfrom_chain_svn` 沿 copyfrom 上溯祖先链 → `_lca_svn` 两链交叉求最近公共祖先（LCA）。无 copyfrom 纯新建时降级"首次 rev-1"（`inferred=true`）。
2. **语义归一 `_semantic_key`**：把 `"100"` vs `100`、`"a "` vs `"a"`、`0.1` vs `"0.10"` 统一成可比较键 `('num',float)/('str',s)/('bool',v)`——**消除"看起来不同其实相同"的假冲突**。
3. **向量化快速路径**：纯数据表走 numpy 矩阵 diff（`_semantic_key` 矩阵 `!=` 广播比较），替换三层 Python 逐格循环 → 10-50×。
4. **稀疏化**：全等行只保留主键格、versions 置空——全表物化从 GB 级降到 MB 级。
5. **哈希索引**：预建 `{fname:{id(row):index}}`，替换 `list.index` 线性扫描（10w×100 列 500 亿次 → 卡死）。

**② resolve 阶段**：
6. **6 策略自动合并**（`base_priority` / `take_newer` / `take_longest` / `take_max` / `range_check` / `manual`）+ 类型校验兜底（等级列写 string 自动降人工）+ 多数表决推荐（manual 未配置时）。
7. **ID 重映射** `id_resolver`：多分支同主键新增行先到先得 + 后到者 max+1，映射表带分支标记同步外键。
8. **引用完整性** `ref_integrity`：外键悬空检测 + 重映射同步。

**③ apply 阶段**：
9. **fast_apply XML 直改**：10w 行表跳过 openpyxl 全量 load+save，改用 zip+lxml 只改差异单元格、纯追加免整表重排；不满足条件回退 openpyxl。
10. **回滚记账** `_rollback_written`：写入前每表备份到 tmp，失败恢复/删除/rmtree 半成品版本目录。
11. **precommit_hold 漏行预检**：`preflight_row_manifest` 算 `base_ids - mergeset_ids`，命中漏行可阻断 + audit 留痕（R9-B1，漏行命中率 100%）。

**④ AI 联动层（与 4-Step Agent 的联通点）**：
12. **AI 建议后台预取** `_prefetch_ai_suggestions`：compare 返回后线程池预取**冲突最多前 5 sheet** 的 AI 建议填缓存（fire-and-forget，异常全吞不阻塞主流程）。
13. **批量 LLM 建议** `suggest_merge_batch`：一次 LLM 调用覆盖全部冲突格（秒级），将 N 次网络往返（每格 15-45s）压为 1 次（约 30-60s）；失败回退逐格并行（≤8 worker）。
14. **AI 语义校验兜底**：AI 建议不得回退成原冲突值。
15. **设计边界（关键创新）**：**merge 核心策略纯代码不 AI 化**——AI 只进"冲突解决建议"这条生成路径，不进"列策略匹配"这条确定性路径。理由：规则确定性更可靠。

### 6.3 创新点（merge 专章）

1. **SVN 无 merge-base 命令的 LCA 反查算法**（copyfrom 链交叉）——解决 SVN 跨分支三方合并的基座难题，是整条 merge 线的算法地基。
2. **语义相等归一 `_semantic_key`**：三方判定与多数表决共用同一把尺，假冲突率 0.5→0。
3. **向量化 numpy 矩阵 diff**：纯数据大表 10-50× 提速。
4. **稀疏化全等行**：10w 行表内存/JSON 从 GB 级降到 MB 级，是"10w 行秒级"的关键。
5. **表级 ProcessPool + sheet 级 ThreadPool 两级并行**（GIL 认知正确：纯 CPU 用进程、IO 段用线程）。
6. **fast_apply XML 直改**：绕开 openpyxl 全量 load+save，只改差异单元格 + 纯追加免整表重排。
7. **AI 建议批量 LLM + 后台预取**：一次 LLM 覆盖全部冲突格 + 冲突最多前 5 sheet 预取，压缩延迟同时控成本。
8. **precommit_hold 事前拦截**：从"事后发现漏行"变"事前预防"，防静默丢数据。

### 6.4 难点与解法（merge 专章）

| 难点 | 解法 |
|---|---|
| SVN 无内置 merge-base 命令 | `--stop-on-copy` + copyfrom 链交叉求 LCA；纯新建降级"首次 rev-1" |
| 10w 行大表比对卡死 | 向量化 + 稀疏化 + 哈希索引三管齐下 |
| Windows spawn 死锁（ProcessPool 子进程 import LLM client） | worker 模块隔离 + 局部 import + 失败自动回退 ThreadPool |
| "同作者多次修改"语义 | 文件名 `_数字.xlsx` 后缀解析版本号 + other_files 顺序兜底 |
| 多分支同主键 ID 撞车 | 先到先得重映射 + 带分支标记 `(file,old_pk)` 外键同步 |
| 假冲突 / 假删除 | 语义归一（100 vs "100"）；`src_scope_only=True`（假 source_deleted 69→0） |

### 6.5 性能优化（改动 + 前后指标，merge 专章）

| 改动 | before | after | 指标 |
|---|---|---|---|
| **compare 稀疏化 + calamine + 序列化** | 全表物化 GB 级 | 只物化变动行 | **compare 48s→6.4s（7.5×）** |
| **apply fast_apply XML 直改** | 全量 load+save | 只改差异格 | **apply ~19.6s→~4.3s（3.8×）**；提交数据 **46.4MB→0.1MB（-99.8%）** |
| **路由层 calamine + svn info 批量**（R3） | openpyxl 0.3-0.5s/文件 + N 次 svn log ~190s | calamine <0.1ms + 一次 svn info ~0.2s | subdir **2.33x**（21.8→9.3s）；branch **1.11x**（99.5→89.5s）；svn 调用 **~950×** |
| **假删除修复 `src_scope_only`**（R3） | 假 source_deleted 69 | 0 | **69→0**（正确性） |
| **语义归一**（R6） | 假冲突 0.5 | 0 | **假冲突归零** |
| **未变更表跳过**（R6） | 全表重比 | rev==base 早返回 | demo_svn 74 表中 **64 表（86%）跳过** |
| **ProcessPool 两级并行** | ThreadPool | ProcessPool（≥4 表）+ sheet 级 ThreadPool | samples **~2.0×**（真并行需 worker 隔离） |
| **AI 批量建议** | N 次网络往返（每格 15-45s） | 1 次 LLM（约 30-60s） | **N 次 → 1 次**；失败回退逐格并行 |

> ⚠️ 诚实边界（技术场必讲）：① 7.5×/3.8× 来自 `merge/scripts/benchmark_report.md` 实测；② ProcessPool 真并行需 worker 模块完全隔离才可启，当前 demo_svn 仍回退 ThreadPool（卡死保护）；③ 路由层 branch 优化后 89.5s 中 ~88s 在引擎层 compare loop，非路由层范围——**分层归因，别混口径**。

---

## 七、全局性能总结页（PPT 第 18-19 页）

### 7.1 全局 LLM 预算账（一句话版）

> **Agent 侧：6 步线性 23+ 次 LLM → 4-Step 3-5 次（-80%）**。钱省在哪：Step3 执行零 LLM（原来最大头）+ Step1 分段/粗路由/关系推断零 LLM + Step2 校验规则化 + Step4 汇总模板化。**LLM 留在"语义拆分、意图分类路由、自检补漏、反模式归纳"四处需要语义/纠错的位置**，其余靠规则与确定性代码。
> **Merge 侧：AI 只进"冲突解决建议"这一条生成路径，不进"列策略匹配"确定性路径**——同一条"能省则省、能用代码绝不用 LLM"的设计哲学贯穿双引擎。

### 7.2 全部"改动 + 前后指标"速查表（答辩主表）

| # | 引擎 | 改动 | before | after | 提升 | 证据集 |
|---|---|---|---|---|---|---|
| 1 | Agent | 4-Step 重构（6步线性→4子智能体） | 23+ 次 LLM / 10+ min | 3-5 次 / <90s 目标 | **-80%** | S-F |
| 2 | Agent | 索引定位 | 全表扫描 6.8s | 0.58s | **11.7x** | L-0728 |
| 3 | Agent | R1 语义门+级联 | 定位 0.667 / 精准 0.519 | 0.806 / 0.692 | **+20.8% / +33.4%** | S-A |
| 4 | Agent | R1 严格通过 | 0.167 | 0.333 | **+100%** | S-A |
| 5 | Agent | R4 跨表模板+值容错 | 用例 106 覆盖 0.16 / 耗时 204s | 峰值 0.39 / 112s | **+143% 覆盖 / -45% 耗时** | S-B |
| 6 | Agent | R8 column_matcher | quest_npc 引用一致 0.06 | 1.00 | **达标** | S-D |
| 7 | Agent | quest_npc 覆盖/精准/耗时 | 0.82 / 0.56 / 134s | **1.00 / 0.78 / 107s** | — | S-D |
| 8 | Agent | skill 简版 30 样例 | 定位 0.933 / 严格 0.704 / 533ms | **1.000 / 0.959 / 321ms** | 定位+7pts、耗时减半 | S-G |
| 9 | Agent | skill 真写盘（8 样例） | 定位 0.472 / 覆盖 0.417 / 精准 0.497 | **0.639 / 0.583 / 0.590** | +0.167/+0.167/+0.093 | S-A |
| 10 | Agent | 反模式自学习 | 失败即丢 | 门控沉淀 | 确定性定位成功率恒 1.0000 | S-G |
| 11 | Agent | 单测体系 | 145 → 204 | 323+ | 零回归 | 累计 |
| 12 | Agent | 大表全链（10 万行） | 卡死 | ~10.4s | compare 3.9s+apply 6.3s | S-H |
| 13 | Merge | compare 稀疏化+calamine | ~48s | ~6.4s | **7.5×** | benchmark_report |
| 14 | Merge | apply fast_apply XML | ~19.6s / 46.4MB | ~4.3s / 0.1MB | **3.8× / -99.8%** | benchmark_report |
| 15 | Merge | compare 序列化 | 4.9s | 0.3s | **16×** | benchmark_report |
| 16 | Merge | 路由层 calamine + svn info 批量 | subdir 21.8s / branch 99.5s | 9.3s / 89.5s | **2.33x / 1.11x**（svn 调用 ~950×） | M-svn |
| 17 | Merge | 假删除修复 | 假 source_deleted 69 | 0 | **69→0** | M-svn |
| 18 | Merge | 语义归一 | 假冲突 0.5 | 0 | **归零** | M-svn |
| 19 | Merge | 未变更表跳过 | 全表重比 | 64/74 跳过 | **86% 表跳过（1.10x）** | M-svn |
| 20 | Merge | ProcessPool 并行（8表 3000×150） | ThreadPool 3.69s | 1.84s | **2.0x** | R6 #32 |
| 21 | Merge | AI 批量建议 | N 次往返/每格 15-45s | 1 次 LLM | **N→1 次** | agent_service |
| 22 | Merge | 公式快扫（10 万行） | openpyxl 全量 ~6s | zip 扫描 ~0.05s | **~120×** | R9-A1 |
| 23 | Merge | 分阶段复算 S0→S3（compare 处理） | 31.4s | 7.3s | **4.3×** | benchmark_report |
| 24 | Merge | 分阶段复算（big_data apply） | 19.2s | 4.2s | **4.6×** | benchmark_report |
| 25 | Merge | 大表操作压测（get/set 10k） | — | get 31.2s / set 332s | 成功率 100%、准确率 50% | bench_perf_tables |

> ⚠️ 已用脚本复跑（2026-08-31）：`bench_merge_before_after.py` 新鲜出数 **before 62.1s → after 58.6s（1.06x）**。注意：当前 demo_svn 快照 trunk/dev1 全部 rev 不同 → `no_change_tables=0`，#33 未命中；历史存档（08-11）当时 rev 全等的 64/74 表跳过（1.10x）——**两者不矛盾，是快照差异，答辩按来源标注日期**。

### 7.3 给策划 / HR 的话术（不出现技术黑话）

- **给策划**："您说人话，系统拆成表格能懂的步骤；写错会在动手前被拦下；跨表联动自动按正确顺序执行；每次失败都被记住，下次同类问题更少。多人改同一张表的改动，机器自动合并并标出冲突点，AI 先给建议，您只做最终拍板。"
- **给 HR**："这不是一次性交付，是**双引擎、会自学习、质量门禁前置、失败可回滚**的工程。单测从 145 涨到 323+，全程零回归；大表比对提速 7.5 倍、提交数据量省 99.8%——稳定、省钱、可持续。"
- **给技术**（诚实边界）："Agent 的 <90s 是设计目标，复杂跨表受外部 LLM 服务端阻塞未实测达成；但 LLM 调用 -80% 真实落地。Merge 的 7.5×/2.33x 均为已落地实测；ProcessPool 真并行仍受 worker 隔离限制，当前回退 ThreadPool——分引擎、分层级如实汇报。"

---

## 八、附：每页 PPT 建议组织（页面脚本）

| 页 | 标题 | 放什么 | 主讲对象 |
|---|---|---|---|
| 1 | 封面：AI 配表助手 —— 4-Step Agent × Merge 双引擎 | 标题 + 一句话定位 | 全场 |
| 2 | 核心数字一屏 | 〇章表格 | 全场 |
| 3 | 时间线总铆钉（上：07-23 ~ 08-03） | 一章表格前半 | 全场/HR |
| 4 | 时间线总铆钉（下：08-10 ~ 当前版） | 一章表格后半 + 演进主线 | 全场/HR |
| 5 | Step1 技术逻辑 | 流程图 + 5 环节 | 技术 |
| 6 | Step1 创新点与难点 + 类比 | §2.3/2.4 + 非技术类比 | 技术/策划 |
| 7 | Step1 性能优化 | §2.5 表 | 技术 |
| 8 | Step2 技术逻辑 + 创新 | L0/L1/L2 漏斗图 | 技术/策划 |
| 9 | Step2 性能优化 + 类比 | §3.5 表 | 技术 |
| 10 | Step3 技术逻辑 + 零 LLM 执行 | 拓扑排序图 + 占位符示例 | 技术 |
| 11 | Step3 性能优化 + 类比 | §4.5 表 | 技术 |
| 12 | Step4 自学习闭环 | 反模式门控流程 + §5.5 表 | 技术/HR |
| 13 | Merge 总览 + 架构分层图 | §6.1/6.2 ①②③④ + 分层图 | 技术 |
| 14 | Merge 核心算法（LCA + 语义归一） | copyfrom 链交叉图 + semantic_key 示例 | 技术 |
| 15 | Merge 性能优化（compare/apply） | §6.5 上表（7.5× / -99.8%） | 技术/策划 |
| 16 | Merge 与 Agent 的联通（AI 建议） | 后台预取 + 批量 LLM 流程图 | 技术/策划 |
| 17 | Merge 难点 + 非技术类比 | §6.4 + "多人改同一本书"类比 | 全场 |
| 18 | 全局 LLM 预算账 | §7.1 | 技术 |
| 19 | 全部指标速查 | §7.2 主表 | 技术/策划/HR |
| 20 | 结束页 + 诚实边界 + 三套话术 | §7.3 | 全场 |
