# PPT 答辩数据充分性审查 + 问答预案

> 审查对象：`PPT逐页脚本_整合重排版.md`（新页1-40）。逐页比对 `PERF_DATA_SUMMARY.md`（含 §四"已知边界与诚实项"）核实数据出处、样本量、是否有反向指标被藏起来。
> **结论先说**：内容页信息量够，但**有 7 类系统性缺口**会在"有答辩环节"的场合被直接问住——不是数据造假问题，是"该说的没说"问题。按优先级列在下面，🔴 必须改，🟡 建议改，🟢 可保留。

---

## 一、总体判断

| 维度 | 现状 |
|---|---|
| 真实 e2e 数据占比 | 约 60%（Merge 侧偏高，Agent 侧部分是小样本/单用例） |
| 样本量标注 | **多数页缺失**——"0.6667→0.8056"这类数字没在页面上标 N=8，观众/评委会问"几条样本" |
| 反向指标披露 | **有选择性遗漏**——已知有变差的指标（耗时、ok率）没放上去，只放了变好的 |
| 测试环境说明 | **全篇缺失**——没有一页说明硬件/数据规模/是否多次取平均 |
| 业务影响换算 | **缺失**——没有"比人工配表快多少""省多少钱/token"这类给业务评委看的换算 |
| 局限对称性 | **不对称**——Agent 有"当前验证结论"讲局限，Merge 没有对应页 |
| 创新定性 | 5 项"本轮优化"本质是 bugfix，包装成"创新点"在技术答辩上容易被问"这算创新还是维护" |

---

## 二、🔴 必须改（会被直接问倒，建议答辩前修完）

### 1. 样本量（N）没有标在页面上

**问题页**：新页8（定位0.6667→0.8056）、新页11（字段精准率0.5185→0.6917）、新页21（skill效果数据）等所有带百分比的页。

现状：页面上只写"0.6667→0.8056"，没写"基于 S-A 集 8 条样本"。评委追问"这是多大样本跑出来的"，你当场翻文档找数字，会很尴尬。

**改法**：每个百分比指标后面统一加 `(N=8)` / `(N=30)` 这种小字标注。这是最低成本、最高收益的修改——不用新增数据，只是把已经有的 N 写出来。

**预案问答**：
> Q：这个提升是几条样本测出来的，靠得住吗？
> A：S-A 集 8 条是早期评测基线，样本量确实不大，但配合后面 Skill 简版 30 样例、真写盘 8 样例、复杂样例 6/6 全过，三组独立样本方向一致，不是单点巧合。（→ 顺势带到新页21/新页24 的多组交叉验证）

---

### 2. 反向指标被选择性藏起来

**问题页**：新页8（Step1指标）。

`PERF_DATA_SUMMARY.md` §1.2 原始数据里，同一批 R1 评测还有两条**变差**的指标：

| 指标 | before | after |
|---|---:|---:|
| 平均耗时(ms) | 82236 | 98637（**变慢 20%**） |
| 响应ok率 | 0.8333 | 0.3333（**下降 60%**，预期下降因为拒错数据） |

现在的新页8只放了定位/覆盖率两条正向数字，没提这两条。**这是答辩里最容易被抓的点**——评委只要翻你的证据文件就能发现，届时"选择性展示数据"的印象比数字不好看本身更致命。

**改法**：新页8 加一行"代价说明"：
> 精准度提升的代价：平均耗时 82.2s→98.6s（更严格的校验多花时间），响应ok率 0.83→0.33（主动拒绝了更多有问题的输入，而不是硬凑答案）。

**预案问答**：
> Q：耗时变长、ok率下降，这是不是负优化？
> A：ok率下降是设计意图——旧版本"能凑就凑"，凑出来的答案里有大量错误；新版本宁可拒答也不给错误答案，ok率下降对应的是"错误答案率下降"。耗时增加 16.4s 是校验层增加的必要成本，换来定位准确率 +20.8%、字段精准率 +33.4%，这是有意识的取舍，不是失控。

---

### 3. 缺"测试环境/口径说明"独立页

**问题**：全篇 40 页没有一页说明：硬件配置、数据规模基准、是否多次取平均、"大表"具体多大。比如"48s→6.4s"的"大表"是多少行多少列？"10w行"是哪个测试文件？

评委在技术答辩里几乎必问"测试环境"和"是否单次测量"，没有这页会显得不严谨。

**改法**：在新页7（Prompt工程）之前或新页24（Agent指标汇总）附近插入一页**"数据口径说明"**，内容：
- 所有 before/after 均为同一批次、同硬件、同数据集对比，不跨样本集横比
- 明确几个反复出现的"大表"基准：如 3000行×150列（ProcessPool测试）、10万行（全链压测）、25MB sheet（fast_apply测试）
- 标注哪些是**单次实测**、哪些是**多样本聚合**（如30样例/8样例）
- 标注哪些是**设计值/模拟压测**（本轮5项优化 + 早期"跨6表全链<90s目标"）

**预案问答**：
> Q：这些提速数字是不是挑了一次跑得好的结果？
> A：Merge 侧的核心数字（compare 48s→6.4s、apply 19.6s→4.3s）来自 `merge/scripts/benchmark_report.md` 的固定基准脚本，可重复跑；Agent 侧的准确率数字来自固定样本集（S-A/S-B集），不是随机抽的单次结果。哪些是单次实测、哪些是多样本聚合，这页（数据口径说明页）有标注。

---

### 4. Merge 章节缺"已知局限"对称页

**问题**：新页37 只讲了 Agent 的局限（跨段代词、省略主语等），Merge 从新页26 到新页36 全程都是"做成了什么"，没有一页讲"Merge 还有什么没做完/有风险"。

答辩最怕的就是"你讲的东西听起来完美无缺"——这通常意味着还没被问过。`PERF_DATA_SUMMARY.md` §四已经有现成的诚实项，直接搬：

**改法**：新页37 之后加一页"Merge 已知局限"：

| 局限 | 说明 |
|---|---|
| 大表压测准确率仅50% | 10k行表 set 操作语义层存在偏差（`bench_perf_tables_latest`），已暴露待优化 |
| ProcessPool 真并行未在生产开启 | worker 需完全隔离才可启，当前 demo_svn 环境回退 ThreadPool，机制在独立样例中验证为 2.0x，生产收益待确认 |
| 未变更表跳过命中率依赖快照 | 08-11 历史快照 rev 全等时 64/74 跳过；09-01 复跑因 rev 全不同未触发（1.06x），按日期标注，不可跨快照横比 |

**预案问答**：
> Q：Merge 听起来已经很完善了，还有什么没解决？
> A：三个已知点：大表 set 操作准确率还只有50%，语义层判断有偏差；ProcessPool 真并行受 worker 隔离限制，生产环境还没打开；未变更表跳过这个优化的命中率依赖分支快照的 rev 是否相同，不是恒定加速比。这些都记录在案，是下一阶段的优化清单。

---

## 三、🟡 建议改（不改也能过，改了更有说服力）

### 5. "本轮5项优化"的创新定性偏弱

新页22-23 五项改动本质是 bugfix（假阳性、漏检、误清空、口径错误），包装成"本轮工程优化"没问题，但如果主持人问"这个算什么创新点"，直接说"是优化"会显得单薄。

**建议讲法**：不要单独强调"这是创新"，改成强调**方法论**——"这5处都不是拍脑袋改的，是先用真实代码/构造fixture跑出量化 before/after，再决定要不要改"，创新点落在**"用可复现的模拟压测代替'看起来应该没问题'"这套验证方法**上，而不是落在改动本身的技术难度上。这样问答时站得住。

### 6. 缺"对比人工配表"的业务影响换算

40 页全是技术指标（秒、%），没有一页把"3-5次LLM调用/几十秒"换算成"人工做同样的事要多久"。技术答辩如果有业务方评委，这类换算比"耗时6.4秒"更有冲击力。

**建议**：如果有可信估算（哪怕是"策划反馈单表添加通常需要5-10分钟核对外键/枚举"这类访谈式数据），加一页"对人工效率的影响"。**如果没有可靠数据，宁可不加，也不要编**——可以口头补一句"目前没有做严格的人工对照实验，这是下一步要补的量化项"，比硬编数字诚实。

### 7. LLM 成本没有换算成 token/费用

新页24"一次配表LLM调用 23+次→3-5次"只讲了次数，没讲省了多少 token 或多少钱。如果有 token 用量数据（`bench_iter_results.md` 应该有单次调用的 token 消耗），换算一下更有分量；没有就不强行编。

---

## 四、🟢 可以保留（已经够格）

- Merge 性能链条（新页28-35）：几乎每步都有具体数字+证据文件，量级从"卡死→秒级"讲得清楚，是全篇最硬的部分。
- Skill 系统效果数据（新页21）：30样例+8样例两组独立样本方向一致，够用。
- 本轮优化的"诚实边界"披露（新页22-23已有）：这个做法本身就是加分项，continue保持。
- 新页37 Agent 已知局限：已经诚实披露风险点，不用改，配上第4点新增的 Merge 对称页即可。

---

## 五、落地优先级（按你说"有答辩环节"，建议这个顺序改）

1. 🔴 全篇百分比指标补 N 标注（成本最低，收益最高，务必做）
2. 🔴 新页8 补耗时/ok率代价数据（避免"选择性展示"被抓）
3. 🔴 新增"数据口径说明"页（防"测试环境"追问）
4. 🔴 新增"Merge已知局限"页（防"听起来太完美"追问）
5. 🟡 调整本轮优化的讲法话术（不用改页面内容，改讲稿）
6. 🟡 视时间/数据可得性决定是否加业务换算页和token成本页——**没有可靠数据就不加**

前 4 条是防守型修改，建议答辩前必须做；后 2 条是进攻型加分项，量力而行。
