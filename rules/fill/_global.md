# 全局填表规则（所有表生效）

以下规则是强约束，AI 在理解输入、产出表格数据 JSON 时必须遵守：

- 新增 ID / 编号时，取该字段当前段内最大现有值 +1，禁止硬编码、禁止越界。
- 整数列不接受字符串；枚举列先归一化为枚举值再填。
- 列表列（`list[...]`）用逗号分隔字符串写入，禁止写成 JSON 数组（如 `[]`）。
- 布尔列填 `True` / `False` 或 `1` / `0`。
- 表头含 `\n` 尾注时，取 `\n` 之前的主名作为列名。
- 未提及的可选字段留空，不要臆造值。
