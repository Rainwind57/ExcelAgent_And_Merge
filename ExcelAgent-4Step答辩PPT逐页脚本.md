# Excel-Agent × Merge 答辩 PPT 逐页脚本（专业版 · 排版统一）

> **本文件用途**：可直接照着做 PPT 的逐页脚本。每页统一四段结构——**定位一句话 → 流程图/明细表 → ★ 关键技术点 → 指标框**。
> **布局规则**：文字承载逻辑解释，表格承载指标/结构/流程，流程图承载数据流向。指标一律用确定值（`48s→6.4s`），准确率/通过率配样例条数。
> **术语首次出现配页脚注脚**（不打断正文）；增强技术用 ★ 凸显，附录有索引总表。

---

# 页面流程线

```
封面(1) → 目录(2)
  ├─ 第一章 业务背景(3-4)
  ├─ 第二章 Excel-Agent 4-Step(5-16)
  │    架构总览(6) → Step1 解析(7-11) → Step2 校验(12) → Step3 执行(13)
  │    → Step4 自学习(14) → 指标汇总(15) → 创新点收束(16)
  ├─ 第三章 Merge 三方合并(17-26)
  │    流程(18) → 环境(19) → merge-base(20) → Compare(21)
  │    → 三大性能优化(22-24) → AI 建议(25) → 指标汇总(26)
  └─ 第四章 总结与展望(27-30)
结束页(31)
```

---

# 第一章 · 业务背景

## 第 1 页｜封面

**居中大字：**
- **AI 配表助手**
- 自然语言驱动配表 + 三方合并 Merge
- 吴植贤 | 2026年9月4日

---

## 第 2 页｜目录

**四章：**

| # | 章节 | 一句话 |
|---|---|---|
| 01 | 业务背景与任务目标 | 为什么做 |
| 02 | Excel-Agent：4-Step 多 Agent 配表流水线 | 怎么改表 |
| 03 | Merge：面向 SVN 目录分支的三方合并 | 怎么合表 |
| 04 | 总结与后续优化方向 | 收束与展望 |

---

## 第 3 页｜业务背景：真实配表的复杂性

### 痛点（要点列表）

- 配表规模大：表多、字段多、跨表引用密集，人工维护成本高。
- 当前生产依赖 SVN 外链和目录隔离，缺少真正的分支合并语义。
- 策划表达是自然语言："新增活动、绑定奖励、配置技能、调整数值"。
- 难点不在"写单元格"，而在理解**目标表、字段、外键、枚举、依赖顺序**。

> 【页脚注脚】外键 FK = 指向另一表主键的列；枚举 = 字段允许取值白名单。

---

## 第 4 页｜任务目标

| 引擎 | 目标 | 关键约束 |
|---|---|---|
| Excel-Agent | 自然语言 → 多表配表 | 输出可校验、可执行、可追踪的结构化意图 |
| Merge | 目录分支模式 Excel 三方合并 | 保护公式、ID 引用、冲突可解释 |

> **★ 核心主张**：模型负责理解，规则负责约束，执行层保持确定性。
> 共同目标：把模型理解、规则校验、确定性写盘、工程回归串成**可落地闭环**。

---

# 第二章 · Excel-Agent 4-Step

## 第 5 页｜章节封面：Excel-Agent 4-Step

**大字：** Excel-Agent：4-Step 多 Agent 配表流水线
**副题：** 模型负责理解，规则负责约束，执行层保持确定性

---

## 第 6 页｜架构总览（立全局预期）

### 4 个子智能体管道

```
用户自然语言
      │
      ▼
┌─────────┐   ┌───────────┐   ┌──────────┐   ┌───────────┐
│ Step1   │──▶│  Step2    │──▶│  Step3   │──▶│  Step4    │
│ 解析     │   │  校验      │   │  执行     │   │  汇总+自学习 │
│ Parse   │   │ Validate  │   │ Execute  │   │ Conclude  │
│(按需LLM)│   │(L0零LLM)  │   │(零LLM)★  │   │(按需LLM)  │
└─────────┘   └───────────┘   └──────────┘   └───────────┘
     intents      validated        results        summary
```

| Step | 子 Agent | 职责 | LLM |
|---|---|---|---|
| 1 解析 | ParseAgent / LocatorAgent / DecomposeAgent | 分段 → 定位 → schema 注入拆解 → 语义计划编译 | 按需 |
| 2 校验 | ValidateAgent | 字段/类型/枚举/FK/占位符/依赖校验 | L0 零 LLM |
| 3 执行 | ExecuteAgent | 依赖排序确定性写盘、ID 分配、引用回填 | **零 LLM** |
| 4 汇总 | ConcludeAgent | 变更归纳、错误、自学习反模式 | 按需 |

### 全局预期（第 15 页回扣）

| 指标 | 早期 6 步线性 | 4-Step 终态 |
|---|---|---|
| 一次配表 LLM 调用 | 23+ 次 | 3-5 次 |
| 跨 6 表全链耗时 | 10+ 分钟 | 目标 <90 秒（设计值） |

> **【增强技术预告】**：RAG 检索增强定位、schema 注入、语义计划编译、L0/L1/L2 校验、反模式自学习——后续逐页展开。

---

## 第 7 页｜Step1：全链路的核心

### 定位（一句话）

> Step1 职责 = 把自然语言转成**可执行计划**，不是"一句话变 JSON"。

### 6 步动作链

| 顺序 | 动作 | 说明 | 展开页 |
|---|---|---|---|
| 1 | 分段 | 多意图规则切段，一段一次 LLM | — |
| 2 | 候选表定位 | 规则 + 检索 + 关系扩展 | 第 8 页 |
| 3 | schema 注入 | 真实表结构注入 prompt | 第 9 页 |
| 4 | LLM 拆解 | 产出 add/update/delete intents | — |
| 5 | 语义计划编译 | 结构化为可执行计划 | — |
| 6 | 后处理修复 | 质量门禁 | 第 11 页 |

> **★ 优化重点**：降低**幻觉表、字段串表、重复影子行、自产自消依赖、FK 错指向**。

---

## 第 8 页｜★ 增强技术：LocatorAgent 三级定位（含 RAG）

### 定位（一句话）

> **先靠规则 + 检索 + 关系扩展（全部零 LLM），只有歧义才请模型裁决。目标是"用户说错表名也能命中"。**

### 数据流

```
用户指令 "给神通加个新技能"
        │
        ▼
┌──────────────────────────────────────────────────────┐
│  L1 规则通道   表名/中文表头/别名/字段关键词  → 精确命中  │
│  L2 模糊通道   rapidfuzz 混合相似度           → 变体兜底  │
│  L3 关系扩展   外键一跳扩展                   → 带上绑定表 │
│  L4 RAG 召回   BM25 语义召回                 → 补语义近邻 │
└──────────────────────────────────────────────────────┘
        │  (path, sheet) 去重，按置信度降序
        ▼
   定位候选 + 歧义候选 →（歧义时才）LLM 裁决
```

### 四通道明细 + 候选打分

| 通道 | 技术 | 置信度 | 作用 |
|---|---|---|---|
| L1 规则 | 表名 / 中文表头 / 别名 / 字段关键词 | 0.7-1.0 | 直接命中，零成本 |
| L2 模糊 | rapidfuzz 混合（WRatio×0.5 + token_set×0.3 + partial×0.2；无库回退 0.6×余弦+0.4×Jaccard） | 0.7-1.0 | 拼写/命名变体兜底 |
| L3 关系 | 外键（FK）一跳扩展 | 中 | 命中主表带上绑定表 |
| L4 RAG | **BM25 语义召回**（O18 方法 G） | **0.3-0.5** | 规则未命中时补召回语义近邻 |
| 候选打分 | **`candidate_scorer` 动词邻近度加权** | 弱置信段 +0.06 封顶 | 动作词（新增/绑定/修改）附近实体名优先 |

> **★ 关键设计**：RAG 置信度**刻意低于规则命中**（0.3-0.5 vs 0.7-1.0）——只"补召回"、绝不压过精确规则。候选打分同理，仅对 `conf<0.80` 弱置信段小幅加权（上限 0.06、封顶 0.79），**不跨强命中分界线**。

### RAG 具体怎么实现

| 环节 | 实现 |
|---|---|
| 数据源 | 复用 `_table_index.json` 的 `search_blob`（非长文本列去重值、小写、`\n` 分隔） |
| 分词 | jieba 中文分词（零新依赖） |
| 检索算法 | 标准 **Okapi BM25**（`k1=1.5, b=0.75`），取 top-5 |
| 置信度 | 原始分（约 0-20）归一映射到 **0.3-0.5** |
| 去重合并 | `(path, sheet)` 去重，保留最高置信度 |
| 可关停 | env `CODEMAKER_RAG_MODE=bm25\|off`，默认 bm25，一键关闭向后兼容 |

### 为什么用 RAG 而不是多调 LLM

> BM25 召回是**纯本地计算、零 LLM、零成本**；在规则没命中时补上语义近邻候选，把"用户口吻没说准表名"这种最易翻车的情况**在进入 LLM 之前**就兜住。

### 指标归因（讲清楚谁带来什么）

| 指标 | 优化前 | 优化后 | 归因 |
|---|---|---|---|
| 定位 10 万行表 | 全表扫描 6.8s | 索引 0.58s | **索引化**（`table_index.json`） |
| 定位准确率（S-A 8 条） | 66.7%（约 5/8） | 80.6%（约 6-7/8） | **R1 值语义门 + 级联** |
| 表定位命中率（quest_npc 链） | 0.64 | 0.91 | **column_matcher**（相似度匹配） |
| RAG 召回 | — | 新增 top-5 召回层 | **O18 方法 G**（补召回，A/B 待验证） |

> ⚠️ **诚实边界**：RAG 层 2026-08-18 上线（O18），当前为"召回层已接入、A/B 对照评估留 follow-up"状态——已实测能召回语义近邻、全量单测 981 项通过，但"开启 vs 关闭 RAG"的定位命中率差值尚未出报告（G6 follow-up）。讲解口径：**RAG 能力已上线、量化评估在途；已量化的是它两侧的索引化（6.8s→0.58s）与 column_matcher（0.64→0.91）**。

---

## 第 9 页｜★ 增强技术：DecomposeAgent schema 注入拆解

### 定位（一句话）

> 输入 = 候选表**真实结构** + 多级表头 + 字段类型 + FK + 填表规则；输出必须是**结构化 intents**，不是自然语言。

### 输入构成（它拿到什么）

| 输入 | 来源 | 内容 |
|---|---|---|
| 候选表 | LocatorResult | 命中表 + FK 边（外键关系） |
| schema | 每表全部业务 sheet 的 **row1+row2 表头** | 多级表头、字段类型、填表规则 |
| 链型上下文 | 候选表间 FK 关系 | 跨表依赖如何连 |

### 输出契约（它产出什么）

> 每表一 op 的 `SplitIntent`，必须是 **add / update / delete**，不是自然语言摘要。

```
{table, sheet, action, fields, produces:[...], consumes:[...]}
  │      │       │       │         │             └─ 本意图消费的占位符（见第 10 页）
  │      │       │       │         └─ 本意图产出的占位符
  │      │       │       └─ 字段（只能是真实列名）
  │      │       └─ add/update/delete
  │      └─ 命中 sheet
  └─ 命中表
```

### 输入 → 输出示例

```
输入：Ability 表 row1/row2、字段类型、外键、填表规则
        │
        ▼
输出：{action:add, table:Ability, fields:{id:100, name:"疾风"}, produces:[<aid>]}
```

| 输入 | 输出 |
|---|---|
| `Ability` 表 row1/row2、字段类型、外键、填表规则 | `{action:add, table:Ability, fields:{id:100, name:"疾风"}, produces:[<aid>]}` |

### ★ 关键技术点

- **schema 注入**：模型**只能从真实列名里选**——从源头掐死幻觉列名（历史上 `column_not_found` 是最大失败根因）。
- **few-shot**：提示中附正确 ID/占位符示例；**禁止伪造 `<resolved_from_xxx>`**；超时降级逐表重拆。
- **规则安全网**：LLM 为主、规则兜底——产 **≥2 条 intent** 才取代规则模板；产 **<2 条或畸形**时回退 `cross_table_splitter` 的 11 个硬编码模板，**保 ok 率不回归**。新链型从此零代码接入。
- **LLM 能力级自检补漏（新增）**：`_llm_complete_fields` 让 LLM 对照原文 + **全量 schema**（`_build_full_schema_block` 不裁剪）补齐漏产字段，代码只做 schema grounding 白名单过滤——**漏字段从"静默丢"变"主动补"**。

### 可观测性与性能配套（同一文件内）

| 机制 | 作用 |
|---|---|
| schema 内存缓存 `(stem,sheet)→(headers,type_row)` | 同 session 主路径+兜底+重跑重复读表头，**省 60-70% I/O** |
| session 复用（主线程 `_sid` / 工作线程 threading.local） | 多次 decompose 不再每次建会话，**省 1 次 HTTP 建会话 RTT/次** |
| LLM IO 可观测 harness（`CODEMAKER_DECOMPOSE_TRACE=1`） | prompt/raw 落 log 或 dump 目录，**离线复现"为何只产 N 条 / 字段缺失"**，纯结构化遥测不绑业务词 |
| thinking 事件推送（`__json:<kind>` → 前端 SSE） | 前端可视化 decompose 产/留/丢清单，定位遗漏根因 |

### 性能小结

| 指标 | 数值 |
|---|---|
| schema 读取 I/O | 省 **60-70%** |
| LLM 调用 | 每候选表 1 次 → O1 单 prompt 主路径（候选 ≤3 时）**token 砍 50-70%** |

---

## 第 10 页｜★ 增强技术：produces / consumes 依赖契约

### 定位（一句话）

> 跨表新增时，后续表常依赖前面新建行的 ID——靠契约把它确定下来，而不是靠模型记忆。

### 依赖链示意

```
[Step A 新增 奖励表] produces → <奖励ID>
        │
        ▼
[Step B 新增 技能表] consumes ← <奖励ID>   （保证 A 在 B 之前执行）
```

| 术语 | 含义 | 作用 |
|---|---|---|
| produces | 本意图产出的占位符 | 记录"我生成了哪个 ID" |
| consumes | 本意图消费的占位符 | 记录"我用哪个 ID" |
| 占位符 placeholder | 写表时的临时标记 | 执行时替换成真实 ID |

### ★ 关键技术点

> 把"跨表依赖"从模型记忆搬到**确定性契约**——Step3 靠它排序、分配 ID、回填外键。（属机制性创新，无性能指标）

- **entity_key 占位符编译（新增）**：LLM 只产 `entity_key`，由**代码层**（`semantic_plan._compile_entity_keys`）编译为稳定占位符 `new_<stem>_<key>_id`——**占位符协议全代码化，禁止 LLM 直接控制**。这从机制上杜绝 `resolved_from_xxx` 伪造。

---

## 第 11 页｜Step1 质量门禁（后处理修复）

### 定位（一句话）

> LLM 交卷后 ParseAgent 继续 **schema-aware 编译**——最后一道结构质检。

### 检查清单

- 删孤立空壳 add、同 sheet 稀疏影子重复。
- 修字段不在 schema、sheet/name 混用、目标表大小写差异。
- 检查 produces/consumes：悬空 placeholder、自产自消、依赖环、FK 目标错表。

### 问题 → 修复

| 问题 | 修复 |
|---|---|
| 孤立空壳 add / 影子重复 | 删除 |
| 字段不在 schema | 修正到真实列 |
| 悬空占位符 | 从已存在资源或本批次新行解析真实 ID |

---

## 第 12 页｜★ 增强技术：Step2 L0/L1/L2 三层校验漏斗

### 定位（一句话）

> 写盘前质量门禁——检查字段存在、类型可转换、枚举合法、FK 指向正确、placeholder 有 producer；**硬错误阻断写盘**。

### 三层漏斗

```
       全部 intents
            │
            ▼
   ┌────────────────┐   约 80% 在此被拦截（零 LLM）
   │  L0 规则闸      │───▶ 直接通过
   │ 字段/类型/枚举/范围 │
   └────────────────┘
            │  剩余难例
            ▼
   ┌────────────────┐   1 次 LLM 语义裁决
   │  L1 模型判官     │
   └────────────────┘
            │
            ▼
   ┌────────────────┐   最终人工兜底
   │  L2 人工确认     │
   └────────────────┘
```

| 层级 | 手段 | LLM | 覆盖 |
|---|---|---|---|
| L0 规则闸 | 字段/类型/枚举/范围代码化校验（枚举源含 *Type/*类型 权威表） | **零 LLM** | 约 80% |
| L1 模型判官 | 1 次 LLM 语义裁决 | 1 次 | 剩余难例 |
| L2 人工确认 | **全字段可编辑卡**（§9.1） | — | 最终兜底 |

### ★ 关键技术点

- **值语义门** = 写盘前检查值是否符合字段语义（类型 / 枚举 / 范围），把"错数据"挡在写盘之前。
- **硬/软分级**：`unique_violation / type_mismatch / col_not_found` + 主键列缺失为硬阻断；`enum_invalid` 等**非阻断**（可交互 ask，不硬跳过）。
- **L2 整表编辑卡（§9.1 新增）**：同一 intent 的硬阻断问题合并成**一张全字段编辑卡**（前端 `field_edit_table`），用户改完 → 自动重校验，最多 3 轮，**绝不绕过硬错误进 Step3**。

> 指标并入第 15 页。

---

## 第 13 页｜★ 增强技术：Step3 确定性执行（零 LLM）

### 定位（一句话）

> Step3 **不再调用模型**，只做可解释写盘——这是 4-Step 与 6 步线性最大的架构分界。

### 执行动作

| 动作 | 说明 |
|---|---|
| 拓扑排序 | 按 consumes/produces 依赖排执行先后（producer 先于 consumer） |
| ID 分配 + 占位符替换 | 新增行分配主键，替换后续 placeholder |
| 失败结构化归因 | 缺字段 / PK 冲突 / 类型错误 / FK 悬空 |
| 可恢复边界 | 失败停在可恢复点，避免半写入污染 |

### ★ 关键技术点（D4 决策）

> **执行是确定性工程，不该依赖概率模型**——这是成本（LLM 5→2-3 次）与稳定性（6/6 通过）双赢的根基。

- **孤儿行防护（增强）**：执行前扫描悬空非 `<auto>` 占位符并跳过写入（防缺外键残缺行）；**自增主键占位符豁免（新增）**——本行 `<new_<stem>_id>` 属自产主键不算悬空依赖，避免假失败。

### 指标框

| 指标 | 数值 |
|---|---|
| 全链 LLM 往返 | 5 次 → 2-3 次 |
| 高复杂跨表样例 | **6/6 通过**、LLM 稳定 19 次 |

---

## 第 14 页｜★ 增强技术：Step4 自学习闭环

### 定位（一句话）

> Step4 不止"告诉成功/失败"，还做**失败沉淀**——把踩过的坑变成下次的规则。

### 门控提升闭环

```
失败 failures
     │
     ▼
 结构化清单（step_id / 错误类型 / 根因 / 建议）
     │
     ▼
 1 次 LLM 归纳反模式（失败则降级模板）
     │
     ▼
 pending_review ── mini_regression ──▶ active
     │                                │
     └──────── 未通过，丢弃 ◀───────────┘
     │
     ▼
 写入 rules / examples / retrieval → 下次在 Step1/2 拦截
```

| 阶段 | 动作 |
|---|---|
| 收集 | failures → 结构化失败清单（step_id / 错误类型 / 根因 / 建议） |
| 归纳 | 1 次 LLM 归纳反模式（失败降级模板） |
| 门控 | 新反模式先进 pending_review，过 **mini_regression** 才转 active |
| 沉淀 | 写入 rules / examples / retrieval，下次在 Step1/2 拦截 |

### ★ 关键技术点（反模式自学习）

> 系统从自己的失败里学——**错误越跑越少**；门控防"学坏"。

### 指标框

| 指标 | 数值 |
|---|---|
| 确定性定位成功率 | 6 轮迭代恒 **100%** |
| 失败归纳 | 8 条失败 trace → **4 条反模式** |

---

## 第 15 页｜Agent 全链指标汇总（回扣页 6）

### 性能指标（确定值）

| 指标 | 优化前 | 优化后 | 说明 |
|---|---|---|---|
| 一次配表 LLM 调用 | 23+ 次 | 3-5 次 | 6 步线性 → 4-Step |
| 单表新增（灵兽朱雀） | — | 29.9s 通过 | bench_v1 实测 |
| 复合新增（瞭望塔） | — | 33.5s 通过 | bench_v1 实测 |
| 单表修改（灵兽资质） | — | 25.4s（ok=false） | bench_v1 实测 |
| 单表删除（测试兽） | — | 14.3s（ok=false） | bench_v1 实测 |
| QA 提问 | — | 9.2s 通过 | bench_v1 实测 |
| 定位 10 万行表 | 全表扫描 6.8s | 索引 0.58s | — |
| 10 万行大表全链 | 卡死 | 约 10.4s | compare 3.9s + apply 6.3s |

### 准确率 / 通过率（S-A 8 条同集，R1）

| 指标 | 优化前 | 优化后 |
|---|---|---|
| 定位准确率 | 66.7%（约 5/8） | 80.6%（约 6-7/8） |
| 覆盖度 | 61.1% | 75.0% |
| 字段精准率 | 51.9% | 69.2% |
| 严格通过率 | 16.7% | 33.3% |

### 复杂级联样例（case 106-111，R4）

| 指标 | 优化前 | 优化后 |
|---|---|---|
| 用例 106 耗时 | 204s | 112s（**-45%**） |
| 用例 106 覆盖度 | 0.16 | 峰值 0.39（**+143%**） |
| 用例 107 精准 | 0 | 0.79 |

### skill 简版（30 样例确定性，无 LLM 波动）

| 指标 | off | on |
|---|---|---|
| 定位成功率 | 0.933 | **1.000** |
| 精确命中 | 0 | **1.00** |
| 严格综合 | 0.704 | **0.959** |
| 平均耗时 | 533ms | **321ms（减半）** |

### 4-Step Loop 6 复杂样例实测（bench_iter_results，parallel=1）

| # | 样例 | 复杂度 | 墙钟(s) | LLM 调用 | ok |
|---|---|---|---|---|---|
| 1 | 封印魔龙（跨 6 表+占位符） | 高 | 464.3 | 19 | ✓ |
| 2 | 幽冥宗门派（跨 3 目录 6+ 表） | 高 | 681.6 | 19 | ✓ |
| 3 | 九尾天狐全生命周期（跨 2 目录 4 表） | 高 | 947.3 | 19 | ✓ |
| 4 | 万圣狂欢（跨 6 表 add+modify） | 高 | 225.1 | 19 | ✓ |
| 5 | 聚灵塔（同 xlsx 5 sheet） | 高 | 323.7 | 19 | ✓ |
| 6 | 复合修改+删除级联（跨 4 表） | 高 | 372.4 | 19 | ✓ |

> **★ 核心结论**：6/6 全过、每样例稳定 LLM 19 次。对比早期（bench_v2 parallel=3 全 Timeout / bench_myrun parallel=2 几乎全败）：**串行 parallel=1 是稳定性基线**——LLM 服务端共享阻塞下，稳 > 快。

### quest_npc 单链深化（R8/R8c）

| 指标 | 早期 | 终态 |
|---|---|---|
| 引用一致率 | 0.06 | **1.00**（达标） |
| 覆盖度 | 0.82 | **1.00** |
| 精准率 | 0.56 | 0.78 |
| 耗时 | 134s | 107s |

> 页底一行：Agent 相关回归 **109 项全部通过**、单测 **145→204→323+ 项零回归**、skill 定位成功率 6 轮恒 100%——优化这么多，可靠性没倒退。

---

## 第 16 页｜★ 4-Step 增强技术收束（一屏五 ★）

| # | 增强技术 | 解决的问题 |
|---|---|---|
| 1 | ★ 先定位 + RAG | 用户说不准表名也能命中，压幻觉 |
| 2 | ★ schema 注入 + 语义编译 | 模型只能从真实列里选，杜绝幻觉列 |
| 3 | ★ produces/consumes 契约 + 拓扑排序 | 跨表依赖不靠猜，执行零 LLM |
| 4 | ★ L0/L1/L2 漏斗 + 值语义门 | 约 80% 错误零 LLM 拦截 |
| 5 | ★ 反模式自学习 + mini_regression | 失败沉淀成规则，错误越跑越少 |

> **一句话哲学**：能省则省（LLM）、先建契约、先校验后动手、失败可沉淀。

---

# 第三章 · Merge 三方合并

## 第 17 页｜章节封面：Merge 三方合并

**大字：** Merge：面向 SVN 目录分支的三方合并
**副题：** 可信 Base + 稀疏 diff + 引用保护 + 安全写回

---

## 第 18 页｜Merge 总体流程（7 步）

### 流程

| # | 步骤 | 说明 |
|---|---|---|
| 1 | 选择 From/To 目录 | — |
| 2 | SVN copyfrom 定位 merge-base | 共同祖先（第 20 页） |
| 3 | 提取数据/表头/公式/样式/元信息 | — |
| 4 | 比较变更、识别冲突 | Compare 四层（第 21 页） |
| 5 | ID 重映射与引用更新 | （第 24 页） |
| 6 | AI 辅助冲突建议 | （第 25 页） |
| 7 | LibreOffice 重算、公式保护、安全写回 | （第 23 页） |

---

## 第 19 页｜模拟生产环境

### 定位（一句话）

> 在一个可控仿真仓库里验证算法——**真实 SVN 历史，不造假 merge-base**。

### 环境

- SVN demo 目录：trunk 74 张表 + subdev_1/2/3 子目录。
- branches dev1-4（全量 / 小表各两种）。
- 用 `merge/svn/demo_svn/` 的 `repo/`（svnadmin 仓库）+ `wc/`（工作副本）。

---

## 第 20 页｜★ 增强技术：SVN merge-base 定位（LCA 反查）

### 定位（一句话）

> SVN 无内置 merge-base 命令，必须**自己反查共同祖先**——没有它，合并就是"谁覆盖谁"的瞎猜。这是整条 merge 线的算法地基。

### 算法步骤

```
From/To 分支
     │
     ▼
① 各自 copyfrom 找派生点（svn log -v --stop-on-copy）
     │
     ▼
② 沿 copyfrom 链上溯，完整祖先链
     │
     ▼
③ 两条链交叉，求最近公共祖先 LCA
     │
     ▼
④ 纯新建无 copyfrom → 降级"首次 rev-1"，标记 inferred
```

| # | 步骤 | 说明 |
|---|---|---|
| 1 | 各自 copyfrom 找派生点 | `svn log -v --stop-on-copy` |
| 2 | 沿 copyfrom 链上溯 | 完整祖先链 |
| 3 | 两条链交叉求 LCA | 最近公共祖先 |
| 4 | 纯新建降级 | 首次 rev-1，标记 inferred |

> **★ 关键技术点**：**LCA（最近公共祖先）** = 两分支共同的最近版本。

---

## 第 21 页｜Compare 全流程（四层比较）

### 分层比较（由粗到细）

```
文件层 → Sheet 层 → 行列层 → 语义层
 新增/删除    新增/删除    主键匹配      公式
 重命名/移动  表头变化     行增删        外键引用
                          字段变更       ID 冲突
                                       可合并性判断
```

| 层级 | 比较内容 |
|---|---|
| 文件层 | 新增 / 删除 / 重命名 / 移动 |
| Sheet 层 | 新增 / 删除 sheet、表头变化 |
| 行列层 | 主键匹配、行增删、字段变更 |
| 语义层 | 公式、FK 引用、ID 冲突、可合并性判断 |

---

## 第 22 页｜★ 增强技术：稀疏化 + 向量化

### 定位（一句话）

> 大表不能逐格 Python 循环——用**稀疏化 + 向量化 + 哈希索引**三招把 10w 行从"卡死"变成"秒级"。

### 技术 → 作用

| 技术 | 作用 |
|---|---|
| 稀疏化（sparse diff） | 只记录变化行列，全等行只留主键格 |
| 向量化（numpy 矩阵 diff） | 数值/字符串批量比较，替换逐格循环 |
| 哈希索引 | 预建 行→位置 映射，替换线性扫描 |

> **★ 关键设计**：稀疏化 = **批改作业只划改错的题，不重抄整本**——10w 行表从 GB 级降到 MB 级，是"秒级 compare"的关键。

### 指标框

| 指标 | 优化前 | 优化后 |
|---|---|---|
| 大表 compare | 48s | 6.4s |
| 提交数据量 | 46.4MB | 0.1MB |
| 10 万行表全链 | 卡死 | 约 10.4s |

---

## 第 23 页｜★ 增强技术：公式缓存保护

### 定位（一句话）

> Excel 内部公式与缓存值分离，直接写盘易丢缓存或失效——只扫公式、不整表读。

### 流程

```
zip 扫描 XML 底层找 <f> 公式标签
        │
        ▼
保存后读回 vs 快照比较
        │
        ├─ 缓存消失 = lost  → LibreOffice 重算再校验
        └─ 值变化   = changed（正常，无需修复）
```

### 指标框

| 指标 | 优化前 | 优化后 |
|---|---|---|
| 10 万行表读取 | openpyxl 全量 6.1s | zip 扫描 0.05s |

> 【页脚注脚】openpyxl = 功能全但慢的 Excel 读写库；LibreOffice = 开源办公软件，用于公式重算。

---

## 第 24 页｜★ 增强技术：ID 重映射与引用完整

### 定位（一句话）

> 多分支同时新增行时主键可能冲突——**先到先得 + 自动重映射 + 引用同步**，不留断链。

### 策略

| 场景 | 策略 |
|---|---|
| 同主键不同分支新增 | 先到先得分配 ID，后到者重映射新 ID |
| 引用列同步 | 改主表 ID 同时更新引用列，不留悬空 FK |
| 假删除 | `src_scope_only` 修复——trunk 有、子目录没有不再误报删除 |

> **★ 关键设计（语义归一）**：把 `100` / `100.0` / `"100"` 视为同值——消除"看起来不同其实相同"的假冲突。

### 指标框

| 指标 | 优化前 | 优化后 |
|---|---|---|
| 假删除（source_deleted）误报 | 69 | 0 |
| 假冲突率 | 0.5 | 0 |

---

## 第 25 页｜★ 增强技术：AI 辅助冲突建议（与 4-Step 联通点）

### 定位（一句话）

> AI 只提供**冲突解决建议**，最终由用户确认——核心策略纯代码，不 AI 化。

### 机制

| 机制 | 说明 |
|---|---|
| 后台预取 | compare 返回后线程池预取**冲突最多前 5 sheet** 的 AI 建议 |
| 批量 LLM | 全部未缓存冲突格拼一个 prompt，**一次调用返回全部建议** |
| 回退并行 | 批量失败时线程池逐格调用 |
| 置信度因子 | LLM 自评 + 修订号领先度 + 值域合理性 |

### ★ 关键技术点

> **merge 核心策略纯代码不 AI 化**——AI 只进"建议"路径，不进"策略匹配"确定性路径（规则更可靠）。

### 指标框

| 指标 | 优化前 | 优化后 |
|---|---|---|
| 冲突建议调用 | N 次网络往返（每格 15-45s） | 1 次批量调用（约 30-60s） |

---

## 第 26 页｜Merge 指标汇总（回扣页 18）

### 端到端（HTTP 实测，merge/scripts/benchmark_report.md）

| 操作 | 优化前 | 优化后 |
|---|---|---|
| branch_compare | ~48s | ~6.4s（**7.5×**） |
| apply 处理（big_data 单表） | ~19.6s | ~4.3s（**3.8×**） |
| apply 上传 payload | 46.4MB | 0.1MB（**-99.8%**） |
| compare 序列化 | 4.9s | 0.3s |

### 路由层 A/B（R3，monkeypatch 确定性）

| 环节 | before | after | 加速 |
|---|---|---|---|
| subdir_compare（5 表） | 21.8s | 9.3s | **2.33x** |
| branch_compare（74 表） | 99.5s | 89.5s | 1.11x（瓶颈在引擎层） |
| calamine 222 次文件打开 | 17.5s | 0.8s | openpyxl 0.3-0.5s/文件 → 0.1ms |
| svn info 批量（rev_cache） | N 次 svn log 约 190s | 一次 svn info 约 0.2s | **~950×** |
| 假 source_deleted | 69 | 0 | 正确性 |

### 引擎层（R6 #24/#32/#33）

| 指标 | before | after |
|---|---|---|
| 假冲突率（seed id=2） | 0.5 | 0（语义归一 100/100.0/1e2） |
| 未变更表跳过 | 全 74 表 compare | **64/74 跳过（86%）**，branch 54.2→49.3s（1.10x） |
| ProcessPool 并行（8 表 3000×150） | ThreadPool 3.69s | **1.84s（2.0x）** |
| 10 万行大表全链 | 卡死 | ~10.4s（compare 3.9s + apply 6.3s） |

### 分阶段复算（S0→S3）

| 阶段 | branch compare(处理) | 序列化 | big_data apply |
|---|---|---|---|
| S0 未优化 | 31.42s | 5.39s | 19.23s |
| S1 读取加速 | 6.78s | 5.87s | 19.76s |
| S2 序列化加速 | 7.71s | 0.33s | 19.30s |
| S3 当前 | 7.31s | 0.29s | 4.19s |

> 页底一行：核心回归 **71 项全部通过**——合表线改得最狠（向量化 / 稀疏化 / fast_apply），回归照样全过。

---

# 第四章 · 总结与展望

## 第 27 页｜章节封面：总结与展望

**大字：** 总结与后续优化方向
**副题：** 双引擎联通 + 总结 + 展望

---

## 第 28 页｜★ 双引擎联通点

| 维度 | Excel-Agent | Merge |
|---|---|---|
| 契约思想 | produces/consumes | ID 重映射 |
| 性能武器 | 缓存 / 零 LLM / 并行 | 向量化 / 稀疏化 / 并行 |
| 质量理念 | 先校验后动手 | 引用保护 / 回滚 |

> 一个哲学贯穿两引擎——**能省则省（LLM）、先建契约、失败可回滚**。这解释了项目名 `Excel-Agent-And-Merge`。

---

## 第 29 页｜总结

### 结论

- **Excel-Agent 正确方向**：不是堆 prompt，而是"LLM 理解 + schema 编译 + 规则校验 + 确定执行"。
- **Merge 正确方向**：可信 Base + 稀疏 diff + 引用保护 + 安全写回。
- **一句话**：把模型理解、规则校验、确定性写盘、工程回归串成**可落地闭环**。

### 可靠性确认

| 指标 | 数值 |
|---|---|
| 回归通过 | Agent 109 项 + 核心 71 项 = **180 项全过** |
| 单测 | **323+ 项零回归** |
| 确定性定位成功率 | 6 轮恒 **100%** |
| 剩余风险 | 枚举口语化 / 资源缺必填字段 / 复杂跨段语义（已部分由 `cross_ref_context` 缓解） |

---

## 第 30 页｜后续优化方向

### 已完成（近期落地，标注于前页）

| 原展望 | 现状 | 落地点 |
|---|---|---|
| 交互修正：全字段可编辑表格 | ✅ 已落地 | L2 整表编辑卡（§9.1），改完自动重校验 |
| 上下文继承：跨段代词/省略主语 | ✅ 已落地 | `cross_ref_context.py` 0 LLM 指代消解 |
| 可观测：补齐确定值 | ✅ 已落地 | `_build_step1_audit` 统一审计报告 |

### 仍待办

| 方向 | 内容 |
|---|---|
| 输入容错 | 枚举口语化解析、资源缺必填字段兜底 |
| 并行隔离 | ProcessPool worker 模块完全隔离（当前回退 ThreadPool） |
| 端到端可观测 | 更多回归 + 耗时/准确率/通过率持续补齐确定值 |

---

## 第 31 页｜结束页

**大字：** THANK YOU
**副题：** 模型负责理解，规则负责约束，代码负责确定性

---

# 附录｜增强技术索引（一页总结给评审看）

| 增强技术 | 所属 | 一句话作用 |
|---|---|---|
| RAG（BM25 语义召回） | Agent Step1 | 规则未命中时补召回语义近邻，压幻觉（O18，A/B 待评） |
| schema 注入 | Agent Step1 | 模型只能从真实列里选，杜绝幻觉列 |
| few-shot 示例 | Agent Step1 | 附正确例子让模型照着做 |
| 语义计划编译 | Agent Step1 | 结构化 intents，可执行可校验 |
| produces/consumes 契约 | Agent Step1 | 跨表依赖不靠猜 |
| **entity_key 占位符编译** | Agent Step1 | 占位符协议全代码化，禁 LLM 直接控制 |
| **LLM 自检补漏** | Agent Step1 | 对照原文+全量 schema 补齐漏产字段 |
| **候选打分（动词邻近度）** | Agent Step1 | 弱置信段动作词优先，不跨强命中分界 |
| **跨段引用上下文继承** | Agent Step1 | 0 LLM 指代消解（它/这个→具体实体名） |
| **L2 全字段可编辑卡** | Agent Step2 | 硬错误合并成整表编辑卡，改完自动重校验 |
| L0/L1/L2 校验漏斗 | Agent Step2 | 约 80% 错误零 LLM 拦截 |
| 值语义门 | Agent Step2 | 错数据挡在写盘之前 |
| 拓扑排序 + 占位符代换 | Agent Step3 | 跨表按依赖顺序零 LLM 执行 |
| 反模式自学习 + mini_regression | Agent Step4 | 失败沉淀成规则，越用越准 |
| 语义归一 | Merge | 假冲突归零（0.5 → 0） |
| 稀疏化 / 向量化 / 哈希索引 | Merge | 大表 compare 48s → 6.4s |
| 公式缓存保护 | Merge | 公式不坏，扫描 6.1s → 0.05s |
| ID 重映射 + 引用完整 | Merge | 冲突 ID 自动重映射，不留断链 |
| AI 批量建议 + 后台预取 | Merge | N 次往返 → 1 次批量调用 |
